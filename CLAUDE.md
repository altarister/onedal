# 1DAL

인성콜 / 화물24시 배차망에서 조건에 맞는 콜(꿀콜)을 자동 선점하고, 서버가 수익성을 판정해
기사님이 최종 결재하는 AI 배차 관제 시스템.

## 구성 (3-앱 MSA)

| 앱 | 역할 | 스택 |
|---|---|---|
| `onedal-app/` | 안드로이드 스캐너 — 배차앱 화면 스크래핑 + 자동 터치 | Kotlin, AccessibilityService, HttpURLConnection |
| `onedal-web/server/` | 판정 엔진 — 파싱·카카오 경로·요율 연산 | Express 5, better-sqlite3, Socket.IO (port 4000) |
| `onedal-web/client-app/` | 관제탑 — 기사님이 KEEP/CANCEL 결재 | Vite 6, React 19, Tailwind v4, Capacitor (port 3000) |
| `onedal-web/logbook/` | 운행일지 대시보드 | Vite + React |
| `onedal-web/shared/` | `@onedal/shared` 공통 DTO (앱 ↔ 서버 규격) | TypeScript |

통신: 앱 → 서버는 REST(`POST /api/scrap`), 서버 → 앱은 **응답 꼬리에 명령을 싣는 피기백**.
서버 ↔ 관제탑만 Socket.IO. (모바일 웹소켓 끊김을 피하려는 의도된 설계)

> **규칙은 두 층에 있다.** 이 문서의 [이건 버그가 아니라 규칙이다](#이건-버그가-아니라-규칙이다--고치기-전에-읽을-것) 는
> **앱 경계를 넘는 것**만 담는다. 앱 안에서만 참인 것은 각 폴더의 `CLAUDE.md` 에 있다 —
> [onedal-app](onedal-app/CLAUDE.md) · [server](onedal-web/server/CLAUDE.md) ·
> [client-app](onedal-web/client-app/CLAUDE.md) · [shared](onedal-web/shared/CLAUDE.md) ·
> [onedal-web](onedal-web/CLAUDE.md)(검증 스크립트)

## 명령

| 목적 | 명령 |
|---|---|
| 로컬 기동 (web+api) | `cd onedal-web && pnpm dev` |
| 서버 타입 체크 | `cd onedal-web/server && npx tsc --noEmit` |
| 서버 테스트 | `cd onedal-web/server && npx jest` |
| 클라 타입 체크 | `cd onedal-web/client-app && npx tsc -b` |
| 앱 빌드 | `cd onedal-app && ./gradlew assembleDebug` (JDK: `export JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home"`) |
| 앱 컴파일만 | `cd onedal-app && ./gradlew :app:compileDebugKotlin` |
| **서버 정체 확인** | `curl -s localhost:4000/api/health \| python3 -m json.tool` |

로컬 개발 시 DB는 `server/local.db`(실서버는 `data.db`)로 자동 분리됨.
`client-app/.env`에 `VITE_API_URL`이 있으면 Vite 프록시가 깨지므로 로컬에서는 비워둘 것.

## 커밋 전 필수

`tsc --noEmit`(server) · `tsc -b`(client) · `npx jest` · **`./gradlew :app:compileDebugKotlin`**(앱 코드를 고쳤다면)
이 전부 통과해야 커밋한다. 실패 시 커밋 금지.

> 🔴 **버그를 고칠 때는 순서를 뒤집는다** (2026-08-18, 기사님 지시로 신설)
> `진단 → 이 버그를 잡았을 검사를 먼저 만들어 **빨간불 확인** → 수정 → 초록불`
> 검사가 빨간불이 안 뜨면 **진단이 틀린 것**이다 — 고치지 말고 진단으로 돌아간다.
> 고친 뒤에는 [docs/버그_대장.md](docs/버그_대장.md) 에 **클래스**와 함께 적는다.
> 같은 클래스가 두 번 나오면 인스턴스가 아니라 **그 클래스를 없애는 수정**을 한다.

무엇을 건드렸느냐에 따라 하나씩 더 붙는다.
**각 검사가 무엇을 잡는지와 실행 함정은 [onedal-web/CLAUDE.md](onedal-web/CLAUDE.md) 에 있다.**

| 건드린 것 | 추가로 돌릴 것 |
|---|---|
| 콜 흐름 (상태·복구·적재·정산) | `cd onedal-web && pnpm scenario` |
| 소켓 이벤트 | `cd onedal-web && pnpm audit:socket` |
| `shared/` · DB 스키마 | **기존 DB 사본**으로 부팅 스모크 (빈 DB 는 문제를 숨긴다) |

> 앱 컴파일이 필수인 이유: 2026-08-09에 `main`이 컴파일조차 안 되는 상태였다는 걸
> 뒤늦게 발견했다(`InsungParser`의 import 누락). 서버는 tsc로 매번 확인하는데
> 앱은 검증 수단이 없어 아무도 몰랐다.

## "무엇이 실제로 돌고 있는가" 확인 (중요)

이 프로젝트에서 반복적으로 시간을 잡아먹은 문제다. 고쳤다고 생각했는데 옛 코드가 돌고 있어
잘못된 결론을 내리는 일이 하루에 여러 번 있었다. 검증 전에 항상 확인할 것.

| 대상 | 확인 방법 | 어긋났을 때 |
|---|---|---|
| 서버 | `curl localhost:4000/api/health` → `bootedAt`, `git.commit` | `tsx watch`가 변경을 놓친 것. `Ctrl+C` 후 `pnpm dev` |
| 앱 | 대시보드 상단 `📦 v...` 또는 `adb shell dumpsys package com.onedal.app \| grep versionName` | `adb install -r` 재설치 |
| 관제웹 | `localhost:3000`으로 접속 | `localhost:4000`은 옛 `dist/` 빌드가 뜬다 |


## 도메인 용어

> 🔴 **용어의 유일한 원천은 [docs/용어집.md](docs/용어집.md) 다** (기사님 확정 2026-08-17).
> 사냥→콜 필터/콜 잡기 · 데스밸리→**안전취소** · 회랑→**경유** · 눈높이→**콜할인율** ·
> 여유→**버퍼**(상차/경유 — 퉁치지 않는다) · 미상→**배차값없음** · `huntPhase`→`callTarget`(타겟).
> **2026-08-17 개명 완료** — 코드·DB·앱 전부. `tests/rules/glossary.test.ts` 가 옛말을 금지한다.

### 콜을 부르는 법 — **조합해서 만든다** (기사님 확정 2026-08-16)

```
타겟명  +  첫짐(생략) / 합짐N  +  (후보)  +  콜
```

| 타겟명 | 첫짐 | 첫 합짐 | 두 번째 합짐 |
|---|---|---|---|
| 노선 (~~목적지~~ 2026-08-17) | `노선콜` | `노선합짐1콜` | `노선합짐2콜` |
| 관내 | `관내콜` | `관내합짐1콜` | `관내합짐2콜` |
| 복귀 | `복귀콜` | `복귀합짐1콜` | `복귀합짐2콜` |

**심사 전(안전취소 30초 안에 결재 안 난 콜)은 `후보`를 넣는다** — `노선 합짐1 후보콜`.
타겟명은 `callTarget`(DEST·LOCAL·HOME), 인덱스는 `getActiveCalls()` 순서와 대응한다.
코드의 `securedOrder` 가 **후보콜**이다.

> 🔴 **`본콜` 은 폐기한다.** 한 단어가 세 뜻으로 쓰이고 있었다 —
> `routeComposer` 는 *잡아 둔 첫 콜*, `kakaoService` 는 *첫짐*, 그리고
> `OrderEvaluator` 의 `본콜 좌표 누락` 은 실제로 **후보콜**이었다.
> 그래서 기사님이 *"내가 KEEP 한 첫 콜에 문제가 있나?"* 로 잘못 읽으셨다.

- **콜 필터** — 앱이 콜을 **집기 전**에 거르는 조건. 서버가 만들어 앱에 피기백으로 내려보낸다
- **판정 기준** — 서버가 콜을 **집은 뒤** 색(🔵🟢🟡)을 정하는 값. **앱에 내려가지 않는다**
  · 둘은 **완전히 분리·격리되어 각각 따로 작동한다**
  · `오늘만` 버튼은 **콜 필터에만** 있다. 판정 기준은 바꾸면 계속 적용된다

- **꿀콜 / 똥콜** — 수익성이 좋은 / 나쁜 콜
- **선빵, 광클** — 경쟁 기사보다 먼저 확정 버튼을 누르는 것
- **안전취소** — 확정 후 패널티 없이 취소할 수 있는 30초 (~~데스밸리~~). 이 사이에 서버가 수익성을 판정
- **합짐** — 기존 경로에 다른 콜을 경유지로 병합
- **경유** (~~회랑~~) — 경로에 목적지를 추가한 우회. 경유 반경 = 경로 주변에서 콜을 받는 폭
- **피기백(Piggyback)** — HTTP 응답 본문 꼬리에 서버 명령(KEEP/CANCEL, 새 필터)을 실어 보냄
- **dispatchPhase** — `STANDBY`(첫짐 탐색) → `GATHERING`(합짐 수집) → `DELIVERING`(운행 중)
  - ⚠️ 일부 문서의 `DRIVING`은 오기. 실제 enum 값은 **`DELIVERING`**

## 문서 신뢰도 ⚠️ 중요

**코드와 다른 문서를 근거로 작업하지 말 것.** 이 레포의 문서는 계획을 완료로 기술한 적이
여러 번 있다 — 없는 방어를 믿고 판단하면 그 전제가 통째로 틀린다.

**어느 문서를 믿어도 되는지는 각 앱의 `CLAUDE.md` 에 있다** (그 폴더에서 일할 때 읽힌다).
여기에 또 적으면 목록이 두 벌이 되고, 신뢰도가 바뀔 때 한쪽만 고쳐진다.

`/docs` 최상위(`architecture/` 포함)는 **검증된 적이 없다.** 원칙은 대체로 유효하지만
구현 서술은 낡은 것이 섞여 있으므로, 쓰기 전에 코드와 대조할 것.
(예: `filter_system.md` 맨 위에 어디가 옛 구조인지 대조표를 붙여 뒀다)

**🔄 진행 중인 정비 계획: [todo.md](todo.md)** — 작업 시작 전 반드시 확인

## 이건 버그가 아니라 규칙이다 ⚠️ 고치기 전에 읽을 것

**반복된 사고의 형태는 늘 같다 — 일부러 비대칭으로 둔 것을 "빠뜨린 것"으로 읽고 고쳤다.**
이 레포에서 비대칭은 대개 결정이다. **비대칭을 발견하면 고치기 전에 "왜 비대칭인가"의 답을 찾고,
못 찾으면 묻는다.**

규칙은 `server/tests/rules/dispatchRules.test.ts` 가 강제한다. 그 테스트가 깨졌다면
먼저 **"내가 규칙을 어긴 건 아닌가"** 를 의심할 것.

**① 콜의 주인은 기사님이다**
- 서버는 콜을 **자동으로 버리지 않는다.** 요율 미달이어도 사유만 표시하고 기사님이 판단
- **직접콜(MANUAL)은 심사하지 않는다** — 기사님 의도다. 안전취소도 LIST 이탈 정리도 없는 건 설계다
- **KEEP 된 콜은 절대 취소하지 않는다**
- 역할 분담: 앱은 *"일단 잡아와라"*, 서버는 *"수익성을 판단한다"*, 기사님이 *"최종 결재한다"*

**② 안전장치는 겹쳐 둔다, 빼지 않는다**
- 앱의 30초 안전취소는 **최후의 안전장치다. 절대 제거하지 않는다**
- 서버는 앱이 **ACK 할 때까지 판결을 지우지 않는다** (한 번 보내고 지우면 유실 시 영구 유실)
- 판결에는 **orderId 를 반드시 싣고** 앱이 대조한다 (오더A 응답이 오더B에 적용되던 사고)
- 타이머는 **ID 를 저장해 취소 가능하게** 한다 (좀비 타이머)
  > ⚠️ `orders.ts` 의 30초 타이머는 **아직 안 지킨다.** `detail.ts` 35초와 같은 콜을 경쟁적으로
  > 정리한다(원 문서의 "결함 #3 이중 타이머 경쟁"). 고칠 것은 [todo.md](todo.md) 에
- HTTP 를 **물고 기다리지 않는다** — 즉시 응답 + 피기백

**③ 상태를 저장하지 말고 데이터에서 파생시킨다**
- **파생값을 만들었으면 그 입력도 한 곳에서 만든다** (같은 사고 3회: 경유 4벌·상태목록 3벌·시별칭)
- 어제 상태가 오늘 되살아나지 않는다 (영업일 전환)
- **기본값의 원천은 DB 다.** 코드에 흩어진 `?? 리터럴` 폴백은 **잔재이며 늘리지 않는다**
  > 2026-04-23 에 "폴백 제거 → DB 중앙화"를 했다고 기록돼 있으나 **코드는 되돌아와 있다**
  > (`settings.ts` `?? 10` · `dispatchEngine.ts` `?? DEFAULT_CORRIDOR_RADIUS_KM` 등).
  > 그러니 남은 폴백을 "규칙 위반"이라며 지우지 말 것 — 지금은 그게 동작의 일부다.
  > **앱의 기본값은 예외다** — 서버 미응답 시를 위한 오프라인 안전망이라 의도적으로 둔다

**④ 데이터를 변조해서 동작을 바꾸지 않는다**
- 합짐이라고 상차반경을 `999km` 로 덮어쓰지 않는다 — **룰로 건너뛴다**
- 없는 숫자를 지어내지 않는다 (`0` 이 아니라 `null`)
- **빈 필터는 "제한 없음"이 아니라 "고장"이다**

**⑤ 앱은 느슨하게 올리고, 서버는 정밀하게 구분한다** ⚠️ 코드를 짤 때마다 상기할 것

기사님(2026-08-15): *"필터는 최대한 느슨하게 해서 콜을 많이 올리는 거고, 서버가 평가할
때는 **최대한 많은 경우의 수를 넣어서 정확한 구분을 추천**하는 것이 이 앱의 목적이야."*

| | 앱(1차 필터) | 서버(판정) |
|---|---|---|
| 목적 | **놓치지 않는 것** | **정확히 구분해 추천하는 것** |
| 태도 | 느슨하게 — 애매하면 올린다 | 정밀하게 — 아는 것을 다 넣는다 |
| 결과 | 후보를 많이 만든다 | 순위와 이유를 붙인다 |

🔴 **"정밀하게"는 "많이 떨어뜨린다"가 아니다.** 지금 서버는 모르는 값을 **불리하게 지어내
탈락**시킨다 — 그건 구분이 아니라 포기다. 모르면 **모른다고 표시하고 기사님께 넘긴다**
(규칙 ① 콜의 주인은 기사님이다).

🔴 **물리적으로 불가능한 콜은 서버가 미리 버리지 않는다.** 기사님이 통화로 시간을 조정하고,
안 되면 사무실에 전화해 취소한 뒤 관제웹의 **사무실 취소** 버튼을 누른다
(`PinnedRouteCard` 의 `⋯ 이 콜 처리` 안에 접혀 있다 — 되돌릴 수 없어서 일부러 숨겼다).
그래서 명세 §1 이 *"시간 약속은 통화에서 확보된다"* 고 못박은 것이다.

**⑤-1 돈은 앱이 이미 걸렀다 — 서버가 다시 세지 않는다**

기사님(2026-08-15): *"1차 필터에서 요금을 잘 걸러서 왔다면 충분히 좋은 단가이니
**중복해서 단가를 계산해야 할 필요가 있을까**."*

앱이 `요금 ≥ 배송거리 × 단가(차종)` 로 이미 통과시킨 콜만 올라온다.
그러므로 서버 판정의 일은 **"이 콜이 돈이 되는가"가 아니라 "지금 경로에 붙일 때 얼마나
좋은가"** 를 구분하는 것이다 — 우회 비용으로 순위를 매긴다.

**⑤-2 모르는 값은 *일반값*으로 계산하고 "미확인"으로 **표시**한다**

기사님(2026-08-15): *"일반적인 값을 넣어두고 미확인으로 표시하면 좋을 듯. 그럼 계산은
일반값으로 하면 꿀콜이 되어 **잡은 후 내가 전화하여 확정**하면 되니까."*

🔴 **불리한 값을 넣지 않는다.** 모른다고 나쁜 쪽으로 잡으면 **잡을 수 있었던 콜을 놓친다** —
잡은 뒤 통화로 확정하는 것이 이 제품의 순서다. 반대로 낙관도 아니다: **가장 흔한 값**을 쓴다.
근거는 실제 신고 데이터다 (2026-08-15 기준 **수작업 44건 · 지게차 12건 — 78%가 수작업**).

⚠️ 규칙 ④(*"없는 숫자를 지어내지 않는다"*)와 어긋나 보이지만 아니다 —
**화면에 "미확인"이라고 함께 적으므로 숫자가 거짓말을 하지 않는다.** 표시 없이 값만 쓰면 위반이다.

**⑤-3 판정의 *색*이 곧 결정이다**

기사님(2026-08-15): *"나는 KEEP 버튼의 내용보다는 **파란색, 녹색이면 너가 만든 코드를 믿고
바로 잡을 거야**."*

🔵 꿀 · 🟢 보통 · 🟡 똥 · 🔴 연산 실패 — 기사님은 **색만 보고 1~2초 안에 누른다.**
설명 문구는 그다음이다. 그러므로 **색을 틀리는 것이 이 시스템의 가장 큰 사고다.**
값이 애매하면 문구를 늘리지 말고 **색을 정확히 내는 데** 힘을 쓴다.

**⑤-4 값을 만들기 전에 *네 가지*가 먼저 확정된다** ⚠️ 코드부터 짜지 않는다

기사님(2026-08-15): *"**어떻게 보여질지까지가 확인되어야** 수정을 진행할 거야.
이것도 개발 시 중요한 기준이니까 항상 상기할 수 있도록…"*

새 값(설정·기준·임계값)을 도입하거나 고칠 때는 **아래 넷을 먼저 문서로 확정**한다.
넷 중 하나라도 비어 있으면 **코드를 시작하지 않는다.**

| | 무엇을 정하나 | 비어 있으면 생기는 일 |
|---|---|---|
| **① 스키마** | 어느 테이블·어느 칸에 사는가 | 코드 상수로 태어나 영영 못 바꾼다 (지금 판정 기준 11개가 그렇다) |
| **② 값** | 기본값은 얼마이고 **근거는 무엇인가** | 누군가의 감으로 정해지고 아무도 못 고친다 |
| **③ 시점** | **언제 읽고 언제 쓰는가** (로그인·국면 전환·저장·자정) | 두 그릇이 갈라진다 (2026-08-12 `설정=파주 / 필터=용인`) |
| **④ 화면** | 어디에 어떤 이름으로 보이고, 고칠 수 있는가 | 화면이 조용히 거짓말한다 |

🔴 **"일단 만들고 나중에 노출한다"를 하지 않는다.** 이 레포에서 그렇게 태어난 값들이
지금 코드에 박혀 있고, 그 값이 **기사님이 1~2초 만에 누르는 색**을 정하고 있다.

**⑤-5 상차 여유와 하차 여유는 다른 것이다 — 퉁치지 않는다** ⚠️ 자꾸 잊는다

기사님이 **여러 번** 말씀하셨는데 반복해서 흔들렸다 (2026-08-16 재확정).

| | 무엇의 여유인가 | 무엇을 답하나 |
|---|---|---|
| **상차지 여유** | **콜 대기 시간의 여유** | *"이 자리에서 얼마나 더 기다리며 **합짐을 잡을 수 있나**"* |
| **하차지 여유** | **배송 여유** | *"돌아가도 약속 시각을 지킬 수 있나"* |

🔴 **여유는 입력값이 아니라 계산 결과다.** `마감 미확인 여유 = 90분` 같은 **단일 상수를
만들지 않는다.** 서버가 마감에서 주행과 정차를 빼서 스스로 구한다.

```
09:00  콜을 잡음
10:00  상차 마감            ← 콜 잡은 시각 + 60분 (규칙)
       주행 20분 + 상차 15분 = 35분
▶ 여유 25분   ← 기사님이 이 자리에서 합짐을 잡을 수 있는 시간
```

🔴 **상차 마감은 "물건을 실어 *보내는*" 시각이다 — 상차 시간을 포함한다.**
기사님: *"화주의 생각은 보통 **여기서 물건 실어서 몇 시에 보낼 수 있을까**야. 그러니 상차 시간을
포함해야 해. 이건 그냥 룰이라고 생각하고 너의 관념에 픽스시켜."*
→ 상차지 **도착** 시각이 아니라 **상차 완료** 시각이므로 역산에 상차 정차가 들어간다.
```
10:00  상차 마감          ← 상차를 마치고 보내는 시각
09:45  상차지 도착        ← 10:00 − 상차 15분
09:25  출발               ← 09:45 − 주행 20분   ▶ 대기 25분
```
화면에는 **둘 다** 적는다 — `09:25까지 출발 (주행 20, 상차 15, 대기 25분)`

**마감은 통화 없이도 서버가 넣는다** (업계 관행). 통화로 실제 시각을 들으면 그게 이긴다.
```
상차 마감 = 콜 잡은 시각 + 60분        (교통량 여유 포함)
하차 마감 = 상차 마감 + 주행 + 30분     (휴게소 등 여유)
```

🔴 **그래서 마감에서 빼는 값도 다르다.**
```
상차 마감  →  현위치→상차지 주행(approach) + 상차 정차   ← 전체 주행을 빼면 안 된다
하차 마감  →  전체 주행 + 상하차 두 번
```
2026-08-16 새벽에 이걸 어겨 사고가 났다 — 상차 약속에서 **전체 주행 82분**을 빼
여유 **−71분**을 만들었고, 그 뒤 온 `목적지 합짐1 후보콜` 이 **전부 막혔다.**

⚠️ 정차 시간(상차 15분·하차 10분)과 **마감은 다른 값이다.** 섞지 않는다.

**⑥ 시퀀스를 압축하지 않는다**
- 콜 처리는 **6단계**다. 한 번에 두 단계를 건너뛰지 않는다
  (기사님: *"두 개를 한 번에 가는 건 기준이 흔들린다"*)

## 지금은 테스트 단계다 — **마이그레이션을 고려하지 않는다**

기사님(2026-08-14): *"지금 테스트라 기존 데이터에 대한 마이그레이션은 고려하지 않는다.
**로컬 라이브 모두 다.** 이건 어디다 명기해 두고 항상 염두해 둬.
나중에 라이브 내가 직접 사용하게 되면 알려줄께."*

- 스키마·상태값을 바꿀 때 **기존 행을 보존하는 마이그레이션을 설계하지 않는다.** 지우고 다시 만든다
- **로컬(`local.db`)·실서버(`data.db`) 둘 다** 해당한다
- 🔴 **이 전제는 기사님이 "이제 라이브를 직접 쓴다"고 말하는 순간 끝난다.**
  그때부터 데이터는 기사님의 실제 매출 기록이라 마이그레이션이 필수가 된다.
  **그 말을 듣기 전까지만 유효하다** — 들으면 이 절을 지우고 마이그레이션 규칙을 쓴다

⚠️ 그렇다고 `db.ts` 의 조건부 `DROP TABLE` 을 되살리라는 뜻은 아니다 (부팅 경로에서 날아간다).
   지우는 것은 **손으로, 의도적으로** 한다.

## 관제앱은 **업무 단위**다 — 정산 단위가 아니다

기사님(2026-08-14): *"관제앱은 업무단위로 보는 것이 맞고, 정산은 관제앱에서 만들어진
데이터로 **정산 페이지에서 따로** 컨트롤하는 것이 맞을 듯하다."*

| | 관제앱 | 정산 페이지(별도) |
|---|---|---|
| 단위 | **일을 끝냈는가** | 돈을 받았는가 |
| 보이는 것 | **그냥 매출** | 미수금 · 기름값 · 톨비 · 순이익 |
| 콜의 끝 | `ORDER_DELIVERED`(하차 완료) | `ORDER_COMPLETED`(정산 완료) |

→ 관제앱에 **정산 개념을 끌어들이지 않는다.** 하차하면 관제앱에서는 끝난 콜이다.
   정산 대기라고 "아직 활성"으로 세면 적재·필터가 전부 틀어진다.

## 하지 말 것

- **승인 없이 배포하지 않는다.** `git push origin main`이 `onedal-web/**` 변경 시 **EC2 자동 배포를 트리거**한다 (`.github/workflows/deploy.yml`)
- 기사님 운행 시간(주간)에 서버를 배포하지 않는다
- 파일·폴더 삭제 또는 이동 전에 반드시 확인을 받는다
- 코드에 들어가지 않은 것을 문서에 "완료"로 쓰지 않는다 (**이미 네 번 발생**).
  계획은 `todo.md`에, 구현된 것만 아키텍처 문서에 쓴다
  > 네 번째: `safety_mode_architecture.md` 가 Phase 3(서버의 matchType 불신)을 현재형으로
  > 적어 뒀는데 코드에 없었다. 없는 방어를 믿은 채로 2026-08-12 유령 콜 사고가 났다

## 작업 스타일

- 한국어로 대화한다
- 되돌릴 수 없는 작업 전에는 먼저 묻는다
- 이해가 안 되면 추측하지 말고 질문한다
- 논리적 근거를 제시하고 합의된 뒤에 코드를 수정한다
