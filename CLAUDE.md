# 1DAL

인성콜 / 화물24시 배차망에서 조건에 맞는 콜(꿀콜)을 자동 선점하고 (셋째 배차망 카카오 픽커는 앱이 읽어 알린다), 서버가 수익성을 판정해
기사님이 최종 결재하는 AI 배차 관제 시스템.

📅 **사업자가 아직 없다** — 인성·화물24시 앱은 설치할 수 없어 그 두 배차망의 DB 콜은 **전부 시뮬레이터가 만든 가짜**다.
**카카오 픽커만 실물 앱**이다. 다만 콜을 잡는 것(수락)은 기사님이 필드에서 그 일을 할 수 있는 라이브 조건에서만 하고,
집에서 작업할 때는 **콜 확정 전까지만** 진행한다 — 집에서 픽커 콜을 수락하게 만드는 시험·코드는 만들지 않는다.
콜 수·분포를 실측인 척 인용하지 않는다. 임계 경로는 코드가 아니라 사업자다.

## 일하는 순서

🔴 **일을 받으면 먼저 종류를 가르고, 그 줄의 README 장을 읽고 시작한다. 마치면 그 줄의 뒷칸으로 확인한다.**
[README.md](README.md) 는 자동으로 읽히지 않는다 — 여기서 가리키는 장을 열어야 보인다.

| 일의 종류 | 시작 전에 읽는 README 장 | 마친 뒤 확인하는 README 장 |
|---|---|---|
| **수정** (코드 · 문서) | 「구성」 · 「이건 버그가 아니라 규칙이다」 · 「짝이 있는 것」 · 「지금은 테스트 단계다」 · 「이 문서 · 코드 주석에 무엇을 적나」 | 「커밋 전 필수」(게이트 전부) · 「여럿이 한 작업 트리에서 일한다」(담을 파일) |
| **검사 · 시험** (시나리오 · 폰 · 주행) | 「무엇이 실제로 돌고 있는가」 · 「포트」 · [onedal-web/CLAUDE.md](onedal-web/CLAUDE.md) 스크립트 표 | 스크립트 표의 «못 잡는 것» 칸 — 초록불은 «잡는 것»만 맞다는 뜻이다 |
| **기획** (계획서 · 설계) | 「이건 버그가 아니라 규칙이다」 ⑤-4(새 설정값의 다섯) · 「구성」 · 「도메인 용어」 | 아래 «계획서에는 넷을 넣는다» |

앱 안에서만 참인 규칙은 그 폴더의 CLAUDE.md 에 있다 — [onedal-app](onedal-app/CLAUDE.md) · [server](onedal-web/server/CLAUDE.md) ·
[client-app](onedal-web/client-app/CLAUDE.md) · [shared](onedal-web/shared/CLAUDE.md) · [onedal-web](onedal-web/CLAUDE.md)(검증 스크립트).
`onedal-sim` · `onedal-map` 은 그 폴더의 README 가 안내다.

- **목적은 하나다 — 좋은 콜을 많이, 노이즈는 적게.** 시킨 대로 하면 좋은 콜을 놓치거나 노이즈가 느는 경우가 보이면
  코드 전에 «이런 경우가 있습니다»로 알린다
- **순서·방법·검사·커밋은 묻지 않고 정한다.** 묻는 것은 **운행 동작이 바뀌거나 새 설정값·저장 칸이 생기는 수정**뿐이다 —
  근거를 제시하고 합의된 뒤에 한다. 되돌릴 수 없는 작업 전에는 묻는다 · 이해가 안 되면 추측하지 말고 묻는다
- 🔴 **push 와 파일·표 지우기는 기사님이 먼저 말씀하실 때만 한다 — 먼저 제안하지도 않는다**
- **문제로 보이면 고치기 전에 효율로 따진다** — ① 실주행에서도 나나, 시험 도구에서만 나나 ② 좋은 콜·노이즈·기사님 손에 무슨 영향인가.
  그다음 다른 에이전트에게 의견을 묻고, 다시 생각한 뒤 기사님께 올린다
- **기사님이 «하나씩»이라고 하시면 한 건 고치고 멈춘다** — «했다 / 결과 / 다음은 이것» 세 줄로 보고하고 답을 기다린다.
  **답이 없는 것은 승인이 아니다** — 조용하면 멈춘 채로 기다린다 (긴 보고를 안 읽으신 사이에 잘못된 방향으로 셋을 더 쌓은 적이 있다)
- 🔴 **「A일 때는 B를 안 본다」는 문장을 쓰게 되면 멈춘다** — 그 문장은 표의 한 칸이고, 표는 반드시 빈다.
  각 부품이 자기에게 필요한 사실을 직접 보게 한다 — 뭉친 이름(상태·모드·단계)은 로그와 화면 표시에만 쓴다.
  내가 어제 만든 것을 오늘 더 정확하게 다듬고 있으면 방향을 잘못 잡은 신호다 — 다듬기 전에 「이 구조가 맞나」를 먼저 여쭙는다
- **계획서에는 넷을 넣는다** — ① 단계마다 «무엇이 참이면 끝인가» 한 줄 ② 「지금은 이렇게 돈다」 ③ 새 값을 쓰기 전에 지금 DB 값 대조
  ④ 둘 이상을 나란히 놓은 순위 검산. «맞게 도는 것도 있습니다»라고 세게 되면 기획이 결과를 정하지 못한 것이다
- **만드는 도중에는 검사를 돌리지 않는다** — README 「커밋 전 필수」의 «버그는 순서를 뒤집어 고친다»의 **빨간불 확인 한 번**과 **커밋 직전 게이트 한 번**이면 족하다.
  그 사이 단계마다 다시 돌리면 응답이 매번 길어져 기사님이 화면을 못 보신다.
  바꾼 파일을 읽는 검사는 `grep -rl <경로>` 로 서버 `tests/` · 관제웹과 shared 의 검사 파일 세 곳에서 찾는다 —
  검사가 소스 글자를 읽으므로 이름으로는 못 찾는다. `pnpm` 명령은 jest 와 동시에 돌리지 않는다
- **시험 도구도 방향을 바꾸지 않는다** — 모의 주행·시나리오가 기사님이 누르지 않은 버튼·방향(복귀 등)을 누르지 않는다. 막히면 그 자리에서 기다린다
- **문제지는 지금 서버 값에 맞춘다** — 시험 때문에 기사님께 필터·설정을 바꾸시라고 하지 않는다. 먼저 지금 값을 읽는다
- 🔴 **식별자(변수·함수·클래스 이름)는 영문만 쓴다** — 주석과 화면에 보이는 문자열은 한국어가 맞다.
  한글 이름은 검색·자동완성·리팩터와 감사 스크립트에서 빠지기 쉽다. `identifierAscii` 검사가 문다
- 🔴 **새 폴더를 만들기 전에 기사님께 묻는다** — README 「구성」 표와 실제 폴더가 갈라지면 그 문서가 매 세션 틀린 구성을 가르친다
  - 묻는다: 지금 없는 폴더를 새로 만들 때 (최상위 · 앱 안 전부) · 안 묻는다: 있는 폴더에 파일을 더할 때
  - 만들라고 하시면 **같은 커밋에서 README 「구성」에 한 줄 더한다** (`layoutListed` 검사가 문다)

## 기사님과 말하는 법

- 한국어로 말한다 — **도구 사이의 진행 안내·명령 설명까지** 한글이다. 식별자·파일 이름·명령만 그대로 둔다
- **주어를 넣는다** — «앱이»·«서버가»·«관제웹이»·«제가». 셋이 같은 값을 다르게 쓰니 주어가 빠지면 어느 쪽 이야기인지 모른다
- 🔴 **내가 만든 말을 쓰지 않는다** — 은유·줄임말(«판»·«밭»·«수문»·«닻» 같은 것)은 한 번에 안 읽힌다. 풀어 쓰거나 괄호로 설명한다.
  용어는 `glossary.test.ts` 의 말을 쓴다. **코드·로그가 쓰는 은유도 옮길 때 풀어 쓴다** — 로그 줄을 붙여 넣기 전에 그 안의 말부터 바꾼다
- **설명은 기사님이 보는 화면과 하루의 말로 한다** — 탭·칸·버튼·콜·상차·집. 파일 이름·함수 이름·자료 구조 이름은 코드를 물으실 때만 꺼낸다
- 🔴 **물으시면 답부터 한다 — 일을 시작하지 않는다**
  「~겠지?」·「~가능한가?」·「~맞아?」로 끝나면 묻는 것이다. **그 질문에만 한두 문장으로 답하고 「~할까요?」로 되묻는다.**
  운행·라이브 중에는 더 짧게 — 묻지 않은 제안·표·선택지 목록을 붙이지 않는다.
  혼자 «할까 말까»를 토론하지 않는다 — 고를 일이면 고르시게 내놓는다
- 🔴 **질문은 그 자리에서 완결한다** — 질문 안에 ① 원문(무엇을 하자는 건지 그 문장) ② 부딪히는 상대 문장 ③ 고를 것 «가/나» ④ 내 추천을 다 쓴다.
  «1번»·«그 줄»처럼 다른 곳을 가리키지 않는다 — 기사님이 위로 올라가 찾거나 되물으면 실패다
- 🔴 **고를 일이 아니면 묻지 않고 «이렇게 합니다»로 정해 쓴다** — 부딪힌다고 느끼면 부딪히는 두 문장을 나란히 놓고 내 결정과 까닭 한 줄을 적는다.
  기사님이 정하실 것은 운행 동작이 바뀌는 것과 새 설정값뿐이다 (위 「일하는 순서」)
- 🔴 **답을 받으면 한 줄로 되짚고 시작한다** — «‹가›로 알아들었습니다. 지금 하는 일은 ○○입니다.» 답은 «가/나»·«예/아니오»로 받을 수 있게 만든다.
  오타나 잘못 말씀하셔도 다른 일로 가지 않게 하는 장치다
- **묻기 전에 «이 답을 들으면 내가 무엇을 하나»부터 확인한다** — 할 일이 없으면 질문이 아니다. 한 줄에 질문 하나.
  **설정값은 묻지 않는다** — «지금 얼마인가»는 DB 를 읽고, «얼마여야 하나»는 기사님이 설정창에서 정하신다. 내 몫은 그 값이 실제로 돌아가게 잇는 것이다
- **«뺄까요?»만 묻지 않는다** — ① 값의 출처(누가 정하나) ② 읽는 곳 ③ 빼면 잃는 것 ④ 왜 거기 있었나, 네 줄을 먼저 드린다. 재료 없이는 답하실 수 없다
- **확실한 것은 다시 묻지 않는다** — 이상한 곳만 묻는다. 기호·표 대신 **기사님이 쓴 문장을 나란히 놓고** «여기가 다르다»로 묻는다
- 기사님 말의 «+»·«와»·«랑»은 합집합인지 교집합인지 **코드 전에 한 줄로 확인한다** — 해석이 틀리면 필터가 좋은 콜을 막는다
- 🔴 **맡기신 일을 마치면 «물으신 것 → 결과 → 남은 것» 순서로 보고한다**
  일이 길어지면 어느 질문 때문에 시작했는지 잊으신다. 그래서 시작할 때 **맡기신 질문을 원문 그대로** 기억한다
  - ① 그 질문 인용 (+ 더 큰 작업 도중이었으면 한 줄) ② 결과 한두 줄 ③ 남은 것 (+ 큰 작업의 다음 자리)

## 하지 말 것

- 🔴 **승인 없이 `git push` 하지 않는다** — `main` 에 `onedal-web/**` 또는 `onedal-sim/**` 변경이 올라가면 **실서버(EC2)에 자동 배포**된다 (`.github/workflows/deploy.yml`)
- 🔴 **`push` 승인을 받았어도 올리기 직전에 «지금 운행 중이신가»를 묻는다** — 운행 중에는 서버를 배포하지 않는다
- 파일·폴더를 지우거나 옮기기 전에 확인을 받는다
- 🔴 **수락 뒤 픽커 화면 캡처를 레포에 올리지 않는다** — 남의 집 주소·동호수·전화번호·문 앞 사진이 가려지지 않았다.
  git 이력에 한 번 들어가면 지워도 남는다. `.gitignore` 가 `ex_images/카카오픽커/실물_2026/` 을 막는다 (수락 전 상세 화면은 된다)
- 🔴 **코드에 없는 것을 문서에 «완료»로 쓰지 않는다** — 구현된 것만 설명 문서에 쓴다.
  없는 방어를 믿으면 막힌 줄 알고 사고가 난다
- 🔴 **코드와 다른 문서를 근거로 작업하지 않는다** — 문서(`CLAUDE.md` · README · 스킬 · 코드 주석)는 쓰기 전에 코드와 대조한다.
  `pnpm audit:docs` 는 없는 파일·식별자·링크를 잡지만 **문장이 틀린 것은 못 잡는다**
