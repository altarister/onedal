# onedal-web — 웹 워크스페이스 (pnpm)

`server` · `client-app` · `logbook` · `shared` 를 묶는 자리. **검증 스크립트가 여기 있다.**
루트 [CLAUDE.md](../CLAUDE.md) 가 먼저다.

## 검증 스크립트

| 명령 | 무엇을 잡는가 |
|---|---|
| `pnpm dev` | 로컬 기동 (web + api) |
| `pnpm scenario` | **실제 서버를 띄워 콜 생애를 끝까지 돌린다** (21건) |
| `pnpm drive` | **모의 주행 — GPS 를 재생해 정거장을 끝까지 걸어간다** (11건 · 40초) |
| `pnpm audit:socket` | 서버 `emit` ↔ 관제웹 `on` 대조 — 한쪽만 고친 것 |
| `pnpm audit:docs` | **문서가 코드와 다른 말을 하는가** — 없는 파일·사라진 식별자·옛말·죽은 링크 |
| `pnpm audit:dead` | **불려야 하는데 안 불리는 것** — 안 쓰이는 export · 세션에 없는 필드 · `session: any` |
| `pnpm map` | 흐름 지도 — 부팅 순서 · 이벤트 사슬 · REST · 상태 쓰기 (소스에서 추출) |
| `pnpm e2e:app` | **앱까지 포함한 왕복** — 화면 인식 → 필터 → 터치 → 확정 (7단계) |
| `pnpm ledger` | **장부를 눈으로 본다** — 콜별 단계(직접·자동·건너뜀)와 신고·약속 |
| `pnpm options` | **콜 옵션을 눈으로 본다** — 화면 선택지와 그 값 (단위 환산·상하차 분·보호 시간) |
| `pnpm reach` | **도달 계수 역산 보고서** — `reach_samples` 표본으로 1차식 적합. 필터에 자동 반영 안 됨 (계수 확정은 기사님) |

**`pnpm drive` 는 «주행에서만 보이던 것»을 책상에서 잡는다.**

2026-08-29 에 고친 것 중 셋이 **기사님이 나가 봐야만** 확인되는 것이었다 —
경로 순서 · 도착 감지 · 궤적의 `stop_type`. 그러면 **하루에 한 번밖에 못 고친다.**

```bash
cd onedal-web && pnpm drive          # 40초. 폰·카카오 키·개발 서버 전부 불필요
DRIVE_LOG=1 pnpm drive               # 서버 로그까지 보고 싶을 때
```

`scenario` 와 다르다 — 그건 **첫 상차지 하나만** GPS 로 찍고 나머지 단계는
버튼(`report-milestone`)으로 넘긴다. `drive` 는 **모든 정거장을 GPS 로 걸어간다.**
그래야 «정거장이 넘어가는가»(`nextStopOf`)를 실제로 본다.

문제지는 **2026-08-25 기사님 실측 그 상황**이다 (좌표도 그때 지점) —
곤지암 하차가 4km 앞인데 가남 상차가 30km 밖. 「상차 먼저」 규칙이면 30km 부터 간다.

- 전용 포트 **4014** · 전용 DB `drive.db` (빈 DB 로 시작해 끝나면 지운다)
- `server/local.db` 와 개발 서버(4000)는 **건드리지 않는다** (실행 전후 지문 확인함)
- ⚠️ 이벤트 이름을 틀리게 들으면 **멀쩡한 제품을 «안 울린다»고 오진한다** —
  만들면서 `auto-approaching` 으로 잘못 듣고 한 번 당했다 (실제는 `next-stop-approaching`).
  `pnpm audit:socket` 목록과 대조할 것

**`pnpm ledger` 는 "화면이 말하는 것"과 "장부에 남은 것"을 대조한다.**
기사님(2026-08-19): *"장부에 남았는지 내가 어떻게 알지?"*
이 레포에서 **화면은 메모리, 장부는 빈** 사고가 네 번 났다 (버그 대장 #4·#6·#8·#15).
저장 결과를 눈으로 볼 수 있어야 그 갈라짐을 먼저 잡는다.
출처가 함께 보인다 — `✍️ 직접`(실측 통계에 씀) · `🛰️ 자동`(참고값) · `⏭️ 건너뜀`(믿을 수 없음).

**`pnpm audit:dead` 는 "고쳤는데 안 돌고 있는 것"을 잡는다.**
2026-08-14 하루에 두 건이 나왔다 — `getActivePolyline`(지나온 구간 제거)·`getLastDropoffCoord`(도착 감지).
둘 다 **호출부는 멀쩡했고**, 세션에서 사라진 필드를 읽어 **몇 달째 `null` 만 반환**하고 있었다.
파라미터가 `session: any` 라 `tsc` 가 잡지 못했다. → **`any` 로 세션을 받지 않는다.**

**`pnpm scenario` 는 다른 검사가 전부 통과한 결함을 잡는 유일한 수단이다.**
2026-08-11 에 배포하면 안 되는 결함 6건이 `tsc`·`jest`·`vite build`·`audit:socket` 을
**전부 통과한 채로** 숨어 있었고 이걸 돌려서만 나왔다
(상차한 콜이 새로고침에 사라짐 · 짐 신고 무시 · 불일치 경고 미발생 · 착불 미기록).

→ **콜 흐름(상태·복구·적재·정산)을 건드렸다면 반드시 돌린다.**

## `pnpm e2e:app` — 앱 구간을 검사하는 유일한 수단

`scenario` 는 서버·관제웹만 본다. **앱이 화면을 읽고 → 필터를 걸고 → 터치하는 구간**은
검사할 방법이 없어서, 실제 배차망에 꿀콜이 뜨기를 기다려야 했다.

배차망 **시뮬레이터**(`~/reps/map/map`)를 띄우면 그 구간이 열린다.

```bash
cd ~/reps/map/map && pnpm dev      # 시뮬레이터 (172.30.x.x:5173)
# 폰에서 픽업 지역을 한 번 고른 뒤
cd onedal-web && pnpm e2e:app
```

7단계를 순서대로 확인하고, **어디서 끊겼는지**를 그대로 알려준다.
그중 5번째(`AUTO 로 보고`)가 가장 중요하다 — 2026-08-12 유령 콜 사고가 정확히 거기서 났다.

### 🔴 **반드시 크롬으로 열어야 한다**

| 브라우저 | 앱이 읽는 텍스트 |
|---|---|
| 크롬 `com.android.chrome` | **41개** — 페이지 안 글자가 전부 |
| 삼성 인터넷 `com.sec.android.app.sbrowser` | **2개** (URL·탭 수). **웹 콘텐츠를 접근성 트리에 안 올린다** |

기본 브라우저가 삼성이면 아무것도 안 읽히는데, 겉보기엔 **"앱이 고장난 것"** 처럼 보인다.
`e2e:app` 은 크롬을 명시적으로 지정해서 연다.

> 터치는 걱정 없다 — 앱이 `dispatchGesture`(화면 좌표 탭)를 쓰므로
> 브라우저든 네이티브든 똑같이 눌린다. 실측으로 확인했다.

## 서버 로그는 파일에도 남는다

```
onedal-web/server/logs/server-YYYY-MM-DD.log      평소(4000)
onedal-web/server/logs/server-YYYY-MM-DD-4012.log 다른 포트(검사·재현용)
```

터미널 출력은 **그대로** 있다 — 파일은 추가지 대체가 아니다.
**3일치만 남고 부팅 때 자동 정리**된다. `.gitignore` 에 들어 있다.

왜 필요했나: *"🏁 도착 감지가 몇 번 찍혔나"* · *"isActive 가 언제 꺼졌나"* 를 확인하려면
매번 터미널을 복사해야 했다. 콘솔은 스크롤이 지나가면 사라진다.
2026-08-14 에 서버 로그를 함께 보고서야 *"앱을 손으로 만져 리스트로 나오면 콜 잡기가 죽는다"* (당시 표현: 사냥)
를 찾았다 — 브라우저 콘솔만으로는 안 보이던 것이다.

⚠️ **파일을 지우면 서버를 다시 띄울 때까지 안 쌓인다** (열려 있던 스트림이 지워진 파일을 계속 문다).

## 🔴 `jest` 는 **`Test Suites:` 줄까지 본다**

```
Test Suites: 30 passed, 30 total     ← 이 줄을 안 보면 통째로 안 도는 스위트를 놓친다
Tests:       452 passed, 452 total
```

**컴파일이 안 되는 테스트 파일은 "실패"가 아니라 "없는 것"이 된다.** `✕` 도 안 찍히고
`Tests:` 숫자에서도 빠진다 — 그냥 조용히 사라진다.

2026-08-15 에 `filterHunt.test.ts`(지금 이름은 `tests/shared/callFilterBlocker.test.ts` — **fail-open 차단** 안전 검사 6건)가 타입에 없는 키 한 줄
때문에 **이틀째 안 돌고 있었다**(08-13 `c609e95` 이후). 그동안 커밋 게이트는 매번 통과했다.
빈 필터로 콜 잡기가 나가는 것을 막는 검사였는데, 그게 죽은 줄 아무도 몰랐다.

→ 결과를 `grep "Tests:"` 로만 보지 않는다. **`Test Suites:` 의 `passed` 와 `total` 이 같은지**
  확인한다. `npx jest` 의 종료 코드도 0 인지 본다 (파이프로 넘기면 코드가 가려진다).

## 함정

- ⚠️ **결과를 믿기 전에 `bootedAt` 을 본다.** 포트에 옛 서버가 남아 있으면 시드하지도 않은 DB로
  검사가 돌아 **멀쩡한 제품이 실패로 나온다.** 세 번 당했다 —
  지금은 `scenario` 가 먼저 포트를 비우고, 기동 시각보다 이른 서버가 응답하면 검사를 중단한다

- `pkill -f "PORT=4012"` 는 **안 잡힌다** (환경변수는 명령줄에 안 보인다).
  `lsof -ti :4012 | xargs kill -9` 로 포트를 쥔 프로세스를 죽인다
