// ━━━━━━━━━━ [관제 배차 평가 상숫값] ━━━━━━━━━━
// 기사님이 언제든지 이 수치들을 조정해서 콜 판독 기준을 바꿀 수 있습니다.
export const DISPATCH_CONFIG = {
    // 1. 단독 주행 판독 기준 — 여기 없다. 첫짐 꿀/똥 기준은 user_judgment(첫짐 그룹)에 있다

    // 2. 합짐 판독 기준 — 여기 없다. 판정색 v2 의 «우회 시급»(운임 대비)이 잰다.
    //    기사님 확정: *"같은 40분이라도 3.5만이면 좋고 5천원이면 나쁘다."*
    //    절대 문턱(추가 N분 이하면 꿀)은 운임을 안 보므로 꿀콜을 똥으로 만든다.

    // 3. 통신 타임아웃 — 여기 없다. DB `user_settings` 의 배차망별 값이다 — `safeCancelSecOf` · `SERVER_CLEANUP_EXTRA_SEC`
    //    코드 숫자로 두면 원달앱·관제웹의 값과 서로 모른다.
    //   
};
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
