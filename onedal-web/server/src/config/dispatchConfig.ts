// ━━━━━━━━━━ [관제 배차 평가 상숫값] ━━━━━━━━━━
// 기사님이 언제든지 이 수치들을 조정해서 콜 판독 기준을 바꿀 수 있습니다.
export const DISPATCH_CONFIG = {
    // 1. 단독 주행 판독 기준 (분)
    // 첫짐 꿀/똥 기준(40/90분)은 user_judgment(첫짐 그룹)로 이관 — 2026-08-17, scoreSolo

    // 2. 합짐 판독 기준 — 🪦 **철거됨** (2026-08-29)
    //    옛 절대 문턱(추가 30분 이하면 꿀 / 60분 이상이면 똥)이었다. 2026-08-21 판정색 v2 가
    //    그 문턱을 폐기하고 «우회 시급»(운임 대비)으로 바꾼 뒤로 **아무도 안 불렀다.**
    //    기사님 확정: *"같은 40분이라도 3.5만이면 좋고 5천원이면 나쁘다."*
    //    되살리지 말 것 — 절대 문턱은 운임을 안 보므로 꿀콜을 똥으로 만든다.

    // 3. 통신 타임아웃 세팅 (밀리초)
    WAITING_WARNING_MS: 30000,  // 30초 후 경고 푸시
    WAITING_TIMEOUT_MS: 35000,  // 35초 후 강제 연결 해제
};
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
