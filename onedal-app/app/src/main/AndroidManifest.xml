<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.onedal.app">

    <!-- 배터리 최적화 제외 권한 (접근성 서비스 유지용) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 🔔 알람 모드의 강한 진동 (기기_모드.md 2단계). 설치 시 자동 승인 — 기사님이 켤 것 없음 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 🧪 설정 화면 «테스트 가상 콜 화면 열기»가 켜는 배차망 시뮬레이터 앱 (2026-09-14).
         안드로이드 11 이상은 다른 앱을 여기 적어 둬야 찾을 수 있다 — 안 적으면 버튼이 조용히 먹통이다 -->
    <queries>
        <package android:name="com.onedal.simulator" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".HijackService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
