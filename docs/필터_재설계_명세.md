# 필터 재설계 명세 (v6 기반)

> **작성일**: 2026-08-13 · **상태**: 설계 확정 · **일부 구현됨** (§5 체크리스트 참조).
> **아직 main 미반영·미배포** — 브랜치 `filter-redesign-p1`.
> 체크 안 된 항목은 **아직 코드에 없다.** 코드와 다르면 코드가 맞다.
> **목업(눌러볼 수 있음)**: https://claude.ai/code/artifact/7081db67-b4b5-4535-ba60-db54e02d74fd
> 관련 결정 기록: 메모리 `cargo-slot-model` · todo.md 하한가 섹션(이 문서로 대체됨)

---

## 1. 핵심 원칙 (2026-08-13 기사님 확정)

1. **필터는 돈만 본다.** 시간(상하차·우회 시간)은 필터 계산에 넣지 않는다 — 시간 약속은 통화에서 확보된다. "원가(시간값)" 개념 폐기.
2. **판정식은 하나다.** 첫짐·합짐·운행중·관내·복귀 전부:
   ```
   통과 = 요금 ≥ 배송거리 × 단가(차종) × (1 − 눈높이)
   ```
   단계의 차이는 판정식이 아니라 **어느 콜을 보느냐**(키워드)뿐.
3. **하한 금액을 입력하지 않는다.** 단가표(고정)와 눈높이(다이얼)에서 파생된다.
   기존 특수값 전부 폐기: 첫짐 5만 · 합짐 2만 · ×0.8 사다리 · 관내 8천 · ±8% 거리 여유.
4. **눈높이는 자동으로 내려가지 않는다.** 콜이 안 잡히면 기사가 직접 한 단계 내린다 (콜의 주인은 기사님).
5. **합짐·관내·복귀-합짐은 순증 매출** — 바닥이 "전부"(금액 무관). 빈 칸으로 출발하는 것만 손해다.

## 2. 기준표

> **구현 상태 (2026-08-13 · 브랜치 `filter-redesign-p1`)**
> `6679c38` P1 shared+server · `af394b1` P2 앱(실기기 실증) · `649559f` P3-1 눈높이 UI ·
> `92bc80a` 단가표 중복 제거(원천 = DB) · `d40c17b` 국면 전환 3단계 + 스와이프
> (옛 `startTwoTrack` 제거 — 콜 완료 처리 버그) · `856e98f` 지도 좌하단 출발 FAB.
> **아직 main 미반영·미배포.**
> 남은 것: minFare/max_discount_percent 제거(P4) · 필터 팝업 탭 재구성 · 실콜 검증 2건

### 2-1. 단가 — ⚠️ **원천은 DB 다**

```
DB user_filters.vehicle_rates (총액 원/km, 설정 화면에서 조작)
  × (1 − agency_fee_percent/100)     ← DB · 수수료 23%
  × (1 − eyeline_pct/100)            ← DB · 눈높이
  = 앱에 내려보낼 차종별 하한 단가표 (ratePerKm)
```

`shared/pricing.ts` 의 `GROSS_RATE_PER_KM` 은 **DB 를 못 읽을 때만 쓰는 폴백**이다.
서버는 반드시 `rateFloorsFrom(눈높이, DB요율, DB수수료)` 로 부른다 — 상수를 직접 쓰면
설정에서 요율을 바꿔도 앱 필터가 안 바뀌어 **표가 두 벌**이 된다 (2026-08-13에 실제로 그랬고 고쳤다).

**`max_discount_percent` 는 `eyeline_pct` 가 대체했다.** 둘은 같은 뜻이었다.
컬럼은 DB 에 남아 있지만 아무도 읽지 않으며, 설정 화면의 "최대 할인율" 입력은 제거됐다.

#### 참고: 총액 시세 → 실수령 환산값 (수수료 23% 기준)

| 차종 | 실수령 단가 | 근거 (총액 시세 × 0.77) |
|---|---|---|
| 오토바이 | 539 | 700 |
| 다마스 | 616 | 800 |
| 라보 | 693 | 900 |
| 승용차 | 693 | 900 |
| 1t | 770 | 1,000 |
| 특수화물 | 2,310 | 3,000 |

- 전제: **배차앱 표시 금액 = 수수료(23%) 공제 후 실수령** — 기사님 기억 기준 잠정.
  🔍 **검증 필요**: 다음 실콜에서 표시 금액과 정산 입금액 대조. 총액이면 환산(×0.77)을 걷어낸다.

### 2-2. 적재 칸

```
내 1t 트럭 용량 = 5   (정규 4칸 + 자투리 1칸 — 파레트 2개를 놓아도 660mm가 남는다)
1t짐 4 · 라보 2 · 다마스 1 · 승용차 1 · 오토바이 0(조수석)
남은 칸 = 5 − Σ 칸(잡은 콜)
```

- 근거는 바닥 면적이 아니라 **부피·중량의 최소값** (다마스는 실내높이 1.05m·450kg가 깎는다).
- **통화가 칸을 갱신한다**: 표시 차종은 "최소 필요 차종"이지 짐 양이 아니다.
  1t 콜이 실제 라면박스 20개면 1칸 → 여유 증가. 통화 확인 3요소: 양·성질(파레트/박스)·무게.
- **넘치면 기사가 포기한다** — 예외를 모델에 넣지 않는다.
- 허용 차종은 입력이 아니라 **파생값**: `칸(v) ≤ 남은 칸`인 차종. 수동 차종 토글 삭제.
- 내부 30점 스케일과 환산: 1칸 = 7.5점. ⚠️ 현재 코드 `VEHICLE_CAPACITY` 다마스 10점(=1/3)은 이 확정과 어긋남 — 수정 대상.

### 2-3. 눈높이 (탭별 범위)

| 탭 | 눈높이 단계 | 비고 |
|---|---|---|
| 첫짐 | 시세 · -10 · -20 · -30% | 기준을 세우는 짐 — "전부" 없음 |
| 합짐 | 시세 ~ -30% · **전부** | |
| 운행중 | (자기 다이얼 없음) | 합짐 기준을 그대로 씀 · 경유 +0 고정 |
| 관내 | 시세 ~ -30% · **전부** | 별도 하한가(8천) 폐기 — 짧으면 하한도 저절로 작다 |
| 복귀 | 시세 ~ -30% · **전부** | 빈 차로 돌아가는 것보다 뭐든 싣는 게 이득 |

## 3. 경유 (용어 확정)

- **경유 +N km** = 카카오 **총거리 증가분** (100km → 105km면 +5km). 옆으로 벗어난 반경(회랑)이 아니다.
- 사용자는 경유 허용(+N km)만 설정. **회랑 반경 = 경유 허용 ÷ 2** 로 서버가 파생 (최악: 갔다 와야 하므로 ×2).
- 정확한 판정은 데스밸리 30초 동안 서버가 카카오로 잰 **콜별 실제 경유 증가분**으로.
- 경유를 넓혀도 비용이 아니다 — 후보가 늘 뿐. 좁히는 이유는 운전 중 조작 최소화(운행중 +0)뿐.
- ⚠️ 목업의 합짐 탭 입력은 아직 "우회 허용/하차지 주변" 표기 — 구현 시 "경유 허용"으로 통일.

## 4. 화면 구조

### 4-1. 관제웹 메인 = 상태·조작

```
┌ 요약줄 (스와이프 카드 3장) ─────────────────────────────┐
│ 🎯 여기서 반경 10km → 파주시 10km 반경으로 필터링 [첫짐] │   탭     → 필터 설정 팝업
│ 💰 금액 -10% (1t ≥ 693원/km) · 📍 지역 122동 · 📦 0/5칸 │   스와이프 → 다음 국면
│                    ● ○ ○                              │
├ 지도 ─────────────────────────────────────────────────┤
│ [🚀 지금 출발]  ← 좌하단 플로팅 버튼 (운행중 전환·경유 +0) │
└───────────────────────────────────────────────────────┘
  콜 카드 (KEEP/CANCEL 결재)
```

**하루 국면 = 스와이프 카드 3장** — 스와이프 순서가 하루의 흐름과 같다:

| # | 카드 | 제목줄 (필터를 사람 말로) | 사냥 대상 | 팝업 탭 |
|---|---|---|---|---|
| 1 | 🎯 목적지행 | "여기서 반경 10km → 파주시 10km 반경으로 필터링" | 첫짐·합짐 | 첫짐/합짐/운행중 |
| 2 | 🏘️ 이 동네에서 찾기 | "이 동네(파주시) 안에서 끝나는 콜 찾기" | 관내 | 관내 |
| 3 | 🏠 복귀행 | "여기서부터 집으로 10km 범위로 필터링" | 복귀 | 복귀 |

- **사냥은 언제나 한 방향** (동시 두 필터 없음). 관내 병행 토글은 폐지 — 관내는 토글이 아니라 카드다.
  투트랙은 순차 전환으로 커버된다: 관내 콜을 **잡고 나면**(수행=배달은 사냥과 무관) 복귀행으로 스와이프.
  아침에 목적지 콜이 없으면 이동네 카드로 스와이프하면 된다.
- **카드 형식 (고정)**: 제목줄 = 필터 문장 + 단계 뱃지(`첫짐`/`합짐 N번째`/`관내`/`복귀`) + 상태(사냥 중/잠깐 멈춤/꺼짐+이유).
  아랫줄 = 항상 같은 순서 **① 💰 금액(할인율) ② 📍 지역(동 수) ③ 📦 적재(x/5칸)**.
- 🔴 **스와이프 전환은 폐기했다 (2026-08-14).** 기사님: *"드래그로 바꾸면 안 될 듯싶다.
  이렇게 필터가 쉽게 바뀌면 오작동이 될 가능성이 있을 것 같다. 버튼을 누르게 하고
  알럿창으로 확인받는 것이 안전할 듯하다."*
  국면 전환은 목적지·반경을 바꾸고 **회랑을 통째로 재계산**한다(지리 연산 수 초 + 앱 필터 교체).
  운전 중 스크롤하다 손가락이 스치면 사냥이 엉뚱한 방향으로 간다. 임계값(80px)·탭 판정(6px)을
  넣어도 완전하지 않았다. 하루 두 번 하는 조작이라 **확인 한 번이 부담이 아니다.**
- **국면 전환 = 버튼 3개 + `confirm`**: 요약줄 아래 `🎯 목적지행 / 🏘️ 이 동네에서 찾기 / 🏠 복귀행`.
  지금 국면 버튼은 비활성. 확인창에 "잡아 둔 콜은 그대로 있습니다 (필터만 바뀝니다)"를 적어
  콜이 사라지지 않는다는 것을 분명히 한다. 전환 후 토스트.
- 요약줄 본문을 누르면 필터 설정 팝업.
- 출발 감지: GPS가 상차지 200m 벗어나 60초 → **알림만** ("출발하셨나요?"). 자동 전환 없음.

### 4-2. 필터 설정 팝업 = 기준

```
┌ 필터 설정                × ┐   ← 슬림 타이틀바 (한 줄)
│ 제외 단어 [착불, 수거]      │   ← 공통은 이것 하나
│ 첫짐● 합짐 운행중 관내 복귀  │   ← 탭 부제 없음 · 초록점=지금 · 노란점=미저장 변경
│ ── 탭 내용 ──              │
│  입력: 목적지·반경·눈높이    │
│  하한표: 차종별 단가·N/5칸  │   ← 자동 계산, 읽기 전용
│  동 미리보기 (359개 동…)    │   ← 유지 (숫자 피드백)
│ [평소값][오늘만 적용][계속]  │   ← 전역 저장 + "N곳 변경" 표시 · 자정에 평소값 복귀
└───────────────────────────┘
```

- **팝업에서 삭제된 것** (메인으로 이동 또는 파생화): 방향 스위치·관내 토글(→ 메인 스와이프),
  🚀 출발·🏠 복귀 전환 버튼(→ 메인), 허용 차종 수동 토글(→ 칸 파생), 모든 설명 문구 박스.
- 하한표 행 형식: `차종  시세 N원/km · N/5칸  |  ≥ N원/km  (거리 예시 금액)`.

## 5. 구현 변경 목록 (컴포넌트별)

### shared (`@onedal/shared`)
- [x] `AutoDispatchFilter` 에 `ratePerKm`·`eyelinePct`·`slotsUsed` **추가**(optional).
      ⚠️ `minFare/maxFare` 는 **아직 남아 있다** — 구앱 호환용. 제거는 P4.
      ⚠️ `corridorRadiusKm` → `detourAllowKm` 이름 변경은 **아직**.
- [x] 칸 상수: `SLOT = {오토바이:0, 다마스:1, 승용차:1, 라보:2, "1t":4}`, `CAPACITY = 5` (칸↔점수 환산 1칸=7.5점).

### server
- [x] **국면 전환 `setHuntPhase`** (`set-hunt-phase` 소켓) — 필터만 바꾸고 콜은 안 건드린다.
      옛 `startTwoTrack`(활성 콜 전부 완료 처리 + 가짜 도시명) 제거. 규칙 테스트 6건으로 고정.
- [x] 단가표·눈높이에서 피기백 필터 생성 (거리 하한이 아니라 **단가 하한**을 앱에 내림).
- [ ] `VEHICLE_CAPACITY` 를 칸 체계로 (다마스 1/3 → 1/4). **지금은 30점 척도 그대로**이고 표시만 1칸=7.5점으로 환산한다.
- [x] 허용 차종 파생(기존 경로 유지) + `slotsUsed` 파생: 남은 칸 계산(통화 갱신 반영) → 필터에 자동 반영.
- [ ] 회랑 반경 = 경유 허용 ÷ 2 파생. 데스밸리 판정은 콜별 실제 경유 증가분.
- [ ] 표시금액 총액/실수령 스위치 (설정, DB) — 실콜 검증 후

### client-app (관제탑)
- [x] 요약줄 스와이프 3국면 (목적지행/이동네/복귀행 · 35% 스냅 · 전환 토스트 · 탭=팝업 · 점 3개).
      ⚠️ 카드 3장을 실제로 넘기는 페이저가 아니라 **한 칸이 국면에 따라 바뀌는** 형태로 구현했다.
- [x] 지도 좌하단 🚀 출발 플로팅 버튼 (짐이 있고 출발 전일 때만).
- [x] 필터 설정 팝업에 눈높이 다이얼 + 차종별 하한표. ⚠️ 4-2 의 나머지 재구성(탭 5개·제외단어 한 줄·설명 삭제)은 **아직**.
- [x] 콜 카드 4색 — 2026-08-13 오전에 이미 구현돼 있었다 (`PinnedRouteCard`). 🔵 꿀 · 🟢 보통 · 🟡 똥 · 🔴 연산 실패.

### onedal-app (안드로이드)
- [x] `InsungParser` 거리 파싱부: 리스트 최좌측 두 숫자 = [접근거리, **배송거리**] — 버리던 **배송거리를 필드로 보존** (`SimplifiedOfficeOrder`에 추가).
- [x] `shouldClick` 에 단가 판정: `fare ≥ 배송거리 × 단가[차종]`(눈높이는 서버가 이미 반영). 단가표·차종·배송거리 중 하나라도 없으면 **기존 minFare 판정으로 폴백** (통과시키면 리스트 전체가 들어와 데스밸리가 밀린다).
- [x] 피기백 필터 규격 변경 수신 (단가표·눈높이).

## 6. 검증 필요 (구현 전/직후 실콜로)

| # | 항목 | 방법 |
|---|---|---|
| 1 | 표시 금액이 실수령인가 | 실콜 1건: 표시 금액 vs 정산 입금액. 같으면 실수령 ✅ |
| 2 | 화면 배송거리가 직선인가 도로인가 | 실콜 1건: 화면 배송거리 vs 카카오 도로거리. 직선이면 단가에 보정계수(~×1.3 역산) |

## 7. 미결정 (다음 논의)

- ~~관내 병행 ON/OFF 위치~~ → **해결 (2026-08-13)**: 관내는 토글이 아니라 스와이프 3단계의 둘째 카드 (4-1 참조).
- 잠재 매출 표시 (남은 칸 × 시세) — 백로그.
- 하한표를 접기(▾)로 더 압축할지 — 백로그.
- 경유 +N km 이름 변경이 목업 입력 라벨("우회 허용")에 아직 미반영 — 구현 시 통일.
- 복귀 국면 종료는 **방향을 목적지로 되돌리는 것**으로 확정(v6 결정 유지 — 스와이프로 카드 1로 돌아가면 종료).
- `createHomeReturn`(귀가콜 가상 오더)과 `HOME` 국면은 **다른 기능**이다 — 전자는 콜을 만들고
  후자는 필터만 바꾼다. 둘 다 남겨 뒀는데 이름이 비슷해 헷갈릴 소지가 있다. 정리 여부는 미결.

## 8. 오늘 폐기된 것 (되살리지 말 것)

- ~~하한 금액 직접 입력 (첫짐 5만·합짐 2만)~~ → 단가 × 눈높이
- ~~×0.8 자동 사다리~~ → 기사가 직접 내림
- ~~관내 별도 하한가 8,000~~ → 같은 판정식
- ~~±8% 거리 여유~~ → 단가 기준이라 불필요
- ~~원가(시간값) 바닥~~ → 필터는 돈만
- ~~허용 차종 수동 토글~~ → 남은 칸에서 파생
- ~~투트랙 별도 버튼~~ → ~~방향=복귀 + 관내 병행~~ → **3단계 스와이프 순차 전환** (목적지행 → 이동네 → 복귀행)
- ~~＋관내콜 병행 토글~~ → 관내는 토글이 아니라 카드 (동시 두 필터 없음)
- ~~탭 안 실행 버튼~~ → 메인(지도 FAB·스와이프)
