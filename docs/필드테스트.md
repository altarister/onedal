# 필드 테스트 — 절차와 기록

기사님(2026-08-23): *"이번 필드 테스트를 한 문서로 만들어 둘 필요가 있어.
**2일에 한 번은 필드 테스트** 해야 할 듯."*

> 이 문서는 **두 가지**다 — 위쪽은 **매번 쓰는 절차**, 아래쪽은 **회차별 기록**.
> 절차는 회차를 돌 때마다 고쳐 나간다. 기록은 지우지 않는다.

---

# 1부 · 매번 쓰는 절차

## 1-1. 왜 2일에 한 번인가

책상에서는 안 드러나는 것이 있다. 2026-08-23 1회차에서 **주행 3시간 만에** 나온 것들이
전부 책상에서는 몇 주째 안 보이던 것이었다.

| 책상에서 못 보는 것 | 왜 |
|---|---|
| 움직이는 좌표 | 시뮬 좌표는 매끄럽다. 실제 GPS 는 튀고 끊기고 늦는다 |
| 음영 구간 복구 | 터널·지하차도는 만들어 낼 수 없다 |
| **화면이 짧을 때** | 개발은 맥에서 한다 — 폰 세로에서만 넘치는 것이 안 보인다 |
| **운전 중에 쓸 수 있는가** | 앉아서 보면 다 읽힌다. 달리면서는 1~2초다 |
| 로그가 실제로 남는가 | 남는다고 믿었던 것이 안 남는 걸 여기서 안다 |

## 1-2. 등장인물 (주어를 반드시 붙인다)

| 이름 | 무엇 |
|---|---|
| **A24폰 원달앱** | 스캐너 (`com.onedal.app`) — 접근성으로 화면을 읽고 자동 터치 |
| **A24폰 시뮬앱** | 배차망 화면을 띄우는 웹뷰 앱 (원달앱이 이걸 읽는다) |
| **S23폰 관제앱** | 관제탑 (`kr.co.onedal.dashboard`) |
| **라이브 서버** | `1dal.altari.com` (EC2) |
| **리허설 배차망** | `rehearsal.altari.com` — 문제지가 여기 있다 |
| **배포본 배차망** | `map.altari.com` — 옛 레포, **랜덤이라 통제 불가** |

## 1-3. 나가기 전 (약 30분)

- [ ] **문제지 확인** — `rehearsal.altari.com/inseong/dispatch?preset=구로&loop=1&interval=20000`
      · 주소는 **볼륨 위 → 주소 직접 입력** (설정 화면을 거치면 `preset`·`loop` 가 날아간다)
      · 화면에 **첫 문제**가 뜨는지 눈으로 확인
- [ ] **A24폰 원달앱** — 접근성 토글 ON · `isLiveMode` ON · 배터리 최적화 제외 · 화면 항상 켬
- [ ] 🔴 **A24폰 원달앱 접근성 토글을 껐다 켠다** — `processedOrderHashes` 를 비운다.
      안 하면 지난 회차에 본 콜을 **평가조차 안 한다** (문제지는 같은 콜을 되풀이한다)
- [ ] **S23폰 관제앱** — 로그인 · 위치 권한 "정확한 위치" · 화면 항상 켬 · 충전
- [ ] **충전 세 개** — S23 · A24 · (테더링 쓰면) 아이폰
- [ ] **A24폰이 모바일 데이터로 나가는지** — 집 와이파이에 붙어 있으면 나가는 순간 끊긴다
- [ ] 국면·반경 확인 (합짐·운행의 목적지는 **입력칸이 없는 게 정상** — 경로로 판단한다)
- [ ] 🔴 **동네 한 바퀴 10분** — 여기서 끊기면 집에서 고친다. 10분으로 한 시간을 산다

## 1-4. 주행 중

- 🔴 **운전 중에는 화면을 보지 않는다.** 로그가 대신 본다
- 관찰은 **정차했을 때만**. 이상하면 **화면 캡처**만 하고 넘어간다
- **주행 중에 고치려 하지 않는다**

## 1-5. 돌아와서 — 로그 회수 (**당일에 한다**)

🔴 **서버 로그는 3일 뒤 지워지고, 폰 logcat 버퍼는 4~5시간이면 돈다.** 미루면 사라진다.

```bash
OUT=~/Desktop/1dal-주행로그-$(date +%Y%m%d); mkdir -p "$OUT"/{raw,표,폰로그}

# ① 서버 (EC2)
scp -i ~/.ssh/onedal_mac_key ubuntu@<EC2_IP>:.pm2/logs/onedal-web-server-out-0.log   "$OUT/raw/server-out.log"
scp -i ~/.ssh/onedal_mac_key ubuntu@<EC2_IP>:.pm2/logs/onedal-web-server-error-0.log "$OUT/raw/server-error.log"
scp -i ~/.ssh/onedal_mac_key ubuntu@<EC2_IP>:onedal/onedal/onedal-web/server/data.db{,-wal,-shm} "$OUT/raw/"

# ② 폰 (USB · 각각 꽂아서)
adb -s <A24> logcat -d -v time > "$OUT/폰로그/A24_원달앱_logcat_전체.log"
adb -s <S23> logcat -d -v time > "$OUT/폰로그/S23_관제앱_logcat_전체.log"
```

⚠️ **`adb kill-server` 를 함부로 하지 않는다** — 인증이 풀려 폰에서 다시 "허용"을 눌러야 한다.

### 어디에 무엇이 남는가 (1회차에서 확인)

| 원천 | 남나 | 비고 |
|---|---|---|
| 서버 로그 | ✅ | 9.7MB/76,339줄 · **3일치만** |
| DB (`orders`·`intel`·`판정`·장부) | ✅ | 콜의 생애가 전부 |
| **A24폰 원달앱** | ✅ | `AppLogger` → logcat. **여기 답이 있다** |
| **S23폰 관제앱** | ❌ | **자기 로그를 안 남긴다** (`Capacitor/Console` 0줄) |
| **GPS 궤적** | ❌ | 저장하는 표가 없다. `🚨 [위치 점프]` 경고 줄에서 **일부만** 복원 가능 |

## 1-6. 무엇을 물을 것인가 (미리 정한 질문)

돌아와서 뒤지지 않는다. 아래 답을 찾는다.

| # | 질문 | 어디서 |
|---|---|---|
| ① | 리스트를 **몇 번 훑었고** 필터까지 **몇 번 갔나** | A24 logcat `화면: LIST` vs `1차 리스트 필터` |
| ② | 떨어진 콜은 **어느 축**이었나 | `FilterTally` · `intel` |
| ③ | 잡은 콜의 **판정 색**이 맞았나 | `order_judgments` |
| ④ | **안전취소**가 몇 건이고 왜인가 | `orders` 상태 분포 |
| ⑤ | 좌표가 **몇 초 간격**으로 들어왔나 | 서버 로그 |
| ⑥ | **도착 감지**가 실제와 몇 분 차이인가 | 장부 vs GPS |

## 1-7. 성공을 무엇으로 볼 것인가

| 등급 | 조건 |
|---|---|
| **성공** | 주행 중 콜이 잡히고, 판정 색이 나오고, 도착 감지가 떴다 |
| **부분 성공** | 좌표·스캔은 끊김 없이 돌았으나 콜 또는 도착 감지가 안 됐다 |
| **실패** | 통신이 끊겨 로그에 빈 구간이 크다 (원인 분석 자체가 불가) |

**부분 성공도 충분하다.** 얻으려는 것은 *"끊어진 데가 어디인가"* 다.

---

# 2부 · 회차 기록

## 1회차 · 2026-08-23 (토) · 경기 초월 → 서울 구로

**판정: 부분 성공** — 사슬은 이어졌으나 **콜을 거의 못 잡았다.**

로그: `~/Desktop/1dal-주행로그-20260823/` (31MB · 서버·DB·양쪽 폰)

### 그날 함께 한 것

v2 배포(5월 → 8월, 375커밋) · `data.db` 재생성 · 관제앱 네이티브 구글 로그인 신설 ·
리허설 배차망(`rehearsal.altari.com`) 신설 · 구로 문제지 작성.

### 숫자

```
화면: LIST 인식     413 회
요금 못 읽음     12,467 회      ← 여기서 다 죽었다
1차 리스트 필터      18 회
꿀콜 클릭             1 회
모드 AUTO           843 / MANUAL 0

콜 43건 = 안전취소 24 · 내가 방출 10 · 하차 완료 7 · 확정 2
버려진 콜(intel) 1,508 · 판정 29
GPS 복원 187점 (갈 때 19 · 올 때 44 · 집 근처 124)
```

### 🔴 발견 1 — 콜 카드가 **빈 껍데기**로 잡힌다 (최우선)

`💸 [요금 못 읽음]` 뒤가 **공백**이다. 요금을 못 읽은 게 아니라 **카드에 글자가 하나도 없다.**
한 번 스캔에 **약 30개**씩 빈 카드가 나온다 (12,467 ÷ 413).

```kotlin
// InsungParser.kt:500  groupListNodes
val fareNodes = allNodes.filter { it.text.matches(차종정규식) }  // 차종 글자로 카드를 찾고
val rowNodes  = allNodes.filter { y좌표가 겹치는 것 }             // 같은 줄 글자를 묶는다
```

**필터가 나쁜 게 아니었다. 필터까지 갈 기회조차 없었다.**

가설(미확정): ① `rect` 가 비어 y 비교가 아무것도 못 고른다 ② 정규식이 화면의 딴 글자에
걸려 가짜 카드를 만든다 ③ 웹뷰 스크롤로 노드가 재사용되며 좌표가 어긋난다.

→ 확인하려면 **그룹의 좌표와 원본 노드 수**를 로그에 같이 남겨야 한다.
  지금 로그는 *"비었다"* 까지만 말하고 *"왜 비었나"* 는 안 말한다.

### 🔴 발견 2 — 관제앱은 **평상시를 안 남긴다** (사건만 남긴다)

주행 시간대 `Capacitor/Console` **0줄**.

처음엔 *"관제앱이 로그를 안 남긴다"* 로 읽었는데 **절반만 맞았다.** 확인해 보니:

- `logRoadmapEvent` 는 **조건 없이 `console.log`** 를 부르고,
  Capacitor 웹뷰의 `console` 은 **그대로 logcat 에 `Capacitor/Console` 로 찍힌다.**
  (그날 낮 `⚠️ [계약 불일치]` 경고가 실제로 그렇게 찍혔다) → **길은 열려 있다**
- 부르는 자리도 **34곳**이나 된다

**그런데 그 34곳이 전부 "사건" 이다.**

```
order-evaluating · order-detail-received · order-evaluated   ← 콜이 잡혀야 찍힌다
KEEP · CANCEL · 국면 전환 · 필터 저장 · 지도 버튼            ← 눌러야 찍힌다
```

그날 콜은 **1건** 잡혔고 그마저 안전취소됐으며, 운전 중이라 **버튼을 안 눌렀다.**
그래서 **찍을 일이 없었다.** 0줄은 로그가 죽어서가 아니라 **아무 일도 안 일어나서**다.

🔴 **주행 중 알고 싶은 것은 사건이 아니라 "멀쩡히 돌고 있나" 다.**

| 알고 싶은 것 | 지금 |
|---|---|
| GPS 를 몇 초마다 받았나 · 언제 끊겼나 | ❌ |
| 소켓이 붙어 있나 · 몇 번 끊겼다 붙었나 | ❌ |
| 화면이 무엇을 그리고 있나 (콜 0건? 경로 있음?) | ❌ |
| 서버에서 무엇을 받았나 (필터·기기 상태) | ❌ |

**끊긴 것을 알려면 "이어져 있었다"는 기록이 있어야 하는데, 그게 없다.**

> A24폰 원달앱이 없었으면 이번 주행은 **원인 불명**으로 끝났을 것이다.
> 원달앱은 화면을 훑을 때마다(**5초**) 찍는다 — 그래서 16,336줄이 남았고 답이 거기 있었다.

### 🔴 발견 3 — GPS 궤적이 저장되지 않는다

좌표를 저장하는 표가 **없다.** 소켓으로 흘려보내고 메모리에만 산다.
`🚨 [위치 점프]`(속도가 튈 때만 찍히는 경고) 줄에서 **187점**을 긁어냈을 뿐이다.
*"뒤로 가는 경로 판정"* 을 따지기엔 너무 성기다.

### 기사님이 그날 지적하신 것

> *"관제앱이 너무 느림."*
> *"UI/UX 가 화려하기만 하고 **이벤트 발생 시 스크롤해야 함**."*
> *"지도가 상시 있을 필요 없음 — **경로 생성 시와 보고 싶을 때만** 노출되면 됨."*
> *"**원하지 않는 콜을 엄청 올림.** 그리고 그걸 받아서 **판정도 개떡같이 못함.
> 완전 신뢰할 수 없음.**"*

🔴 **마지막 것이 가장 무겁다.** 이 제품의 약속은 *"색만 보고 1~2초 안에 누른다"* 인데
(규칙 ⑤-3), **신뢰할 수 없으면 그 약속이 통째로 깨진다.** 색이 틀리는 것은 이 시스템의
가장 큰 사고다.

각 항목의 조사·수정 계획은 [todo.md](../todo.md) 에 있다.

### 1회차에서 절차에 반영한 것

- 접근성 토글을 **껐다 켜는 것**을 준비 항목에 넣었다 (해시 캐시)
- 시뮬앱 주소는 **설정 화면을 거치지 않는다** (`preset`·`loop` 유실)
- 로그는 **당일에** 받는다 (서버 3일 · logcat 4~5시간)
- `adb kill-server` 금지 (인증 풀림)

---

# 3부 · 다음 판 설계 — **위치가 콜을 부른다**

기사님 구상 (2026-08-23 밤, 주행 직후):

> *"2일에 한 번씩 경기도 광주에서 이천, 여주로 **딱 필요한 테스트용 콜 리스트**를 만들어
> 테스트해야 할 듯. 거기에는 **잡아야 하는 콜 · 잡지 말아야 할 콜**들을 **순서대로** 정렬해서
> 시간대별로. 아님 **GPS 값을 읽고 비슷한 값이 오면 이벤트를 발생**해 콜이 시뮬레이션에
> 노출되도록 만들면, 크롤링앱이 그걸 트래킹하고 **필터를 통과하는지** 알 수 있도록 해야겠다.
> 음.. 근데 그건 **시뮬레이션 GPS 값을 목업으로 만들어도 될 듯싶은데?"*

## 3-1. 여섯 가지로 정리

| | |
|---|---|
| **왜** | 지금은 콜이 **시간순**으로만 흐른다. 그래서 *"내가 여기 있을 때 이 콜이 뜨면 어떻게 되나"* 를 **못 물어본다.** 경로·경유·복귀 판정은 전부 **위치의 함수**인데, 시험은 위치를 안 쓰고 있었다 |
| **무엇을** | 문제 하나하나에 **트리거 위치**를 붙인 문제지. 기사님이 그 지점 근처에 오면 그 콜이 배차망에 뜬다. 각 문제에는 **잡아야 하는가/말아야 하는가**가 적혀 있다 |
| **누가** | **시뮬앱**이 좌표를 보고 콜을 띄운다 · **원달앱**이 그걸 읽고 필터를 건다 · **서버**가 판정한다 · **채점**은 문제지의 정답과 실제를 맞대어 자동으로 |
| **언제** | 로직 시험은 **매일 여러 번**(목업) · 실주행은 **2일에 한 번**(§3-3 이 왜 둘 다 필요한지 말한다) |
| **어디서** | **경기 광주 → 이천 → 여주** — 기사님 실제 동선. 왕복 한 바퀴에 전진·합짐·운행·복귀 네 국면이 다 나온다 |
| **어떻게** | 좌표는 세 갈래로 넣을 수 있다 (§3-4). 어느 갈래든 **문제지와 채점은 같은 것을 쓴다** |

## 3-2. 무엇이 좋아지나

- **재현된다.** *"그때 그 자리에서 이상했다"* 를 **같은 좌표로 다시** 돌릴 수 있다
- **채점이 된다.** 지금은 로그를 사람이 읽어야 알지만, 정답이 적혀 있으면
  **몇 점인지 자동으로** 나온다. 회차끼리 비교도 된다
- **국면을 시험한다.** 같은 콜이 갈 때는 떨어지고 올 때는 잡히는지 —
  **위치를 안 쓰면 물어볼 수 없는 질문**이다

## 3-3. 🔴 목업이면 실주행이 필요 없나 — **아니다. 둘은 다른 것을 잡는다**

기사님이 마지막에 스스로 던지신 질문이 이 설계의 핵심이다.

| | **목업 GPS** (책상) | **실주행** |
|---|---|---|
| 무엇을 잡나 | **로직** — 필터·판정·국면 전환이 옳은가 | **물리** — 그 로직이 진짜 세상에서 도는가 |
| 잡히는 것 | 잘못된 판정 · 국면이 안 넘어감 · 경유 계산 오류 | GPS 튐·끊김 · 음영 구간 · 발열 · **운전 중에 읽히는가** · 배터리 |
| 못 잡는 것 | 위의 오른쪽 전부 | 반복이 안 되어 **원인을 좁히기 어렵다** |
| 얼마나 자주 | **하루에도 여러 번** | 2일에 한 번 |
| 비용 | 0 | 3시간 + 기름 |

**그래서 이렇게 나눈다.**

```
목업으로 매일 돌려 로직을 좁힌다  →  좁혀진 것을 2일에 한 번 실주행으로 확인한다
```

1회차에서 나온 것들을 이 잣대로 보면 갈래가 분명하다.

- **콜 카드가 빈 껍데기** → 목업으로 잡힌다 (화면 읽기 문제라 좌표와 무관)
- **판정을 신뢰할 수 없다** → 목업으로 잡힌다 (같은 콜을 몇 번이고 다시)
- **GPS 튐 · 이벤트 스크롤 · 발열 · 느림** → **실주행에서만** 잡힌다

🔴 **목업은 실주행을 대신하지 않는다. 실주행의 횟수를 줄여 줄 뿐이다.**

## 3-4. 좌표를 어디서 넣나 — 세 갈래

셋 다 **같은 문제지·같은 채점**을 쓴다. 다른 것은 **좌표의 출처**뿐이다.

| 갈래 | 좌표 출처 | 언제 쓰나 |
|---|---|---|
| **① 목업 재생** | 미리 그려 둔 경로를 배속으로 재생 | 매일. 로직 시험 |
| **② 서버 중계** | 관제앱이 보낸 **실제 좌표**를 시뮬앱이 서버에서 받아 씀 | 실주행. 지금 구조에 가장 가깝다 |
| **③ 시뮬앱 직접** | 시뮬앱이 폰 GPS 를 직접 읽음 | 실주행. 서버를 안 거쳐 지연이 없다 |

- **②가 유력하다** — 서버가 이미 `driverLocation` 을 들고 있다.
  시뮬앱이 그걸 받아 쓰면 **관제앱·원달앱·시뮬앱이 같은 좌표를 본다**
- ⚠️ ③은 시뮬앱 웹뷰에서 위치 권한을 또 받아야 하고, **셋이 각자 다른 좌표**를 볼 위험이 있다
  (이 레포가 반복해 당한 형태 — 같은 값을 두 곳에서 만들면 갈라진다)

## 3-5. 코드를 짜기 전에 정할 넷 (규칙 ⑤-4)

**하나라도 비어 있으면 시작하지 않는다.**

| | 정할 것 | 비면 생기는 일 |
|---|---|---|
| **① 스키마** | 문제에 트리거를 어떻게 적나 — `{lat, lon, radius, expect}`? 반경은 문제마다 다른가 | 코드 상수로 태어나 영영 못 바꾼다 |
| **② 값** | 반경 기본값은 몇 m 인가 · **근거는 무엇인가** (도심과 외곽이 다르다) | 누군가의 감으로 정해지고 아무도 못 고친다 |
| **③ 시점** | 반경에 **들어올 때 한 번**인가, 머무는 동안 계속인가. 지나쳤다 되돌아오면 다시 뜨나 | 되돌아올 때 같은 콜이 쏟아진다 |
| **④ 화면** | 채점 결과를 **어디서 보나**. 주행 중에도 보이나, 끝나고 보나 | 만들어 놓고 아무도 안 본다 |

## 3-6. 코스 초안 — 광주 → 이천 → 여주 → 광주

기사님 동선이라 **실제 지형과 통신 환경**이 같이 시험된다.

| 구간 | 무엇을 시험하나 |
|---|---|
| 집(초월) 출발 전 | **첫짐** — 잡아야 할 것 하나, 미끼 하나 |
| 광주 → 이천 | **합짐** — 경로 위 상차지 · 경로 밖 미끼 |
| 이천 부근 | **운행 중 합짐** — 경유 반경 안/밖을 나란히 |
| 이천 → 여주 | **적재 한계** — 만차에서 더 들어오는지 |
| 여주 → 광주 | **복귀** — 갈 때 떨어진 콜이 **올 때 잡히는가** (같은 콜, 다른 답) |

🔴 **마지막 줄이 이 설계의 값어치다.** 같은 콜에 **위치에 따라 다른 정답**을 매길 수 있고,
그건 **시간순 문제지로는 물어볼 수 없는 질문**이다.

## 3-7. 다음에 할 일

- [ ] §3-5 의 **넷을 채운다** (기사님과 합의) — 그 전에는 코드 없음
- [ ] 코스의 **트리거 지점 좌표**를 정한다 (실제 주행 궤적에서 뽑으면 정확하다 —
      1회차 `GPS_갈때/올때` 가 이미 있다)
- [ ] 문제지에 `expect`(잡아야/말아야)를 되살린다 — 지금 구로 문제지는 비워 두었다
- [ ] **채점 화면** — 회차별 점수와 틀린 문제
- ⚠️ **①(콜 카드가 빈 껍데기)을 먼저 고친다.** 카드를 못 읽으면 문제지가 아무리 좋아도
      채점이 전부 0점이다 — 1회차가 정확히 그랬다
