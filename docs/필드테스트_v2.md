# 필드 테스트 1회차 — 팩트 모음 (2026-08-23)

기사님(2026-08-24): _"일단 내가 어제 이야기한 것들을 한곳에 모아 보자. **판단은 없이 팩트만.**"_

> **이 문서에는 우선순위도, 원인 진단도, "무엇을 먼저 해야 한다"도 없다.**
> 판단은 이 문서를 다 읽은 뒤에 **따로** 한다. 여기 있는 것은 **일어난 일**뿐이다.

## 표기 — 출처로 나눈다

|     | 뜻                | 어디서 왔나                         |
| --- | ----------------- | ----------------------------------- |
| 🗣️  | **기사님 말씀**   | 원문 그대로. 줄이지 않는다          |
| 📊  | **측정된 숫자**   | 로그·DB 에서 센 것                  |
| 🔍  | **코드에서 확인** | 파일을 열어 눈으로 본 것            |
| ❓  | **아직 모름**     | 확인하지 않았거나 확인할 수 없는 것 |

⚠️ **추측은 이 문서에 넣지 않는다.** 추측이 필요한 자리는 ❓ 로 비워 둔다.

---

# 1부 · 필드 테스트에서 무엇이 문제였는가 (🗣️ 기사님)

"필터 설정창 팝업이 사이즈가 넘어가 선택을 할 수가 없다."

"관제앱이 너무 느림."

"콜 관련 6단계 버튼 클릭시 스텝별 버튼을 찾으려고 스크롤해야 함."

"지도가 상시 있을 필요 없음 — 경로 생성 시와 보고 싶을 때만 노출되면 됨."

"원하지 않는 콜을 엄청 올림. 그리고 그걸 받아서 판정도 개떡같이 못함. 완전 신뢰할 수 없음."

"폰에서 잘 작동하면 테블릿에서도 잘보일꺼니 폰에서최적화 하자"

> **이 여섯은 2026-08-24 에 기사님이 직접 적으신 것이다.** 원문 그대로 둔다.

---

# 2부 · 측정된 숫자

전부 `~/Desktop/1dal-주행로그-20260823/` 에서 센 것이다.

## 2-1. 스캔 (A24폰 원달앱 logcat · 16,336줄)

```
화면: LIST 인식        413 회
요금 못 읽음        12,467 회
1차 리스트 필터        18 회
꿀콜 클릭               1 회
모드                 AUTO 843 / MANUAL 0
```

📊 `💸 [요금 못 읽음]` 뒤가 **공백**이다 (숫자가 이상한 게 아니라 **글자가 없다**)
📊 12,467 ÷ 413 ≒ **스캔 한 번에 약 30개**

## 2-2. 콜 (서버 DB)

```
콜 43건  =  안전취소 24  ·  방출 10  ·  하차 완료 7  ·  확정 2
버려진 콜(intel)   1,508
판정(order_judgments)  29
```

## 2-3. 화면 (S23폰 관제앱 logcat)

```
주행 3시간(19~21시) 웹뷰 재렌더: 59,679 회 = 초당 5.5 회
```

📊 `setRequestedFrameRate` 를 센 것
📊 같은 시간대 `Capacitor/Console` — **0줄**

## 2-4. GPS

📊 궤적 **187점** 복원 (갈 때 19 · 올 때 44 · 집 근처 124)
📊 출처는 `🚨 [위치 점프]` 경고 줄 — **속도가 튈 때만** 찍히는 줄이다

## 2-5. 로그가 남은 곳 / 안 남은 곳

| 원천                            | 남았나 | 크기·비고                           |
| ------------------------------- | ------ | ----------------------------------- |
| 서버 로그                       | ✅     | 9.7MB / 76,339줄 (**3일치만 보관**) |
| DB (`orders`·`intel`·판정·장부) | ✅     |                                     |
| A24폰 원달앱                    | ✅     | 16,336줄 (**5초마다** 찍는다)       |
| S23폰 관제앱                    | ❌     | `Capacitor/Console` 0줄             |
| GPS 궤적                        | ❌     | 저장하는 표가 없다                  |

---

# 3부 · 코드에서 확인한 것

**눈으로 파일을 열어 본 것만** 적는다. 원인 진단은 아니다.

## 3-1. 카드 파싱

🔍 `InsungParser.kt:500` `groupListNodes`

```kotlin
val fareNodes = allNodes.filter { it.text.matches(차종정규식) }  // 차종 글자로 카드를 찾는다
val rowNodes  = allNodes.filter { y좌표가 겹치는 것 }            // 같은 줄 글자를 묶는다
```

🔍 지금 로그는 _"비었다"_ 까지만 남기고, **그룹의 좌표나 원본 노드 수는 안 남긴다**

## 3-2. 관제앱 로깅

🔍 `logRoadmapEvent` 는 **조건 없이 `console.log`** 를 부른다
🔍 Capacitor 웹뷰의 `console` 은 logcat 에 `Capacitor/Console` 로 찍힌다
(그날 낮 `⚠️ [계약 불일치]` 경고가 실제로 그렇게 찍혔다)
🔍 부르는 자리는 **34곳**이고, 그 34곳은 전부 **사건**이다

```
order-evaluating · order-detail-received · order-evaluated   ← 콜이 잡혀야 찍힌다
KEEP · CANCEL · 국면 전환 · 필터 저장 · 지도 버튼            ← 눌러야 찍힌다
```

🔍 **주기적으로 찍는 자리는 없다** (GPS 수신 · 소켓 상태 · 화면이 그리는 것)

## 3-3. 화면 성능

🔍 쉬지 않는 애니메이션 **19개** — `animate-pulse` 13 · `animate-spin` 5 · `animate-ping` 1
🔍 매초 도는 타이머 **6곳** — 그중 `Header.tsx` 의 시계 `setInterval(…, 1000)`
🔍 지도가 **상시** 떠 있다
🔍 `index-*.js` **775KB** 단일 번들 (gzip 253KB)

## 3-4. 가격 필터

🔍 `InsungParser` 조건 2 가 `if/else` 다 — 단가 모델이 서면 `minFare` 를 **안 본다**

```kotlin
val fareMatch = if (useRateModel) {
    order.fare >= 거리 × 단가 && 상한       // minFare 를 안 본다
} else {
    order.fare >= filter.minFare && 상한   // 단가를 안 본다
}
```

🔍 실측: 10,000원 · 2km → `2 × 693 = 1,386 ≤ 10,000` **통과한다**
🔍 같은 조건이 `Hwamul24Parser` 에도 있다
❓ 서버 `OrderEvaluator` 에도 같은 판정이 있는지 — **대조 안 함**

## 3-5. 필터 설정창 (`OrderFilterModal`)

🔍 `OrderFilterModal.tsx:400` — `DialogContent` 에 높이 제한(`max-h`)이 **없다.**
같은 줄에 `overflow-hidden` 이 붙어 있다

```tsx
<DialogContent className="sm:max-w-lg … p-4 overflow-hidden flex flex-col gap-3">
```

🔍 `ui/dialog.tsx:63` — `DialogContent` 의 기본 클래스에도 높이 제한이 **없다.**
`top-[50%] translate-y-[-50%]` 로 중앙 정렬만 한다

🔍 `OrderFilterModal.tsx:454` — 안쪽에는 `overflow-y-auto` 가 있다

---

# 4부 · 아직 모르는 것 (❓)

## 4-1. 확인하면 알 수 있는 것 (로그가 이미 있다)

- ❓ 판정 29건의 색이 실제와 맞았나 — `order_judgments` 를 하나씩 대조하면 나온다
- ❓ 안전취소 24건이 **각각 몇 초 만에** 사라졌나
- ❓ 좌표가 몇 초 간격으로 들어왔나 — 서버 로그에 있다
- ❓ 도착 감지가 실제와 몇 분 차이였나 — 장부 vs GPS 187점

## 4-2. 확인하려면 코드를 더 남겨야 하는 것

- ❓ 카드가 왜 비었나 — 그룹 좌표·원본 노드 수를 안 남긴다 (§3-1)
- ❓ 주행 중 소켓이 몇 번 끊겼나 — 관제앱이 안 남긴다 (§3-2)
- ❓ 주행 중 화면이 무엇을 그리고 있었나 — 안 남긴다
- ❓ GPS 가 실제로 어떻게 흘렀나 — 187점은 «튄 지점»만이다 (§2-4)

## 4-3. 기사님만 답할 수 있는 것

- ❓ 무입력의 기본값은 무엇인가. 색마다 다른가
- ❓ 자동 KEEP 을 허용하는가
- ❓ 먼발치 1~2초에 읽어야 할 것 두세 개는 무엇인가
- ❓ KEEP/CANCEL 을 어떻게 누르고 싶으신가
- ❓ 사업자 등록 소요 · 배차망 가입 요건 · 이미 진행 중인 것

## 4-4. 자막 ⑬ 관련 (우리 판에 적용할지 미정)

- ❓ 합짐이 **뒤 콜을 미는 비용**을 어떻게 세나 (지금 판정에 이 개념이 없다)
- ❓ 「탈출」 국면을 둘 것인가 (지금 국면은 첫짐·합짐·운행·관내·복귀 다섯)
- ❓ 반경을 **지역 성격**(도심·외곽)에 따라 다르게 할 것인가
- ❓ 도착 전 통화 알림 — 거리 기준인가 시간 기준인가
