# 규칙 강제 대조표 — CLAUDE.md 27개 × 테스트

> **2026-08-16 실측.** 코드·테스트를 직접 읽어 대조했다. 추정이 아니다.
> 목적: *"규칙이 산문에만 있어서 Claude 가 잊으면 끝나는 것"* 이 무엇인지 세는 것.
> 이 문서는 대조 결과이고, **고치는 계획이 아니다.**

## 요약

| | 개수 | 뜻 |
|---|---|---|
| ✅ 강제됨 | **17** | 어기면 `npx jest` 가 빨간불 |
| 🟡 반쪽 | **5** | 서버만 · 알려진 경로만 · 알려진 미준수 |
| ❌ 없음 | **3** | 어겨도 아무도 모른다 |
| ⚪ 개념 | **2** | 태도라서 테스트로 만들 성질이 아님 |

**27개 중 22개는 이미 걸린다.** 산문이 문제가 아니었다.

---

## ❌ 아무도 안 잡는 것 3 + 🟡 반쪽 5

### 🔴 ①-1 「서버는 콜을 자동으로 버리지 않는다」 — **알려진 경로만** 본다

```
dispatchRules.test.ts  it('요율 미달은 사유만 남긴다 — 서버가 콜을 버리지 않는다')
                          └── 요율 경로 하나만 검사한다
```

2026-08-16 에 내가 `looksLikePlaceName` 으로 **새 경로**를 만들어 `오포읍` 콜을
`(주소 확인 중)` 으로 덮었다. 테스트는 그 경로를 모르니 통과했다.

🔴 **알려진 위반을 나열하는 검사는 새로 발명한 위반을 절대 못 잡는다.**
필요한 건 반대 방향 — **"콜이 죽을 수 있는 자리는 정확히 몇 곳인가"** 를 세는 검사다.
지금 서버의 출구는 셋뿐이다:

```
routes/detail.ts:268         session.pendingOrdersData.delete(...)
services/dispatchEngine.ts:44 session.pendingOrdersData.delete(...)
routes/emergency.ts:74        session.pendingOrdersData.delete(...)
```

### 🔴 ⑤-1 「돈은 앱이 이미 걸렀다 — 서버가 다시 세지 않는다」 — **코드가 지금 어기고 있다**

```ts
// OrderEvaluator.ts:284
if (filter.dispatchPhase === 'STANDBY' && filter.minFare > 0 && order.fare > 0) {
    if (order.fare < filter.minFare) → 똥콜         // ← 앱이 이미 거른 것을 다시 센다
}
// OrderEvaluator.ts:294  maxFare 도 같다
```
검사가 없어서 **규칙과 코드가 갈라진 채로 남아 있다.** (`todo.md` P4 와 같은 자리)

### 🔴 앱 4,029줄에 테스트가 **0개**

```
onedal-app/  Kotlin 4,029줄 · 테스트 파일 0개
```
그래서 앱 쪽 규칙은 **하나도 강제되지 않는다**:
- ②-5 30초 안전취소를 지우면 안 된다 → 지워도 아무도 모른다
- ②-7 앱이 `orderId` 를 **대조한다** → 서버가 싣는 것만 검사됨, 대조는 미검사
- ③-12 앱 기본값(오프라인 안전망) → 미검사

`./gradlew :app:compileDebugKotlin` 은 **컴파일만** 본다. 동작은 안 본다.

### 🟡 나머지

| 규칙 | 상태 |
|---|---|
| ②-8 타이머는 ID 를 저장한다 | ✅ 검사 있음. 단 **CLAUDE.md 자신이 `orders.ts` 30초는 미준수라고 적어 둠** |
| ②-9 HTTP 를 물고 기다리지 않는다 | ❌ 검사 없음 |
| ③-12 기본값의 원천은 DB | ✅ 판정·요율은 검사됨. **`?? 리터럴` 44개는 미검사** (CLAUDE.md 가 "지우지 말라"고 해 모순 상태) |
| ⑤-18 물리적으로 불가능한 콜도 미리 안 버린다 | ❌ 검사 없음 (①-1 과 같은 부류) |
| ⑤-4 값 만들기 전 넷(스키마·값·시점·화면) | 🟡 `judgmentDb.test.ts` 가 사실상 강제 — 다만 **판정 기준에만** 적용 |

---

## ✅ 이미 잘 걸리는 것 (대표)

| 규칙 | 어디서 잡히나 |
|---|---|
| ③-10 파생값 입력도 한 곳에서 | `phaseUi` · `traveledTrim` · `loadedRoute` · `callTarget` — **네 파일이 겹쳐 잡는다** |
| ④-14 없는 숫자를 지어내지 않는다 | `callNaming` · `loadedRoute` · `masterGps` · `callTarget` · `callSteps` |
| ⑤-5 상차 여유 ≠ 하차 여유 | `callNaming` 4건 (오늘 만든 것) |
| ⑥ 시퀀스를 압축하지 않는다 | `callSteps.test.ts` 30여 건 |
| 관제앱은 업무 단위 | `workUnit.test.ts` |
| ④-15 빈 필터는 고장이다 | `callFilterBlocker.test.ts` · `traveledTrim` |

---

## ⚠️ 이 표 자체의 한계

**✅ 는 "검사가 있다"이지 "검사가 진짜 잡는다"가 아니다.**
2026-08-16 에 내가 만든 지명 검사는 통과 케이스와 차단 케이스가 **다른 `it` 에 있어서**,
함수를 `return true` 로 바꿔도 스위트가 통과했다.

→ ✅ 17개 중 **변이 테스트로 확인한 것은 일부뿐이다.**
