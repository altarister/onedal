# 문제지 설계 — 이천 왕복 하루 (2026-09-15)

> 기사님 지시 2026-09-15: *"첫콜을 받고 출발을 해서 어떤 속도로 할 때 어디 오면 어떤 콜을 클릭하고 … 너는 문제리스트를 주소와 방향 모의 주행 시간을 가지고 설계하면 그걸 가지고 다른 에이전트가 만드는 거야"*
>
> - **설계:** onedal-b5 (필터 2~4단계를 만든 쪽).
> - **구현:** onedal-49 (현황판 콜 버튼 → 서버 → 시뮬레이터 폴링).
> - 🔴 **계획이다.** «폰이 올린다/막는다» 칸 가운데 🟡 표시는 경로를 직선으로 근사해 잰 **추정**이다. 실제 서버는 카카오 경로와 «동 경계가 띠에 걸치면 넣기»로 더 넉넉하다. 한 바퀴 돌고 이 문서의 칸을 실측으로 고친다.
> - 기준 코드는 커밋 `0667929a` 이다. 필터 영역·복귀 대기·심사석은 `docs/기획/목업_이식_전수표.md` 의 2~4단계다.

## 0. 이 문제지가 보려는 것

하루 한 바퀴 동안 필터가 **단계마다 모양을 바꾸는가**를 콜 하나씩 눌러 확인한다.

```
집 대기 ─01 확정─▶ 콜 쥠(출발 전) ─주행─▶ 출발 ─이천─▶ 관내 ─↩️복귀 켬─▶ 복귀 대기(목적지 둘) ─복귀콜 확정─▶ 복귀(집 하나) ─초월역─▶ 끝
 내∪목적∪마름모   +경로 · 내 영역 남음     내 영역 빠짐   원 안만    마름모 둘 · 🎯 둘          마름모 하나 · 🎯 하나
```

## 1. 시작 전에 맞출 것

| 무엇 | 값 | 어디서 |
|---|---|---|
| 목적지 | 이천시 | 🔍 필터 |
| 반경 | 40km 기준 자동 → 지금 **상차 4.55km · 목적지 원 4.55km · 마름모 15.9km · 라인 띠 2.73km** (잰 거리 18.2km) | 서버 `effectiveRadii` · `/api/sim/preflight` |
| 각도 | 출발각 120 · 목적각 120 | 🔍 필터 |
| 모양 | 노선 | 🔍 필터 |
| 제외 지역 | 서울 전체 | 🔍 필터 |
| 복귀 | **끔** | 🔍 필터 |
| 내 차 | 1t (100박스 · 다마스 30 · 승용차 5) | ⚙️ 설정 |
| 차 위치 | 현황판 «📍 위치 찍기» **🏠 집** — 모의 주행을 멈추고, 콜이 0건일 때 | 현황판 |
| 모의 주행 | 🚗 **보통(3배)** · 정차 **12초** — 현황판 ↩︎ 기본으로 (11초 아래면 «정차»가 안 굳는다 · 모의 좌표 도착은 500m 안이면 곧바로라 정차가 도착을 막지 않는다 · 2026-09-15 여섯 번째 바퀴 기사님 «정차가 너무 길고 속도가 너무 빠르다») | 현황판 |
| 관제웹 | ⌘+Shift+R | 브라우저 |

⚠️ 자동 반경은 **그날 처음 잰 거리**로 정해진다. 값이 다르면 «다시 구하기»를 누르거나, 1번 표의 반경으로 아래 정답을 다시 본다.

## 2. 모의 주행 실측 (🚗 보통 · 정차 18초 · 2026-09-14 23:39 바퀴 서버 로그)

한 걸음 = 1초에 1.5km (서행 0.38km) · 정거장에 닿는 거리 2.2km.

| 구간 | 거리 | 달리는 시간 | 그 정거장에 서 있는 시간 |
|---|---|---|---|
| 집 → 모다아울렛 | 2.2km | 약 9초 (확정 → 도착) | 약 19초 |
| 모다 → 곤지암성당 | 3.7km | 약 2초 | 약 21초 |
| 곤지암성당 → 신둔농협 | 6.2km | 약 6초 | 약 21초 |
| 신둔농협 → 이조갈비 | 2.2km | 약 2초 | 약 21초 |
| 이조갈비 → 이천제일 | 1.6km | 1초 안 | 약 22초 |
| 이천제일 → 이천터미널 | 1.8km | 약 1초 | 약 17초 |

🔴 **달리는 동안에는 누를 수 없다고 본다** — 구간이 대부분 몇 초다. 그래서 문제마다 **«○○에 정차한 동안»**에 누른다.

🔴 **정거장 순서는 서버(카카오)가 정한다.** 확정할 때마다 순서가 바뀔 수 있어서, 표의 정차 자리가 화면 순서와 다르면 **그 이름의 정차**에서 누른다.

## 3. 지점 (좌표는 카카오 장소검색 실값 · `onedal-sim/.../presets.ts`)

🔴 **폰이 보는 동 이름은 문제의 `region` 칸이다.** 시뮬레이터가 그 칸을 콜 목록에 적는다(`toForcedPair` → 생성기). 좌표에서 뽑지 않으니 `region` 을 주소 글자와 같게 적는다.

| 이름 | region (폰이 보는 동) | 주소 | 경도, 위도 | 집에서 | 이천 중심에서 | 차선 |
|---|---|---|---|---|---|---|
| 초월역 (집 대신) | 초월읍 | 경기 광주시 초월읍 경충대로 1066 초월역 | 127.299905, 37.373379 | 0.6km | 17.6km | — |
| 모다아울렛 | 초월읍 | 경기 광주시 초월읍 경충대로 907 모다아울렛 곤지암점 | 127.312587, 37.363298 | 2.2 | 16.0 | — |
| 곤지암성당 | 곤지암읍 | 경기 광주시 곤지암읍 경충대로543번길 19 곤지암성당 | 127.348642, 37.346213 | 5.9 | 12.4 | — |
| 신둔농협 예스파크 | 신둔면 | 경기 이천시 신둔면 도자예술로 72 신둔농협하나로마트 예스파크점 | 127.401207, 37.309733 | 12.0 | 6.3 | — |
| 이조갈비 | 사음동 | 경기 이천시 사음동 452-4 이조갈비함흥냉면 | 127.416293, 37.294522 | 14.1 | 4.1 | — |
| 이천제일 | 관고동 | 경기 이천시 관고동 107-5 이천제일식자재마트 | 127.429230, 37.285068 | 15.6 | 2.6 | — |
| 이천터미널 | 중리동 | 경기 이천시 중리동 219-1 이천터미널 | 127.446936, 37.277421 | 17.4 | 1.2 | — |
| HD현대 신둔 | 신둔면 | 경기 이천시 신둔면 경충대로 3126 HD현대오일뱅크직영 신둔주유소 | 127.40410, 37.30574 | 12.5 | 5.8 | 🔼 북행(집 방향) |
| 우리주유소 | 신둔면 | 경기 이천시 신둔면 경충대로 3282 우리주유소 | 127.39719, 37.31740 | 11.2 | 7.1 | 🔼 북행 |
| 곤지암스타주유소 | 곤지암읍 | 경기 광주시 곤지암읍 경충대로 698 곤지암스타주유소 | 127.33209, 37.35310 | 4.2 | 14.0 | 🔼 북행 |
| 마장주유소 | 마장면 | 경기 이천시 마장면 서이천로 564 마장주유소 | 127.40176, 37.27208 | 15.0 | 4.1 | 🔼 북행 |
| 롯데아울렛 이천 | 호법면 | 경기 이천시 호법면 프리미엄아울렛로 177-74 롯데프리미엄아울렛 이천점 | 127.40048, 37.24236 | 17.6 | 4.9 | — |
| 이마트 광주점 | 경안동 | 경기 광주시 경안동 493-4 이마트 광주점 | 127.258213, 37.410390 | 4.9 | 23.1 | — (집 뒤쪽 · A1 · 좌표는 서버 `geocode_cache` 카카오 실값) |

기준점: 이천시 중심 127.44727, 37.26682 · 광주시 중심 127.22458, 37.39285 (`callNet.cityCenter`) · 집 127.29444, 37.37669.

## 4. 문제 목록

칸의 뜻:
- **누를 때:** 정차 자리.
- **폰:** ⭕ 올린다 / ✖ 막는다(어느 칸에서).
- **기사님:** 관제웹에서 할 일.
- **보여야 하는 것:** 지도 · 심사석 · 서버 로그 줄.

### A. 집 — 모의 주행 멈춤 · 콜 0건

| # | 누를 때 | 콜 (상차 → 하차) | 요금 · 차종 | 폰 | 기사님 | 보여야 하는 것 |
|---|---|---|---|---|---|---|
| A1 | 집 · 맨 처음 | 이마트 광주점(경안동) → 신둔농협(신둔면) | 3만 · 다마스 | ✖ **상차 목록** — 콜 전 상차 목록은 현위치 반경뿐, 경안동은 뒤쪽 7km 반경 밖 (#134 · 2026-09-15 개정 — 기사님 «마름모 영역까지 상차를 20분 안에 할 수 없잖아»로 마름모를 걷었다) | — | 알람 없음 |
| A2 | A1 뒤 | 모다아울렛(초월읍) → 신둔농협(신둔면) | 5만 · 다마스 | ⭕ 상차 2.2km · 신둔면이 목록(23동)에 있음 | **KEEP** | 로그 `🔗 [불변식] dispatchPhase STANDBY → GATHERING` · `🕸️ [필터 목록] 목적지 이천시 · 라인(얼린 경로)`. 지도에 **내 위치 점선 원**이 남는다. 집 뒤 **매산동·쌍령동·양벌동 파란 점이 안 빠진다** |
| A3 | A2 확정 뒤 | 곤지암성당(곤지암읍) → 이천제일(관고동) | **5천** · 다마스 | ✖ **요금** — 9.8km × 616원 = 6,036원 하한. 확정 뒤라 상차 반경은 안 본다 | — | 알람 없음 |
| A4 | A3 뒤 | 모다아울렛(초월읍) → 이천터미널(중리동) | 15만 · **5t** | ✖ **차종** | — | 알람 없음 |
| — | A4 뒤 | — | — | — | 현황판 모의 주행 **▶ 시작** | 몇 초 뒤 로그 `🚀 [출발]` **한 번** · `dispatchPhase GATHERING → DELIVERING`. 점선 원이 사라지고 매산·쌍령·양벌 점이 빠진다. 시트가 내려간다 |

### B. 가는 길 — 출발 뒤

| # | 누를 때 | 콜 | 요금 · 차종 | 폰 | 기사님 | 보여야 하는 것 |
|---|---|---|---|---|---|---|
| B1 | **모다아울렛 정차** | 곤지암성당(곤지암읍) → 이천제일(관고동) | 5만 · 다마스 | ⭕ 곤지암읍·관고동이 목록에 있음 (합짐이라 상차 반경 안 봄) | **KEEP** | 심사석 판에 결론 한 줄: «✅ 기존 콜은 안 밀린다» 또는 «⚠️ … N분 늦어진다 · ☎️». 둘째 줄 끝 «· 더 쓰는 N분». 지도에서 이 후보가 늘린 구간만 판정 색으로 굵게 |
| B2 | **곤지암성당 정차** | 이천터미널(중리동) → 모다아울렛(초월읍) | 5만 · 다마스 | ✖ **상차 목록** — 곤지암에서 이천터미널은 현위치 원 ∩ 라인 밖 (#134 · 초월읍도 지나와서 하차 목록에서 빠졌다) | — | 알람 없음. 폰 로그캣 `도착지(… 초월읍)=❌` |
| B3 | **신둔농협 정차** (A2 하차) | 이조갈비(사음동) → 이천터미널(중리동) | 5만 · 다마스 | ⭕ 사음동·중리동 목록 안. 적재 90/100 | **KEEP** | 로그 `🎯 [판] … → 이천시`. 심사석 «근거 ▾» 맨 아래 «기존 콜 — 이 후보를 받으면»에 정거장마다 «약속 → 예정 (±N분)» |

### C. 이천 — 복귀 대기

🔴 **관내는 목록을 다시 만들 때만 정해진다**(확정 · 하차 완료). 이천에 들어서기만 해서는 안 켜지고, 이천 안에서 하차를 한 번 마쳐야 켜진다(지난 바퀴 21:16:32 실측). 그래서 관내 확인은 C 가 아니라 **이천제일 하차를 마친 뒤**에 본다.

| # | 누를 때 | 콜 | 요금 · 차종 | 폰 | 기사님 | 보여야 하는 것 |
|---|---|---|---|---|---|---|
| C1 | **이조갈비 정차** | — | — | — | 🔍 필터 **↩️ 복귀 켬** | 지도에 🎯 **둘**(이천 · 광주), 마름모 **둘**. 로그 `🧭 [국면 전환] DEST → HOME` · `🕸️ [필터 목록] 목적지 이천시 ∪ 광주시`. 폰 목록이 늘어난다 (🟡 추정 37동 — 광주 경안동·초월읍·곤지암읍·마장면 등) |
| C2 | **이천제일 정차** (B1 하차) | 이천터미널(중리동) → 이천제일(관고동) | 3만 · 승용차 | ⭕ 관고동이 이천 목적지 원 안 — 목적지가 살아 있다 | **❌ 취소** (확인만 · 잡지 않는다) | 알람이 울리고 심사석이 뜬다. **D1 과 대조한다** |
| C3 | **이천터미널 정차** (B3 하차 · 이천 일을 다 내린 뒤 — B3 확정 직후면 동쪽으로 가는데 상차가 서쪽 신둔이라 뒤로 가기 · 2026-09-15 여섯 번째 바퀴) | HD현대 신둔(신둔면) → 초월역(초월읍) | 5만 · 다마스 | ⭕ 신둔면·초월읍이 합친 목록 안 (초월읍은 광주 그물) | **KEEP** | 로그 `🎯 [판] … → 광주시` · `🕸️ [필터 목록] 목적지 광주시`. 지도 🎯 **하나**, 마름모 **하나**. 콜 카드 하차지 옆에 «🎯광주시» / «🎯이천시» (판이 섞였으니 보인다) |

### D. 복귀 — 복귀콜 확정 뒤

| # | 누를 때 | 콜 | 요금 · 차종 | 폰 | 기사님 | 보여야 하는 것 |
|---|---|---|---|---|---|---|
| D1 | **이천터미널 정차** (B3 하차) | 이천터미널(중리동) → 이천제일(관고동) | **3만 1천** · 승용차 | ❌ 하차지 — 복귀콜(C3)을 쥐었으니 하차 목록이 집 · 관고동은 밖 (2026-09-15 일곱 번째 바퀴 intel `region` · 옛 «올라오면 취소(추정)»은 틀렸다) | 기다리기만 | 요금만 C2 와 다르다 (폰 지문) |
| D2 | D1 뒤 · **이천터미널 정차** | 이천터미널(중리동) → 롯데아울렛(호법면) | 5만 · 승용차 | ✖ **도착지** — 호법면은 어느 목록에도 없다 | — | 알람 없음 |
| D3 | **HD현대 신둔 정차** (C3 상차) | 우리주유소(신둔면 · 북행) → 곤지암스타(곤지암읍 · 북행) | 3만 · 승용차 | ⭕ 🟡 곤지암읍은 집 가는 경로 띠에 걸친다 | **KEEP** | 오는 길 합짐. 로그 `🎯 [판] … → 광주시` |
| D4 | **우리주유소 정차** (D3 상차) | 마장주유소(마장면) → 초월역(초월읍) | 3만 · 승용차 | ✖ **상차 목록** — 복귀콜을 쥐었으니 현위치 원 ∩ 라인 띠, 마장면은 라인 밖 (#134 · 실제 지도 검사) | — | 알람 없음 |
| — | **초월역 정차** (C3 하차) → 곤지암스타 → 끝 | — | — | — | — | 마지막 하차 뒤 로그 `🔗 [불변식] dispatchPhase DELIVERING → STANDBY` · `🧭 [타겟 자동 순환] HOME → DEST` (복귀콜이 있었으니 맞다). 목록이 집 대기 23동으로 돌아온다 |

## 5. 단계별 필터 목록 — 🟡 추정

`shared/callNet.netForGoal` 에 1번 표의 반경을 넣고, 경로는 정거장을 잇는 **직선**으로 잰 값이다. 실제 서버는 카카오 경로와 경계 걸침을 더해 더 많다.

| 단계 | 동 수 | 목록 |
|---|---|---|
| 집 대기 (A1) | 23 | 갈산동 고담동 곤지암읍 관고동 도척면 마장면 매산동 사음동 산북면 송정동 신둔면 쌍령동 안흥동 양벌동 양지읍 율현동 장록동 중리동 증일동 증포동 진리동 창전동 초월읍 (서버 실측과 같다) |
| 관내 — 이천 원 안 | 13 | 갈산동 고담동 관고동 사음동 송정동 안흥동 율현동 장록동 중리동 증일동 증포동 진리동 창전동 |
| 복귀 대기 — 합친 목록 (C1) | 37 | 위 관내 13 + 신둔면 곤지암읍 초월읍 도척면 마장면 매산동 산북면 쌍령동 양벌동 양지읍 + 광주 경안동 고림동 고산동 마평동 목동 삼동 역동 운학동 장지동 중대동 직동 추자동 탄벌동 태전동 |
| 복귀콜 확정 뒤 (C3) | 26 | 이천 동은 **경로 띠로만** 남는다 → 차가 지나가면 빠진다. 마장면·도척면·곤지암읍·초월읍은 직선 근사에서 빠지지만 🟡 실제 경로가 걸치면 들어온다 |

## 6. 알고 돌리는 것 (이 문제지로 드러날 수 있는 것)

| 무엇 | 어디서 보이나 | 상태 |
|---|---|---|
| 심사석 결론의 «N분 늦어진다»가 **추정 약속**까지 센다 — 판정 색은 통화로 확정한 약속만 봐서 둘이 다르게 말할 수 있다 | B1 · B3 · C3 | ⬜ 고칠 안 B — «(통화 전 추정)»을 붙인다 |
| 정거장 줄의 동 이름이 좌표 중심점이라 넓은 면에서 틀린다 (신둔면 → «사음동») | 근거 «기존 콜» 줄 | ⬜ 고칠 안 C |
| 정거장 이름이 «콜 하차»로 나온다 (타겟명 빠짐) | 근거 · 관문 문장 | ⬜ 고칠 안 D |
| 출발 뒤 합짐을 확정하면 차 바로 뒤 동(초월읍)이 **약 17초** 다시 들어왔다 빠진다 | B1 확정 직후 | 알고 둔다 — B2 를 곤지암성당 **정차**에서 누르면 이미 빠진 뒤다 |
| 복귀콜 없이 사이클이 끝나면 복귀가 저절로 꺼진다 | 이 문제지에서는 안 걸린다 (C3 에서 복귀콜을 잡는다) | ⬜ 고칠 안 A |
| 모의 주행이 경로를 새로 받을 때 차가 **처음 자리로 튄다** (23:37:48 · 23:41:53 `🚨 [위치 점프]`) | 확정 직후 | 🔍 원인 안 봄 — 튀면 지나온 곳 빼기가 틀어진다. 한 바퀴에서 몇 번 나는지 적는다 |
| 앱이 콜 목록이 빈 동안은 60초마다만 서버에 묻는다 — 필터 변경이 늦게 간다 | 정차 중 목록이 비었을 때 | 알고 둔다 — 다음 콜이 뜨면 1초마다로 돌아온다 |

## 7. 시나리오콜 — 서버가 사건순으로 내고, 현황판 «🎬» 카드가 보여 준다 (코드 2026-09-15)

> 기사님 2026-09-15:
> - *"너가 알려주는 건 안 돼, 시선이 분산되니까. 현황판에서 모두 해결할 수 있도록 해줘"*
> - *"시뮬레이터에 내기 버튼을 클릭하면 서버가 그냥 콜리스트를 … 순서대로 뿌리면 되는거 아냐?"* → **목록은 서버 · 시계순이 아니라 사건순**
>
> 기사님이 할 일: 시뮬레이터를 «🚚 개별콜» 탭으로 켠다 → 현황판 **[▶ 시작]** → 카드의 «지금» 줄이 시키는 대로 KEEP · ❌ 취소 · ↩️ 복귀 켬 · 모의 주행 ▶ 시작.

### 7-1. 어디에 무엇이 있나

| 자리 | 파일 | 하는 일 |
|---|---|---|
| 줄 데이터 | `server/src/core/simScenarioIcheon.ts` | §3 지점 + §7-5 16줄 — **한 곳**. 이 문서를 고치면 여기도 고친다 |
| 판단 | `server/src/core/simScenario.ts` 의 `stepScenario` | 순수 함수 — «세상»을 받아 줄 상태와 낼 콜을 낸다 (검사 `server/tests/core/simScenario.test.ts`) |
| 서버 경로 | `server/src/routes/sim.ts` | `GET /api/sim/scenario` 상태 · `POST /scenario/start` · `/skip` · `/stop` — 켜진 동안 1초마다 «세상»(메모리 콜 · `intel` 최근 50줄 · 도는 필터)을 읽어 판단에 넘기고, 낼 콜은 **개별콜 대기열**(`pushSimCall`)에 넣는다 · 개발 빌드만 · 시뮬레이터가 10초 안에 안 물었으면 시작을 안 받는다 |
| 현황판 | `client-app/src/statusboard/ScenarioCard.tsx` | 1.5초마다 상태를 읽어 그리기만 — 시작 조건 줄 · ▶ 지금 줄 크게 · 다음 줄 · 지난 줄 ✅/🔴 · [▶ 시작] [⏭️ 건너뛰기] [⏹ 멈춤] [📋 결과 복사] |

### 7-2. 줄의 네 종류와 끝나는 규칙 (`stepScenario`)

| 표시 | 뜻 | 보낸 뒤 «지금» 줄 | ✅ | 🔴 · 🟠 · ❔ |
|---|---|---|---|---|
| 🟢 **KEEP** | 올라와야 하고 잡는다 | 짝 콜이 오면 «🟢 지금 관제웹에서 KEEP» | 짝 콜이 확정 상태(`ORDER_CONFIRMED` 이후) | 취소됨 🔴 · 폰이 막았다(`verdict` 가 막힌 축) 🔴 · 75초(원달앱 하트비트 60초 + 여유) 안 올라옴 🔴 (끝내지 않고 기다린다 — 건너뛰기는 기사님) |
| 🟡 **취소** | 올라와야 하고 잡지 않는다 | «🟡 지금 관제웹에서 ❌ 취소» | 짝 콜이 취소 상태 | KEEP 했다 🟠 · 폰이 막았다 🔴 · 75초 안 올라옴 🔴 |
| ⚪ **막힘** | 폰이 막아야 한다 | «⏳ 폰이 읽는 중 — 안 올라와야 맞다» | `intel.verdict` = 막을 축 | 다른 축 🟠 · `pass` 면 15초 기다려 콜이 오면 «❌ 취소» 안내, 안 오면 🔴 · 짝 콜이 오면 «🔴 뚫림 — ❌ 취소» · `locked`/`null` ❔ · 30초 판정 없음 ❔ (기다린다) |
| 🧭 **할 일** | 기사님 조작 | 줄의 할 일 문장 | 필터 값(`done`)이 참 | — |

- **끝난 줄은 3초 뒤** 필터 채점(7-4)을 하고 다음 줄로 넘어간다 — 서버가 목록을 다시 만드는 틈이다.
- **짝짓기는 좌표로 한다.**
  - 보낸 뒤 **새로 생긴** 메모리 콜과 `intel` 줄 가운데, 상차·하차 좌표가 각각 **0.2km 안**인 것이 짝이다.
  - 주소 글자는 서버가 `normalizeAddress` 로 줄여 적어서 쓰지 않는다.
  - 좌표가 같은 콜(C2 ↔ D1)도 «보낸 뒤 새로 생긴 것»만 보니 앞 줄 콜을 제 것으로 집지 않는다.
- **막을 축 낱말**은 폰이 `verdict` 에 보내는 그대로다 — `pickup` · `fare` · `vehicle` · `region` · `routeOrder` · `blacklist`. 한국어는 `client-app/src/statusboard/callVerdict.ts` 의 `VERDICT_AXIS_LABEL`.

### 7-3. «○○에 서면» — 콜을 내는 때

- 줄의 `when` 이 `{ arrive: 'A2', stop: 'dropoff' }` 이면, A2 줄이 짝지은 콜의 **`arrivedDropoffAt`** 이 찍혀야 서버가 그 줄 콜을 낸다 (서버가 GPS 도착을 적는 그 칸 · `dispatchEngine`).
- 찍히기 전에는 «지금» 줄에 «⏳ 신둔농협 예스파크에 서면 낸다».
- 가리키는 줄의 콜을 못 잡았으면(막혔거나 건너뜀) 기다릴 수 없다고 적는다 — [⏭️ 건너뛰기].

### 7-4. 스스로 채점하는 것 (`useFilterConfig` 의 `filter`)

줄이 ✅ 로 넘어간 뒤 **3초** 기다렸다가 본다 (서버가 목록을 다시 만드는 틈). 결과는 «지난 줄»에 괄호로 남긴다.

| 이름 | 보는 값 |
|---|---|
| `phase` | `filter.dispatchPhase` |
| `goals` | `filter.goalCities.length` |
| `target` | `filter.callTarget` |
| `listHas` / `listLacks` | `filter.destinationKeywords` 에 그 동이 있다 / 없다 |

지도 모양·심사석 문구처럼 카드가 못 보는 것은 **채점하지 않는다** — 끝난 뒤 «결과 복사» 글에 «눈으로 볼 것» 으로만 적는다 (달리는 중에 기사님 눈을 뺏지 않는다).

### 7-5. 줄 목록 (카드가 쓰는 그대로)

콜의 주소·좌표는 §3 표를 그대로 쓴다 (`region` 포함).

| 줄 | when | 콜 (상차 → 하차 · 요금 · 차종) | 표시 · 막을 축 | 할 일 문장 | 채점 (7-4) |
|---|---|---|---|---|---|
| A1 | 바로 | 이마트 광주점 → 신둔농협 예스파크 · 30000 · 다마스 | ⚪ `pickupList` | — | — |
| A2 | A1 뒤 | 모다아울렛 → 신둔농협 예스파크 · 50000 · 다마스 | 🟢 | — | `phase = GATHERING` · `listHas 매산동 쌍령동 양벌동` |
| A3 | A2 뒤 | 곤지암성당 → 이천제일 · **5000** · 다마스 | ⚪ `fare` | — | — |
| A4 | A3 뒤 | 모다아울렛 → 이천터미널 · 150000 · **5t** | ⚪ `vehicle` | — | — |
| M1 | A4 뒤 | — | 🧭 | 현황판 모의 주행 **▶ 시작** (🚗 보통 3배 · 정차 12초) | 알아채는 값 `phase = DELIVERING` → `listLacks 매산동` |
| B1 | A2 상차에 서면 | 곤지암성당 → 이천제일 · 50000 · 다마스 | 🟢 | — | — |
| B2 | B1 상차에 서면 | 이천터미널 → 모다아울렛 · 50000 · 다마스 | ⚪ `pickupList` | — | `listLacks 초월읍` |
| B3 | A2 하차에 서면 | 이조갈비 → 이천터미널 · 50000 · 다마스 | 🟢 | — | — |
| C1 | B3 상차에 서면 | — | 🧭 | 관제웹 🔍 필터 **↩️ 복귀 켬** | 알아채는 값 `target = HOME` → `goals = 2` |
| C2 | B1 하차에 서면 | 이천터미널 → 이천제일 · 30000 · 승용차 | 🟡 | — | — |
| C3 | B3 하차에 서면 | HD현대 신둔 → 초월역 · 50000 · 다마스 | 🟢 | — | `goals = 1` |
| D1 | B3 하차에 서면 | 이천터미널 → 이천제일 · 31000 · 승용차 | ⚪ 막힘 `region` | — | — |
| D2 | D1 뒤 | 이천터미널 → 롯데아울렛 이천 · 50000 · 승용차 | ⚪ `region` | — | — |
| D3 | C3 상차에 서면 | 우리주유소 → 곤지암스타주유소 · 30000 · 승용차 | 🟢 | — | — |
| D4 | D3 상차에 서면 | 마장주유소 → 초월역 · 30000 · 승용차 | ⚪ `pickupList` | — | — |
| E1 | D4 뒤 | — | 🧭 | 마지막 하차지에 서면 관제웹에서 하차 완료 (초월역 · 곤지암스타) — 모의 주행은 콜이 없으면 그 자리에서 대기한다 (#133 개정) | 알아채는 값 `phase = STANDBY` → `target = DEST` |

🔴 **한 회차 안에 폰 지문(상차 동 + 하차 동 + 요금)이 겹치면 안 된다** — 폰(`CallMemory`)은 본 콜을 그 지문으로 기억해 다시 판정하지 않는다. 그래서 C2 와 D1 은 요금을 다르게 뒀다 (검사 `server/tests/core/simScenario.test.ts`).

🧹 **[▶ 시작]은 이전 회차 콜을 셋 다 리셋한다** (버그 대장 #129) — 회차가 달라도 같은 콜이면 요금을 바꾸지 않는다.

| 어디 | 무엇을 | 어떻게 |
|---|---|---|
| 서버 | 대기열의 콜 | `resetSimCalls` — 비우고 회차(`round`)를 올린다 |
| 시뮬레이터 | 개별콜 목록 · 확정 목록 · 열린 상세 | `GET /api/sim/calls` 답의 `round` 가 바뀌면 `takeInjected` 가 `clear` → `DispatchPage` 의 `resetCalls` |
| 폰 원달앱 | 본 콜 기억 | `/api/scrap` 응답 `deviceControl.callMemoryRound` 가 바뀌면 `CallMemory.onRound` 가 비운다 (앱 2.9.5 부터) |

🔴 **폰이 새 회차를 받기 전에는 첫 줄을 안 낸다** — 폰이 기억을 비우기 전에 A1 이 뜨면 «이미 본 콜»로 삼킨다. 카드는 그동안 «🧹 이전 콜 리셋 중»을 띄운다. 시뮬레이터 목록이 비는 순간 폰 화면이 바뀌어 곧 받고, 늦어도 폰의 1분 생존신고에 받는다.

🔴 **폰은 처음 걸린 축 하나만 보낸다** — 한 콜이 두 축에 걸리면 어느 쪽이 먼저인지는 폰 순서다. 그래서 막힘 줄은 **한 축에만 걸리게** 짰다(D 줄은 적재가 안 걸리게 승용차). 🟠 가 나오면 문제지를 고칠 자리다.

🟡 추정 줄(D1 · D3 · D4 — §4)은 🔴 가 나와도 **제품 고장이 아니라 설계 추정이 틀린 것**일 수 있다 — 결과 글에 그 표시를 함께 적는다.

### 7-6. «결과 복사» 글

한 번 누르면 아래 모양의 글을 클립보드에 넣는다 — 기사님이 채팅에 붙이면 onedal-b5 가 서버 로그와 대조한다.

```
🎬 이천 왕복 하루 · 2026-09-15 14:20~14:31 · 서버 bootedAt …
A1 ✅ 막힘 · A2 ✅ KEEP (phase GATHERING ✅ · 매산동 쌍령동 양벌동 ✅) · … · D1 🔴 뚫림(🟡추정) · …
눈으로 볼 것: 내 위치 점선 원(A2) · 🎯 둘/하나(C1/C3) · 심사석 결론(B1 B3 C3) · 후보 구간 판정 색(B1)
```

### 7-7. 짝

- 콜 모양은 개별콜과 **같다** — 서버 `core/simCallQueue.ts` 의 `SimCallInput`. 시뮬레이터(`useSimInjectedCalls`)는 개별콜과 같은 길로 받는다.
- 🔴 **시뮬레이터가 켜지기 전에 낸 콜은 안 받는다** (`simCallsAfter` 의 첫 물음) — 그래서 서버가 «10초 안에 물었나»로 시작을 막는다.
- 🔴 **서버를 다시 띄우면 시나리오도 멈춘다** — 메모리 타이머다. 다시 [▶ 시작].

## 8. 이천 성공하는 5콜 — 빨리 도는 문제 (2026-09-15)

기사님: *"빠른시간에 잘되는 콜들로 빨리 빨리 테스트 하고 싶어 — 5개 짜리 문제 · 2개는 갈때 1개는 복귀클릭하고 복귀콜이 잡히기전에 나머지는 복귀 콜로"* · *"이천 왕복하루 아래에"*.

- 🔴 **막힘·취소 줄이 없다** — 콜 다섯이 전부 KEEP. 경로는 §7-5 에서 **실제로 KEEP 까지 간 것**만 쓴다 (13:04 바퀴 A2 · B1 · B3 · C3 · D3 ✅).
- 줄 데이터는 `server/src/core/simScenarioIcheon.ts` 의 `ICHEON_FIVE_OK` 한 곳 · 현황판은 «이천 왕복 하루» 카드 아래 «🎬 이천 성공하는 5콜» 카드 (서버 `sim.ts` 의 `SCENARIOS` 이름표 `icheonFive`).
- 시작 조건은 §1 과 같다 (목적지 이천시 · 복귀 끔 · 콜 0건 · 시뮬레이터 «🚚 개별콜» 탭).

| 줄 | when | 콜 (상차 → 하차 · 요금 · 차종) | 표시 | 할 일 · 채점 |
|---|---|---|---|---|
| S1 | 바로 | 모다아울렛 → 신둔농협 예스파크 · 50000 · 다마스 | 🟢 가는 길 1 (= A2) | — |
| M1 | S1 뒤 | — | 🧭 | 모의 주행 **▶ 시작** · 알아채는 값 `phase = DELIVERING` |
| S2 | S1 상차에 서면 | 곤지암성당 → 이천제일 · 50000 · 다마스 | 🟢 가는 길 2 (= B1) | — |
| C1 | S2 하차에 서면 | — | 🧭 | **↩️ 복귀 켬** · 알아채는 값 `target = HOME` |
| S3 | C1 뒤 | 이조갈비 → 이천터미널 · 50000 · 다마스 | 🟢 복귀콜 전 이천 안 콜 (= B3 경로 · 🟡 추정 — 복귀 켠 뒤로는 처음) | — |
| S4 | S3 하차에 서면 | HD현대 신둔 → 초월역 · 50000 · 다마스 | 🟢 복귀콜 1 (= C3) | — |
| S5 | S4 상차에 서면 | 우리주유소 → 곤지암스타주유소 · 30000 · 승용차 | 🟢 복귀콜 2 (= D3) | — |
| E1 | S5 뒤 | — | 🧭 | 마지막 하차지에서 하차 완료 · 알아채는 값 `phase = STANDBY` |
