# 실전 콜 판정 설계서 — 지금 기준

> 🔴 **이 설계서의 핵심 과제는 2026-09-21 판정 개편(Step 1~5)을 통해 코드에 전수 구현·검증되었습니다.**
> §9의 항목은 전부 완료되었으며, **후속 국면별 잔여 과제(Step 5.5 ~ Step 9)는 [국면별 판단 직행 로드맵](국면별_판단_직행_로드맵.md)으로 단일화되어 통합 관리됩니다.**
> 지금 도는 판정은 [docs/지금/판정.md](../지금/판정.md) · 개편 이력은 [docs/기록/판정_개편_이력.md](../기록/판정_개편_이력.md) 가 답합니다.
>
> **근거**: 노하우 ep5(콜 단가·꿀콜 판별) · ep8(매출) · 2026-09-19 04:54 실전 서버 로그(복정동 → 대치4동) ·
> `order_judgments` 53건 전수조사 · 08-29 판정 전수조사 결정
>
> **낱말**: 무조건 빨간불(점수와 무관하게 🔴) · 기준(돈·약속·공간·성질·지리) · 우회 시급(추가 운임 ÷ 추가 걸린 분 × 60).
> «게이트»·«문지기»·«축»은 안 쓴다 — 08-29 에 버린 말이다.

---

## 1. 무엇을 고치려는 것인가

### ① 쌓인 판정 53건이 말한 것

`order_judgments` 표를 전수 조사한 결과다. 추측이 아니라 실제 기록이다.

| 기준 | 점수를 낸 횟수 | 낸 점수 | 판별력 |
|---|---:|---|---|
| 돈 | 50 | 넓게 퍼짐 | 있다 |
| 공간 | 20 | 0 · 20 · 65 · 70 | 있다 |
| 성질 | 20 | **전부 100** | **없다** |
| 약속 | 18 | **15건이 0** | 망가졌다 |
| 지리 | 0 | — | 꺼져 있다 |

| 병 | 증거 |
|---|---|
| **성질이 상수다** | 스무 건 전부 100점. 평균을 25점씩 올리기만 한다 |
| **약속 곡선에 음수 구간이 없다** | 여유 −8분과 −103분이 똑같이 0점. 얼마나 늦는지가 사라진다 |
| **그 0점이 평균에 녹는다** | 여유 −103분인데 **보통 49점**. −91분도 **보통 66점** |
| **첫짐은 돈 하나가 결정한다** | 53건 중 30건이 그렇다. 총점이 돈 점수와 같다 |
| **돈 눈금이 3만에서 천장을 친다** | 3만/h 와 5.1만/h 가 같은 100점. 좋은 콜끼리 구분이 안 된다. 비율식이라 우회 235분도 요금이 크면 보통이다 |
| 🔴 **평균이 순위를 뒤집는다** | 같은 판에서 **1.6만/h 콜이 48점, 3.9만/h 콜이 43점**. 나쁜 콜이 이겼다. 돈(버는 것)과 약속·공간(남는 여지)은 뜻이 달라 더해 나눌 수 없다 |

### ② 04:54 실전 로그 — 복정동 → 대치4동

- 운임 12,000원 ÷ 65분(주행 17 + 대기·상하차 48) = **환산 시급 1.1만/h** → 시스템 판정 **🟡 36점 똥**
- 기사님: *"새벽에 복정에서 서울(강남) 방면으로 올라가는 첫 단추 콜인데 왜 똥콜이지?"*
- 원인 둘: 빈 차라 약속·공간·성질이 빠지고 **돈 혼자 36점**을 냈다 · 첫짐에는 붙일 경로가 없는데
  **목적지 쪽으로 나아가는 값**을 재는 규칙이 비어 있었다 — 규칙 ⑤-1(«지금 경로에 붙이면 얼마나 돌아가나»)의 빈칸이다

---

## 2. 노하우에서 가져오는 것 — 원리 둘

**ep5 — 우회 시급 (합짐의 원리)**
> *"주우러 가는데 15분, 갖다 주는데 15분 추가로 걸리는 2만 원짜리 오더는 시간당 가치 4만 원이다.
> 내가 이동하는 경로상에 얹혀져 약간의 시간만 더 쓰는 오더라면 남들이 다 버릴 때 나에게는 황금이 된다."*

합짐은 이미 짐을 싣고 달리는 중이라 **안 잡아도 손해가 없다.** 그래서 눈금을 높고 엄격하게 둔다.

**ep8 — 바퀴를 굴려라 (첫짐의 원리)**
> *"바퀴를 굴려서 손해 보는 날은 없다. 중심지 쪽으로 몸을 움직여라."*

빈 차로 서 있으면 시급은 0원이다. 첫짐에 합짐과 같은 눈금을 대면 기사를 길가에 묶어 둔다.
**기사님이 정한 목적지 쪽으로 움직이며 기름값을 버는 첫짐은 살린다.**

🔴 **노하우의 숫자(꿀 5.1만/h 등)는 코드 상수에 박지 않는다.** DB 칸의 기본값으로 두고 화면에 «업계 기준값 — 실측이 쌓이면 바꾼다»라고 적는다 (기사님 확정 · 08-17 원칙을 그렇게 좁혔다).

---

## 3. 두 층으로 가른다

```
[1층] 되나 안 되나 — 무조건 빨간불
   성질  같이 못 싣는 짐인가 (위험물 상극 · 금지 낱말)
   약속  통화로 굳힌 약속이 늦나         (임시 약속이 늦으면 → 2층에서 약속 0점)
   공간  신고한 적재로 자리가 없나       (추정 적재로 없으면 → 2층에서 공간 0점)
      │
      └─ 하나라도 걸리면 🔴 사고. 점수 평균에 들어가지 않는다
      🔴 무조건 빨간불은 확정값만 본다 — 통화로 굳힌 약속 · 기사님이 신고한 적재.
         추정값(잡은+20분 임시 시한 · 차종 글자에서 넣은 기본 박스 수)은 점수다
      │
[2층] 얼마나 좋나 — 점수
   합짐  우회 시급 하나 — 두 점 꺾은선       (안 잡아도 손해가 없다 → 엄격하게)
   첫짐  시급 × 목적지 전진 배수             (안 잡으면 0원이다 → 너그럽게)
```

🔴 **2층에서 약속·공간을 어떻게 셀지는 아직 안 정했다** — 이 문서 안에 두 말이 있다.
§3 은 «합짐 총점은 우회 시급 하나»라 하고, §4-1 표는 «추정이면 약속·공간 점수 0»이라 한다.
총점이 돈 하나면 그 0점은 아무 데도 안 쓰인다. **둘 다 참일 수 없다.** 지금 코드는 셋째 길(넷 평균)이고 그것이 순위를 뒤집는다 (§5).
후보는 «돈 × 여지 배수» — 첫짐과 같은 모양이라 갈래가 늘지 않고, 약속·공간을 버리지 않는다. 기사님 결정 대기.

- 🔴 **판정은 색만 낸다.** 점수가 높다고 자동으로 잡지 않는다 (규칙 ①)
- **첫짐과 합짐은 눈금이 다르다** — 기회비용이 다르기 때문이다. 함수는 하나다(08-29 결정 유지). 사실을 채운 뒤 국면(`first`/`merge`)으로 눈금만 고른다
- **확정값만 무조건 빨간불** — 약속(통화로 굳힌 것만, 코드 그대로)과 공간(신고한 적재만, 기사님 확정)이 같은 원칙이다.
  추정값으로 색을 덮으면 오독 한 번에 멀쩡한 콜이 🔴 가 된다 — 차종 불일치를 딱지로 내린 것과 같은 까닭

---

## 4. 공식

### 4-1. 무조건 빨간불

| 기준 | 🔴 가 되는 조건 | 값의 출처 | 조건이 추정값에만 걸리면 |
|---|---|---|---|
| 성질 | 실린 짐과 상극인 성질 · 금지 낱말 | 콜 글자 · 실린 콜의 성질 | — (추정 없음) |
| 약속 | 이미 실린 콜의 **통화로 굳힌** 도착 약속이 늦는다 | `timing.ts` 의 `declared` 약속 | 약속 점수 0 (색은 안 덮는다) |
| 공간 | **기사님이 신고한**(또는 실측한) 박스 수로 자리가 없다 | 통화 신고 · 현장 실측 (`capacityConfidence`) | 공간 점수 0 (색은 안 덮는다) |

**적재는 «함께 실리는 최대»로 센다** — 잡은 콜을 다 더하지 않는다. 3번을 싣기 전에 1번을 내리면 그 자리가 돌아온다.
경로의 정거장 순서를 걸으며 상차에 더하고 하차에 빼서 그 동안의 최대값을 쓴다 (`peakLoadPoints`).
🔴 **순서를 모르면 다 더한 값으로 물러선다** — 낙관하면 «들어갈 줄 알았는데 안 들어가는» 사고가 난다.

### 4-2. 합짐 — 두 점 꺾은선

기사님이 정하는 숫자는 둘이다. **보통 시급 T**(50점) · **꿀 시급 H**(100점).

```
ΔT ≤ 0          →  100점  (길목이다. 우회가 없다)
h = 추가 운임 ÷ ΔT(추가 우회 분) × 60

h ≤ T           →  50 × h / T
h > T           →  50 + 50 × (h − T) / (H − T)      ← 100 에서 자른다
```

| 우회 시급 h | 점수 | 색 |
|---:|---:|---|
| 1.5만 | 25 | 🟡 똥 — 우회할 가치 없음 |
| 2.4만 | 40 | 🟢 보통 하한 |
| 3.0만 | 50 | 🟢 보통 — 일상적인 합짐 |
| 3.9만 | 73 | 🔵 꿀 하한 |
| 4.0만 | 75 | 🔵 꿀 — ep5 의 «가치 있는 콜» |
| 5.0만 이상 | 100 | 🔵 꿀 — ep5 의 «대박» |

### 4-3. 첫짐 — 시급 × 목적지 전진 배수

```
돈점수     = min(100, h_solo ÷ S × 100)                    S = 첫짐 기준 시급
h_solo     = 운임 ÷ (주행 + 대기·상하차 분) × 60

전진율 p   = (dist(현위치, 목적지) − dist(하차지, 목적지)) ÷ dist(현위치, 하차지)
             −1 ≤ p ≤ 1   (완전히 목적지 쪽 1 · 수직 0 · 반대 −1)

목적지배수 = clamp(1 + (Mmax − 1) × p,  Mmin,  Mmax)

첫짐점수   = min(100, 돈점수 × 목적지배수)
```

**왜 덧셈이 아니라 곱셈인가** — 덧셈(`돈 × 0.4 + 목적지 × 0.6`)이면 **요금 0원짜리가 60점(보통)이 된다.**
목적지 가치는 돈을 **키우는 배수**이지 돈과 더하는 것이 아니다. 곱셈이면 「돈이 0이면 총점도 0」이 저절로 따라온다.

**전진율의 분모가 «현위치 → 목적지»가 아닌 까닭** — 그 분모는 목적지에 가까워지면 0 으로 수렴해 터지고,
터지기 전에도 목적지 근처에서는 어떤 콜이든 전진율이 낮게 나온다. 목적지 근처가 콜이 많은 곳일 수 있다.
분모를 **이 콜이 움직이는 거리**로 두면 0 이 되지 않고 뜻도 정확해진다 — «이 콜로 움직이는 거리 중 얼마가 목적지 쪽으로 줄어드는가».

**전진율은 지리 기준의 첫 잴 값이다** (기사님 확정) — 08-29 에 «잴 값이 생기면 켠다»고 열어 둔 자리 그대로다.
첫짐에만 곱한다. 합짐의 지리는 우회 시급이 이미 센다. 하차지 주변에 콜이 얼마나 있나(후속 콜 빈도)는 **별개**라 건드리지 않는다.

**목적지 좌표의 출처** — 기사님이 그날 콜 필터에 두신 목적지(노선 타겟)다. 지역 목록(강남 +30 같은 것)은 지어내지 않는다 (규칙 ④).

**잴 수 없을 때** (규칙 ⑤-2)

| 경우 | 배수 | 딱지 |
|---|---:|---|
| 목적지를 안 정하셨다 | 1.0 | 「목적지 미설정」 |
| 목적지 이름으로 좌표를 못 구했다 | 1.0 | 「목적지 미확인」 |
| 현위치가 목적지 반경(`destinationRadiusKm`) 안이다 | 1.0 | — (도착했으니 전진할 것이 없다) |

🔴 **거리는 직선으로 잰다.** 전진율은 방향 지표이지 거리 값이 아니다. 도로로 재면 콜마다 카카오를 불러야 한다.
왕복 분리 도로에서 직선이 실제와 크게 다른 것은 알려져 있으니 **이 값을 거리로 읽지 않는다.**

---

## 5. 검산

| 경우 | 돈점수 | 배수 | 총점 | 색 |
|---|---:|---:|---:|---|
| 2026-09-19 04:54 복정동 → 대치4동 (1.2만 ÷ 65분 = 1.1만/h, 목적지 강남) | 44 | 1.95 | **86** | 🔵 꿀 |
| 같은 콜인데 목적지를 안 정했다 | 44 | 1.0 | 44 | 🟢 보통 |
| 요금 0원인데 목적지 방향 완벽 | 0 | 1.95 | **0** | 🟡 똥 |
| 시급 1.1만인데 완전히 반대 방향 | 44 | 0.5 | 22 | 🟡 똥 |
| 목적지 반경 안에서 시급 3만 | 100 | 1.0 | 100 | 🔵 꿀 |

첫 줄이 이 설계가 풀려는 것이다. 지금 시스템은 같은 콜을 **36점 똥**으로 낸다.
합짐 눈금만 쌓인 53건에 되돌려 보니 색이 아홉 건 바뀌었고 전부 엄격해지는 쪽이었다.

### 🔴 순위 검산 — 한 건씩 보지 않고 **나란히** 놓는다

한 콜이 몇 점인가만 보면 «좋은 콜이 나쁜 콜보다 높은가»를 못 본다. 그걸 안 봐서 오늘 이것이 통과했다 —

| 같은 판의 합짐 둘 | 돈 | 약속 | 공간 | 성질 | 총점 |
|---|---:|---:|---:|---:|---:|
| 1.6만/h · 여유 +3분 · 자리 20% | 27 | 46 | 20 | 100 | **48** |
| 3.9만/h · 여유 −24분 · 자리 0% | 73 | 0 | 0 | 100 | **43** |

**나쁜 콜이 이겼다.** 새 규칙을 검산할 때는 늘 **둘 이상을 나란히** 놓고 순위가 안 뒤집히는지 본다.

---

## 6. 설정값 — 규칙 ⑤-4 의 다섯

원천은 `shared/src/judgment.ts` 의 **`JUDGMENT_FIELDS`** 다. 표에 한 줄 더하면 DB 칸과 관제웹 폼이 따라온다.

| 이름 | ① 스키마 | ② 값과 근거 | ③ 시점 | ④ 화면 | ⑤ 읽는 곳 |
|---|---|---|---|---|---|
| `target_hourly_krw` | 있는 칸 — **뜻만 바뀐다** | 30,000 그대로. 「100점 기준」→ **「보통 50점의 기준선」** (기사님 확정 · 08-21 «3만 = 꿀»을 뒤집음) | 합짐을 판정할 때 | 판정 기준 탭 — 🔴 **설명 문구를 코드와 같은 커밋에서 고친다.** 안 그러면 같은 숫자를 다른 뜻으로 읽으신다 | 합짐 점수 |
| `honey_hourly_krw` | `user_judgment` 새 칸 | 50,000 — ep5 의 «꿀» 5.1만/h. **업계 기준값**, 실측이 쌓이면 바꾼다 | 합짐을 판정할 때 | 같은 탭 · 설명에 «업계 기준값» | 합짐 점수 |
| `solo_hourly_krw` | `user_judgment` 새 칸 | 25,000 — 합짐 보통(3만)보다 낮게. 빈 차는 안 잡으면 0원이라 기준이 같을 수 없다. ⚠️ 근거는 이 한 줄뿐 — 실측 전 임시값 | 첫짐을 판정할 때 | 같은 탭 · 설명에 «업계 기준값» | 첫짐 돈점수 |
| `dest_bonus_max` | `user_judgment` 새 칸 | 2.0 — 목적지로 완벽히 전진할 때의 배수 | 첫짐을 판정할 때 | 같은 탭 | 첫짐 배수 |
| `dest_bonus_min` | `user_judgment` 새 칸 | 0.5 — 완전히 반대로 갈 때의 배수 | 첫짐을 판정할 때 | 같은 탭 | 첫짐 배수 |
| ~~`weight_buffer_cost`~~ | **뺐다** — 타입·표·폼에서 | 08-29 에 약속과 합쳐 읽는 곳이 0 이던 칸 (기사님 확정) | — | 폼에 없다 | 없음. 기존 DB 의 칸은 남지만 아무도 안 읽는다 |
| 색 경계 `color_honey_min` 70 · `color_normal_min` 40 | 그대로 | — | — | 그대로 | 그대로 |

---

## 7. 옛 결정과의 관계 — 무엇을 지키고 무엇을 뒤집나

| 옛 결정 | 이 설계에서 |
|---|---|
| 08-21 절대 문턱 폐기 → 시급 비율 | **지킨다.** 눈금만 두 점으로 늘린다 |
| 08-21 목표 시급 3만 = 100점 | **뒤집는다** — 3만은 보통의 기준선 |
| 08-29 판정 함수 하나 · 첫짐/합짐 갈래 없음 | 함수 하나는 **지킨다.** 눈금은 국면으로 고른다 |
| 08-29 무조건 빨간불은 약속·성질 둘 · 만석은 가중치 | **가른다** — 신고한 적재는 무조건 빨간불, 추정 적재는 점수 |
| 08-29 지리 가중치 0 · «잴 값이 생기면 켠다» | **켠다** — 전진율이 첫 잴 값. 첫짐에만 |
| 08-29 차종 불일치는 딱지 · 못 쟀으면 `null` · 세 대답 | **지킨다** |
| 08-17 노하우 값 이식 금지 | **좁힌다** — 코드 상수 금지. DB 칸 기본값은 «업계 기준값» 표시로 |
| 08-19 첫짐에는 시간 하강 없음 · 시한은 임시 | **지킨다** |
| 국면 넷 (`first·merge·drive·home`) | **지킨다** — 「탈출」을 더하지 않는다 |
| 후속 콜 빈도는 1단계 표시만 | **지킨다** — 전진율과 별개 |

---

## 8. 아직 확인 안 된 것

| 무엇 | 왜 막혀 있나 |
|---|---|
| 2026-09-19 04:54 에 목적지를 무엇으로 두셨나 | §5 첫 줄이 「목적지 강남」을 전제한다. 다르면 86점이 안 나온다 |
| 첫짐 공식을 쌓인 53건으로 되돌린 결과 | 목적지 값이 기록에 없어 못 돌렸다. 합짐 눈금만 돌려 봤다 |
| 실제 콜에서 전진율이 얼마로 나오나 | 만들기만 했다 — 실주행이나 체험 콜로 봐야 한다. 지도 중심점이 «시내» 한 점이라 큰 시에서는 거칠다 |
| 새 적재 셈이 실제로 자리를 돌려주나 | `pnpm scenario` 가 씨앗 콜(좌표 붙은 2건)이 없어 못 돌았다. 콜을 잡은 뒤 돌려야 한다 |
| 후보 콜 자기 박스는 자리 계산에 안 든다 | `freePct` 는 **붙이기 전** 남는 자리다. 후보의 박스 수는 대개 신고 전이라 모른다 — 규칙 ⑤-2 대로 두었다 |
| 잡아 둔 콜을 더해 세는 적재 셈 | 예약은 실린 짐이 아니다. 공간 무조건 빨간불이 서려면 이 셈부터 고쳐야 한다 |

---

## 9. 만드는 순서와 검사

🔴 **단계마다 «끝의 기준»이 있다** — «무엇을 한다»만 적으면 절반만 하고 닫게 된다. 실제로 4번이 그랬다:
«성질·약속을 무조건 빨간불로 올린다»를 «색을 덮는다»로만 읽고 ✅ 를 찍었는데 «2층 점수에서 뺀다»가 남아 있었다.

| | 무엇을 | **끝의 기준** — 이것이 참인 것을 **눈으로 본 뒤** ✅ | 상태 |
|---|---|---|---|
| 1 | 돈 눈금 — 합짐 두 점 꺾은선 · 첫짐 제 기준선 | 로그에 «보통 3.0만 · 꿀 5.0만» · «첫짐 기준 2.5만» 이 찍힌다 | ✅ 완료 |
| 2 | 「지리」를 첫짐의 전진 배수로 (엔진 `role: 'multiplier'`) | 배수가 평균에 안 들고 총점에 곱해진다 (변이로 확인) | ✅ 완료 |
| 3 | 전진율을 서버가 싣는다 | **실제 콜 로그에 배수가 1.0 이 아닌 값으로 찍힌다** | ✅ 완료 (Step 2) |
| 4 | 성질·약속을 무조건 빨간불로 | 색을 덮는다 **그리고** 2층 총점에 안 든다 | ✅ 완료 (Step 1 `role: 'gate'`) |
| 5 | `weight_buffer_cost` 칸을 뺀다 | 타입·표·폼에 없고, 직접 쓰는 자리를 세는 검사가 초록 | ✅ 완료 |
| 6 | 기존 DB 의 `weight_geography` 를 켠다 | `local.db` 두 행이 1 | ✅ 완료 |
| 7 | 적재를 «함께 실리는 최대»로 | 주행 로그에 «최대 N박스 (합 M박스 — 자리가 돌아온다)» 가 찍힌다 | ✅ 완료 |
| 8 | **공간**을 무조건 빨간불로 — 신고·실측일 때만 | 추정 만석이 색을 안 덮고, **신고 적재 초과가 🔴** | ✅ 완료 (Step 4 추정 격리) |
| 9 | 2층에서 약속·공간을 어떻게 셀지 정한다 (§3 위 🔴) | **같은 판의 합짐 둘에서 좋은 콜이 더 높다** (§5 순위 검산) | ✅ 완료 (Step 3 순위 역전 검증) |
| 10 | 판정의 «도착했나»를 콜 필터 반경에서 뗀다 | 서울 안 첫짐에서 배수가 1.0 이 아니다 | ✅ 완료 (Step 2 도심 3km 분리) |

🔴 **후속 잔여 과제(Step 5.5 ~ Step 9: 픽커 제외어 연결, 약속 지연 분 전달, 역주행 차단, 국면 가르기, 소박스 정차 단축, 갇힘 지역 감점 등)는 [국면별 판단 직행 로드맵](국면별_판단_직행_로드맵.md)으로 단일화되어 진행됩니다.**

🔴 **새 값을 쓰기 전에 지금 DB 값을 본다.** 「반경 안이면 배수 1.0」을 쓸 때 그 반경이 22km 인 것을 안 봤다.
`destinationRadiusKm` 은 콜 필터가 «어디까지 볼까»에 쓰는 값이라, 판정의 «도착했나»와 **다른 질문**이다 —
읽는 곳이 둘이면 값을 갈라야 하는지 의심한다 (규칙 ⑤-4 ⑤).

| 검사 | 무엇을 무나 |
|---|---|
| `tests/rules/judgmentDb.test.ts` | 표 ↔ DB 칸 ↔ 폼이 어긋나지 않는가 |
| `tests/rules/judgmentTunable.test.ts` | 판정 기준 탭의 값이 정말 바뀌는가 |
| `tests/rules/judgment.test.ts` | 실측 콜 회귀 · 색 경계 |
| `tests/rules/judgeCriteria.test.ts` | 기준이 섞이지 않는가 · 세 대답 |
| 새 검사 | 요금 0원이 절대 보통 이상이 되지 않는가 · 전진율 분모가 0이 되지 않는가 · 추정 적재·임시 약속이 색을 덮지 않는가 |

🔴 **빨간불을 먼저 본다.** 새 검사를 지금 코드에 걸어 빨간불을 확인한 뒤에 공식을 바꾼다.
