# 목업 → 실물 이식 계획 (2026-09-05)

> 기사님: *"우리 목업 만든 거 실 프로젝트에 붙이는 거 안 해?"*
>
> **원천은 [화면규칙.md](화면규칙.md)** 다 — 규칙과 그것을 잠그는 검사가 거기 있다.
> 이 문서는 **«무엇을 어느 순서로 옮기나»** 만 적는다. 규칙을 여기 다시 쓰지 않는다.
>
> 🔴 **동생 문서가 생겼다 — [이식_계획_지도필터콜.md](이식_계획_지도필터콜.md)** (2026-09-11).
> 이 문서는 **시트·판정 자리·QR** 이고, 그쪽은 **지도 실험실(`MapMockup.tsx`)이 만든
> 지도·필터·콜 리스트**다. §5 의 스키마 대조표는 09-09 것이라 그 뒤 목업 커밋 102개를
> 모른다 — **지도·필터를 옮길 때는 동생 문서 §2 를 본다.**

## 0. 지금 어디까지 와 있나 — 실측

**실물에 아직 없는 규칙 30개.** (화면규칙 표의 «실물» 칸이 ⬜ 인 것)

```
실물 StageView 가 지금 하는 것
├ 3단 시트 껍데기          ✅  StageSheet · snap 상태
├ 자동 높이 규칙            ✅  stageRules.stageStep (S11·S12)
└ 내용물                   🔴  <PinnedRouteBody sheetOnly>  ← **옛 화면을 그대로 넣었다**

목업 SheetMockup 이 하는 것
├ 3단 시트 (기사님 재정의)   🔴 실물에 없다   S3·S4·S5·S9
├ 높이의 유일한 길           🔴 실물에 없다   S6·S7 (sheetTransition)
├ 콜 목록 (잡은 순서·색)     🔴 실물에 없다   L1~L6
├ 판정 자리 (시트 맨 아래)   🔴 실물에 없다   J1
├ 지도 버튼 두 귀퉁이        🔴 실물에 없다   M2·M3
└ QR 덮개                  🔴 **실물에 아예 없다**  Q1~Q9
```

🔴 **`NaviQr` 는 실물 어디에서도 안 불린다.** 목업만 쓴다.
   **QR 은 폰 둘을 잇는 유일한 자리**인데(개인폰으로 내비를 켠다) 실물엔 없다.

🔴 **`sheetTransition.ts` 도 목업만 쓴다.** `audit:dead` 는 초록이다 — 목업이 쓰니까.
   **감사가 «실물에 안 이어졌다»를 못 본다.**

## 1. 🔴 먼저 정할 것 — **무대를 기본으로 켜는가** (기사님)

지금 무대는 `localStorage.stagePreview` **토글 뒤에 숨어 있고 기본은 꺼짐**이다.
이식은 전부 **그 안으로** 들어간다. 그러므로:

| 안 | 뜻 | 값 |
|---|---|---|
| **가** | 토글을 **켠 채로** 이식하고, 다 되면 기본으로 켠다 | 옛 화면이 그대로 남아 **되돌릴 자리**가 있다 |
| 나 | 이식하면서 옛 화면을 **지운다** | 두 벌을 안 만든다. 대신 못 물러난다 |

⏳ **기사님 답 대기.** 「가」를 권한다 — 실주행 중에 무너지면 옛 화면으로 도망칠 수 있다.

## 2. 순서 — **아래에서 위로**

의존이 아래에서 위로 흐르므로 그 순서로 옮긴다. 각 단계는 **혼자 커밋되고 혼자 검증된다.**

### 1단계 · 시트가 스스로 서게 한다 (S3~S7)
`sheetTransition` 을 `StageView` 에 잇는다 — **높이를 바꾸는 길을 하나로**(S6).
지금은 `setSnap` 이 두 곳(드래그·자동)에서 각자 부른다.
- 잠그는 검사: `sheetTransition.test.ts`(이미 있다) + 실물 배선 검사 **새로**
- ⚠️ 이 단계가 끝나야 «있을 수 없는 화면»(열린 것 없이 100%)이 사라진다

### 2단계 · 콜 목록의 순서와 색 (L1~L6)
잡은 순서 · 정거장 번호 밀림 · `callNo` 로 칠하기.
- 🔴 **L3 이 위험하다** — 지금 실물이 `callNo - 1` 로 찾는 자리가 있는지 **먼저 센다**
- 잠그는 검사: `mockPlans.test.ts` 는 목업용이다. **실물용을 새로** 만든다

### 3단계 · 판정 자리를 시트 맨 아래로 (J1)
지금 실물은 **필터 자리를 치환**한다. 목업은 **시트 맨 아래**다.
- 🔴 **자리를 옮기면 규칙이 따라 뒤집힌다** (화면규칙 §모순 ① 참조) —
  실물은 판정 중 시트를 **내려야** 지도가 보이고, 목업은 **올려야** 판정이 보인다.
  이식하면서 S9 를 함께 옮기지 않으면 **두 규칙이 싸운다**

### 4단계 · 지도 버튼 두 귀퉁이 (M2·M3)
좌하단 방침 · 우하단 QR · 시트 **잰 높이** 위에 붙이기.
- 3단계까지 끝나야 «잰 높이»가 믿을 만하다

### 5단계 · QR 덮개 (Q1~Q9) — **실물에 아예 없는 것**
- 🔴 **이것만은 이식이 아니라 «신규»다.** `NaviQr` 부품은 있는데 부르는 곳이 없다
- 🚗 **기사님만 닫을 수 있는 것**: 카카오내비가 주행 중 경유지 순서를 유지하는가

## 3. ⚠️ 이식하면서 반드시 지킬 것

**① 목업을 지우지 않는다.** 이식이 끝나도 목업은 **다음 설계의 자리**다.
   다만 **실물이 원천이 되고 목업이 그것을 쓰게** 뒤집는다 (지금은 반대인 것이 있다).

**② 한 단계에 한 커밋.** 5단계를 한 번에 옮기면 무너졌을 때 **어디서 무너졌는지 모른다.**

**③ 각 단계마다 «실물 배선 검사»를 새로 만든다.**
   목업 검사는 목업만 지킨다 — #96·#97 의 교훈: **옳은 것을 만들어 두고 안 부르면
   화면은 그대로 틀리다.**

**④ 게이트는 전부 돌린다** (버그 대장 #98). `npx jest` 안에 관제웹 규칙 검사 77개가 산다.

## 4. 크기 — 정직하게

| 단계 | 손대는 파일 | 위험 |
|---|---|---|
| 1 시트 | `StageView` | 낮다 — 규칙은 이미 검사로 잠겨 있다 |
| 2 목록 | `PinnedRouteBody`·`CallDeck` | **높다** — 실물 목록은 탭·완료·취소가 얽혀 있다 |
| 3 판정 | `JudgmentSeat`·`OrderFilterStatus` | **높다** — 규칙이 뒤집힌다 (위 참조) |
| 4 지도 | `PinnedRouteCanvas` | 낮다 |
| 5 QR | 새로 | 중간 — 부품은 있다 |

**2·3단계가 이 이식의 무게다.** 나머지는 곁가지다.

## 5. 지도 실험실 → 실서버 — **스키마 대조** (2026-09-09)

기사님: *"목업으로 만든 이유가 여기서 로직을 만들어 실서버로 가려는 건데, 중간중간
이동을 위한 점검을 하는 것이 맞을 것 같다."*

목업(`MapMockup.tsx`)이 들고 있는 상태를 실물 스키마와 하나씩 대조했다.

### ✅ 이미 맞다

| 목업 | 실물 |
|---|---|
| `phaseSettings` 다섯 벌 | `user_filter_phases` — 목업이 `PHASE_FIELDS`·`DEFAULT_PHASE_SETTINGS` 를 **그대로 import** 한다. DB 칸 목록(`FILTER_FIELDS`)은 같은 `PhaseSettings` 에서 나오므로 다섯 칸이 일치한다 |
| 요금 하한·상한 · 차종 단가 | `user_filters.min_fare`·`max_fare`·`vehicle_rates` | 값은 **있다** — `labFilterOutput.ts` 가 `minFare: 30000`·`maxFare: 1_000_000` 을 **코드에 박아** 두고, 단가는 `rateFloorsFrom(discountPct)`(shared 폴백 시세 `NET_RATE_PER_KM`)로 만든다. 실물은 같은 함수를 **DB 요율**로 부른다 |
| `excludedWords` | `user_filters.excluded_keywords` (JSON TEXT) |
| `confirmed[].steps` | `step_call_*` · `step_arrive_*` — 약속·예상·실측·출처가 한 행에 |
| `NET_SRC`(집) | `user_settings.home_x` / `home_y` — **자리는 있다.** 목업은 상수로 박아 뒀을 뿐이다 |

### ⚠️ 맞춰야 한다

| 무엇 | 실물 | 할 일 |
|---|---|---|
| **띠 폭 ↔ 우회 허용** | `detour_allow_km` 은 «총거리 증가분» | ✅ 2026-09-09 `detourWidthKm`(경유 폭)로 갈랐다 — 옛 이름 `detourKm` 은 뜻이 겹쳐 조용히 섞였다. ⚠️ 첫 시도에서 `corridorWidthKm` 로 지었다가 **용어집이 폐기한 「회랑」** 이라 `glossary.test.ts` 가 잡았다 |
| **제외 지역** | **칸이 없다** (`excluded_keywords` 는 적요 글자다) | `user_filters.excluded_regions TEXT DEFAULT '[]'` 를 `excluded_keywords` 옆에 — 화면이 생길 때 함께 판다 (규칙 ⑤-4) |
| **마름모 각도** | **칸이 없다** | `knobs.srcAngleDeg`·`dstAngleDeg`(100°)가 판정의 뼈대인데 저장할 자리가 없다. 값이 굳으면 `user_filter_phases` 에 두 칸 |
| **요금 하한·차종 단가** | `user_filters.min_fare`·`vehicle_rates` | 목업은 **코드에 박혀** 있다(30000 · shared 폴백 시세). 기사님이 못 고치는 값이라 규칙 ⑤-4 ① 그대로다 — DB 에서 읽게 잇는다 |
| **콜의 네 칸** | `orders` | `approachKm` · `approachMin` · `destName` · `routeOption` 추가 |
| 🔴 **직선 폴백** | **실물엔 없다** | ✅ 2026-09-09 걷어냈다. 목업은 실패하면 직선 km 를 **지어냈고**, 서버 `/sim/route` 는 거기에 **`durMin: 0`** 까지 붙였다(0분 = «즉시 도착» → 시급이 통째로 틀어진다). 같은 파일의 `/chain` 은 이미 null 이라 **한 서버가 같은 질문에 두 답**을 하고 있었다. 이제 둘 다 null 이고, 목업은 실물과 같은 `routeComputedAt` 으로 «아직»과 «못 쟀다»를 가른다 |
| **톨비 타입** | `orders.tollFare` 가 **TEXT** | 숫자로 갈지는 별건 — 파싱 자리를 먼저 정한다 |
| **적재** | `step_loaded` 의 콜별 실측 | 목업은 `slotsUsed` 전역 하나 — 「공간」 축과 함께 콜별로 |
| **판정 결과** | `order_judgments` (color·score·detail·judgedAt) | 목업은 아직 색을 안 낸다. 돈·약속 축을 넣을 때 이 모양으로 |

### 🎚️ 필터 화면 — 실험실이 정한 모양과 실물의 자리 (2026-09-09)

실험실 **오른쪽 «필터 옵션» 패널을 걷어냈다** (기사님 지시: 도착 목표·상차 반경·우회 허용·
하차지 주변 넷 삭제). 실물로 들고 갈 것은 **왼쪽 한 벌**이다.

🔴 **실험실에서 지웠다고 «실물에도 없어야 한다»는 뜻이 아니다.** 실물에는 기사님이 값을
고칠 화면이 반드시 있어야 한다. 지운 것은 **실험실 안에서 같은 값을 두 번 보여주던 자리**다
(오른쪽은 국면 5탭을 위해 있던 패널이고, 국면을 걷어내며 역할이 사라졌다).

| 실험실 왼쪽 | 실물의 자리 | 상태 |
|---|---|---|
| 🎯 목적지 | `user_filters.destination_city` | 있다 |
| **출발각° · 목적각°** | `src_angle_deg` · `dst_angle_deg` | ✅ 생겼다 (C3-1·C3-2 · 2026-09-11) |
| 현위㎞ (상차 반경) | `pickup_radius_km` | 있다 |
| 목적㎞ (목적반경) | `destination_radius_km` | 있다 (이름까지 · C4-7) |
| **라인반경㎞** | `detour_radius_km` | ✅ 이름까지 (C4-7) |
| ⛔ 제외지역 | `excluded_regions` | ✅ 생겼다 (C2 · 2026-09-11) |
| 💰 콜할인율 | `call_discount_pct` | 있다 |

#### ⚠️ 「우회 허용」을 어떻게 할지는 이식 때 정한다

```
실물     우회 허용(detour_allow_km · 카카오 총거리 증가분) ──서버가 파생──▶ 경유 반경
실험실   라인 반경 — 기사님이 **파생 결과를 직접** 넣는다
```

실험실에서는 우회 허용을 **지웠다** — 손잡이가 둘이 되어 앞의 것이 아무 데도 안 쓰였다.
아웃풋의 `detourRadiusKm` 칸에는 **라인 반경**을 싣는다 (앱은 그 칸을 안 읽는 것을 확인했다).

이식 때 둘 중 하나를 고른다:
- **가** 라인 반경으로 갈아탄다 — 기사님이 직접 넣는다. 실험실이 그렇게 한다
- **나** 둘 다 둔다 — «총거리가 얼마나 늘었나»는 약속을 깨는지 볼 때 따로 쓸 수 있다

### 🔴 **실험실은 값이 한 벌, 실물은 다섯 벌** (2026-09-09 · 가장 큰 차이)

기사님 확정: *"모두 꺼내 두고 노선이면 라인값을 사용하고 동선이면 사용 안 하면 되니까."*
그리고 화면에서 그 어긋남을 잡으셨다 — *"우리 설정과 다른 값을 쓰고 있고"* ·
*"이제 우리에게 국면이라는 것이 없어진 것 같은데.. 원칙이 바뀐 거 아냐?"*

| | 실험실 (2026-09-09~) | 실물 |
|---|---|---|
| 값 그릇 | **한 벌** (`knobs`) | **한 벌** (`user_filters` 한 행 · ✅ C3-3b 2026-09-11) |
| 국면 | **없다** — 「지금 무엇을 하나」(대기·콜 쥠·주행)만 남았다 | `resolvePhaseKey(callTarget × dispatchPhase)` |
| 숨김(`hidden`) | **없다** — 안 쓰는 값은 그냥 안 읽힌다 | ✅ 감추지 않고 흐리게 — 상태에서 파생 (C3-3a) |
| 아웃풋 | 네 칸(`pickupRadiusKm`·`destinationRadiusKm`·`detourRadiusKm`·`callDiscountPct`)이 **항상 실린다** | 숨김 칸은 `undefined` 로 빠진다 |

🔴 **왜 걷어냈나** — 다섯 벌이 하던 일은 «값을 여러 벌 두는 것»이 아니라 **«지금 안 쓰는 칸을
감추는 것»**이었다. 그런데 감추면 **화면이 조용히 거짓말하고**, 국면이 바뀔 때 **다른 벌이
읽혀 값이 갈린다.** 실험실에서 실제로 그 일이 났다 — 왼쪽 화면은 15km 인데 판정은 다른 벌의
값으로 돌고 있었다.

✅ **실물이 이 모양이 됐다** (C3-3b · 2026-09-11) — `user_filters` 한 행
(기획: [노선_고르는_법 ⑰](노선_고르는_법.md)). 실험실 아웃풋과 실물 필터가 **같은 모양**이다.

### 🔴 실물에 **개념이 없는 것** — 목적지 둘

목업의 ⑮ 기준은 «목적지 하나당 마름모 하나»라 `goals = [파주, 집(복귀)]` 가 **동시에** 산다.
실물은 **한 벌 → 목적지 하나**다 (복귀는 `goalCity` 로 집 시가 **파생**된다 · 2026-09-12).

집 좌표는 `user_settings.home_x/home_y` 에 있으므로 **복귀 목적지 자체는 자리가 있다.**
없는 것은 «둘을 동시에 살려 두고 각각 판정한다»는 구조다 — `PHASE_FIELDS.home.destinationCity`
가 `auto`(저장 안 함)인 것도 그 전제다. **이식할 때 가장 크게 바뀌는 자리다.**

> ⚠️ **이 절은 2026-09-09 에 한 번 틀렸다가 고쳤다.** 처음에 «요금 하한이 목업에 없다»·
> «차종 단가가 없다»·«`straight` 는 `kakaoSoloDurationMin IS NULL` 로 파생된다»라고 적었는데
> **셋 다 사실이 아니었다.** 코드를 다시 열어 검산하고 고쳤다 — 문서만 보고 판단하면
> 없는 것을 만들고 있는 것을 못 본다 (루트 CLAUDE.md 「문서 신뢰도」).

### 🚫 이식 대상이 아니다 (실험실 전용)

`apiLog`(카카오 호출 기록) · `logs` · `view`(줌·드래그) · `driving`·`speedIdx`·모의 시계 ·
**`candFare`·`candBoxes`(지도 위 요금·짐 입력창)**.

⚠️ 길 찾기 4옵션(`liveRoads`)은 2026-09-09 에 **걷어냈다** — 이제 잡은 콜이 라인을 만든다.

🔴 **요금·짐 입력은 심사창에 두지 않는다** (기사님 2026-09-09: *"심사창은 나중에 프로젝트에
넣을 수도 있을 것 같아서 거기에 불필요한 것은 없으면 좋겠는데"*). 실제 콜은 **배차망이 그 값을
주므로** 실물 심사창에는 입력이 없다. 실험실은 지도를 두 번 눌러 콜을 만들어서 값이 없을 뿐이라,
**지도 위에 뜨는 창**으로 옮겼다 — 심사창은 실물에 그대로 갈 수 있는 모양으로 남는다.


## 6. ⚖️ 심사 목록을 실물 심사석으로 (2026-09-09 · 기사님 제안)

기사님: *"**심사 목록의 내용이 너무 좋은데** 이걸 진짜 프로젝트의 심사 영역으로 만들면
좋을 것 같은데.. 네 생각은 어때?"*

### 지금 실물은 무엇을 보여주나

`JudgmentSeat` 은 **세 줄**이다 — 시급 `2.4만/h` · 경로 `11.5km · 15분 (상차 10분)` ·
걸리는 것. 「근거 ▾」를 펼치면 `judgment.gates`·`axes` 를 **나열**한다.

### 🔴 겉으로 올리지 않는다 — 접힌 자리로 들어간다

심사석의 본업은 **1~2초**다 (규칙 ⑤-3: 색이 곧 결정). 실험실 심사 목록은 **읽어야 아는 것**
(표·분·시각)이라 겉에 올리면 안전취소 30초를 잡아먹는다.
→ **겉 세 줄은 그대로 두고, 「근거 ▾」 안을 실험실 ①②③ 로 갈아끼운다.**

### 재료가 실물에 있나 — 세어 봤다

| 실험실 | 실물 | 무게 |
|---|---|---|
| **① 후보콜 보유정보** | `fare`·`distanceKm`·`kakaoTimeExt`·상차지 접근 **있다** | 낮다 — 배선만 |
| ↳ 3거리(상차·하차·나 → 목적지) | **없다** | 필터 2단계 값이라 판정에 안 싣는다. 실을지부터 정한다 |
| **② 판정 다섯 기준 표** | `judgment.axes`(점수·가중치·raw) · `gates`(why) **있다** | **낮다 — 화면이 덜 쓸 뿐이다** |
| **③ 후보콜이 미치는 영향** | 🔴 **없다** (`impactOfStop`·`splitDropImpact`·`promiseTimes` 는 실험실 전용) | **높다 — 이 이식의 무게** |

🔴 **③ 은 새 데이터가 아니라 «안 이어진 배선»이다.** 약속 시각(`promisedArrivalAt`)은
`shared` 에 있고 콜 목록·핀 카드가 이미 읽는다 — **심사석만 안 본다.**
서버가 후보콜을 낀 전체 경로를 재는 것도 이미 한다(심사에 쓰는 값). 둘을 이으면 나온다.

### 순서 (한 단계에 한 커밋 · §3 ②)

1. **② 기준 표** — 펼침 안을 실험실 모양(기준·점수·왜)으로. **재료가 다 있다.** 위험 낮음
2. **① 보유정보** — 배선. 3거리는 **뺀 채로** 시작한다 (필터 값을 판정에 섞지 않는다)
3. **③ 영향** — `promiseTimes`·`impactOfStop` 를 서버로 올리고 심사석이 읽는다.
   🔴 규칙 ⑤-4 다섯을 **먼저** 채운다 — 특히 ①스키마(`order_judgments` 를 늘릴지 새 표일지)와
   ⑤읽는 곳(심사석 · 핀 카드 · 콜 목록이 **각자 다른 질문**을 답하고 있지 않은지)

⚠️ **3단계(J1 · 판정 자리 옮기기)와는 다른 일이다.** J1 은 «어디에 놓나», 이것은 «무엇을 담나».
J1 을 먼저 하면 담을 그릇이 흔들리는 자리에 내용을 부어야 한다 — **이것을 먼저 해도 된다.**
