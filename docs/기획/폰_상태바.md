# 폰 상태 바 — 관제웹이 폰에 대해 보여 주는 모든 것

> **2026-09-02 기사님과 함께 정리.** 코드에서 요소를 전수로 꺼내 컬럼을 붙였다.
> ⬜ 표시는 **아직 화면에 없다** — 만들 것이지 있는 것이 아니다.
> 🔴 2026-09-05 에 이 표를 코드와 전수 대조해 **다섯 곳을 고쳤다** (§1 아래 표) —
>    그중 하나(3 조용한가)는 **✅ 로 적혀 있었는데 함수도 검사도 화면도 없었다.**
>    «계획을 완료로 적는» 사고의 다섯 번째다. 이 문서를 근거로 쓰기 전에 그것을 기억할 것.
> 화면 파일은 `client-app/src/components/dashboard/DeviceControlPanel.tsx` 다.

기사님: *"폰 상태 바인데 여기서 설정을 하니까 문제인 것 같고,
통신 방법이 수동인데 즉시 반영을 원하는 것이 문제인 듯한데."*

이 한 문장이 이 문서의 출발점이다. 상태 바에는 **폰이 말해 준 사실**과
**우리가 정한 목표**가 섞여 있었고, 둘을 같은 모양으로 그려서 목표가 사실처럼 보였다.

---

## 1. 표

| # | 요소 | 표시 | 상태 변경 | 앱 | 서버 | 관제 | 반영 시간 | 포함 |
|---|---|---|---|---|---|---|---|---|
| 1 | **폰 이름** | `1234` | — 고정 | ✗ | **✓** | ✗ | 1회 | **최상위** |
| 2 | **살아있나** | 이름 **깜빡임** + `13:19` | 정상 ↔ 🔴깜빡 | ✓ | ✗ | ✗ | 즉시 | 1 안 |
| 3 | **조용한가** ⬜ | `⏱️` — **조용할 때만** | 조용해지면 뜨고 말하면 사라진다 | ✓ | **✓** | ✗ | 즉시 | 2 안 |
| 4 | **누적** | `수집1234 수락5 취소2` | 늘기만 | ✓ | ✗ | ✗ | 즉시 | 2 안 |
| 5 | **앱 버전** | `2.9.1-hello (49)` | 설치할 때 | **✓** | ✗ | ✗ | 즉시 | 1 안 |
| 6 | **화면 켜짐** ✅ | ↓ 9와 한 배지 | 켜짐 ↔ 💤꺼짐 | ✓ | ✗ | ✗ | 즉시 | 2 안 |
| 7 | **접근성** | 이름 **색** 🟢/⚪ | 읽힘 ↔ 안읽힘 | ✓ | ✗ | ✗ | 즉시 +15초 | 6 안 |
| 8 | **배차망** | `픽커` | 인성↔24시↔픽커 | ✓ | ✗ | ✗ | 즉시 | 7 안 |
| 9 | **화면명** | `[픽커 홈]`<br>꺼지면 `[💤 화면 꺼짐]` | 홈↔리스트↔상세↔운행5↔모름 | ✓ | ✗ | ✗ | **앱이 알아챌 때** | 8 안 |
| 10 | **앱 작업 단계** ⬜ | `대기` · `상세` · `팝업 2/3`<br>`판결 대기` · `안전취소 12초` | **다섯 칸** | **✓** | ✗ | ✗ | 즉시 | 9 안 |
| 11 | **성적표** | `본 42 · 통과 3 요금12` | **여섯 축 중 최다** ↔ 안그림 | ✓ | **✓** | ✗ | 즉시 | 9 안 |
| 12 | **모드** | `[자동]` **하나만** — 누르면 그 자리에 셋 | 셋 + **적용중 ↔ 활성** | ✗ | ✗ | **✓** | **왕복** | 별개 축 |
| 13 | **필터 배지** | `첫짐`·`합짐`·`경로`·`하차`<br>·`직접 모드` | 국면·모드에서 파생 | ✗ | **✓** | ✗ | 즉시 | 12 안 |

> ⚠️ **필터(무엇을 거르나)는 이 표에서 뺐다** (기사님 지시). 12번과 같은 축(«관제가
> 바꾼다»)이므로 **필터 판에서 이 컬럼을 그대로 쓰면** 딤드 설계가 한 번에 된다.
> 🔴 다만 **13번 «필터 배지»는 다른 것이다** — 필터가 지금 **무엇을 찾고 있나**를
> 화면이 읽어 주는 것이고, 관제가 바꾸는 값이 아니라 **국면·모드에서 파생**된다.
> 2026-09-05 까지 이 표에 없어서 «지금 화면의 「합짐」이 뭐지?» 를 물어야 알 수 있었다.

### 🔴 2026-09-05 에 고친 다섯 (코드와 대조)

| # | 무엇이 틀렸나 |
|---|---|
| 3 | **✅ 로 적혀 있었다** — 판정 함수도 검사도 화면도 없다. `⬜` 로 고쳤다 (todo.md 에 남은 셋) |
| 5 | **「앱 ✗」** 였다 — 원천은 **앱**이다(앱이 자기 버전을 싣는다). 「🆕」도 뗐다 — **화면은 이미 그린다** |
| 10 | **「앱 ✗」·「6단계」** 였다 — 원천은 앱이고 값은 **다섯**이다 (`workStageLabel`) |
| 11 | **「8종 중 최다」** 였다 — 축은 **여섯**이다 (`AXES`: 도착지·차종·요금·상차지·경로순서·블랙) |
| 13 | **줄이 통째로 없었다** — 실물이 그리는데 문서가 안 적었다 |

### 컬럼이 묻는 것

| 컬럼 | 묻는 것 |
|---|---|
| **앱** | 이 사실의 **원천이 앱인가** — 폰이 켜지고 망에 붙고 화면을 읽는 것은 전부 앱발이다. 서버가 가공해도 원천은 앱이다 |
| **서버** | 서버가 **자기만 아는 것**을 보탰나 — 등록해 둔 이름, 서버 시계. 앱은 모르는 값이다 |
| **관제** | 관제웹이 **바꾸는가** |

🔴 **«서버가 판정한다»와 «서버가 원천이다»는 다르다.** 서버가 `OFFLINE` 을 찍어도 그 원천은
*"앱이 말을 안 했다"* 이므로 앱발이다. 이 구분을 흐리면 **어디를 고쳐야 하는지**가 흐려진다.

---

## 2. 포함 관계 — 한 줄기

```
1 폰 이름
├ 5 앱 버전
└ 2 살아있나 ────── 3 조용한가 ⬜ · 4 누적
  └ 6 화면 켜짐 💤
    └ 7 접근성 (이름 색)
      └ 8 배차망
        └ 9 화면명          ← 6과 한 배지로 표시한다
          ├ 10 앱 작업 단계 ⬜
          └ 11 성적표

12 모드   ← 딴 줄기. 유일하게 «우리가 정한 것»
└ 13 필터 배지          ← 모드·국면에서 **파생**된다. «지금 무엇을 찾나»
                          모드가 «직접» 이면 필터가 안 돌아 «직접 모드» 하나만 뜬다
```

**위가 꺼지면 아래는 뜻이 없다.** 접근성이 ⚪면 8~11 은 옛 값이고, 화면이 💤면 9~11 이 없다.

⬜ 는 **아직 화면에 없는 것**이다 — 3 은 판정 함수가 없고, 10 은 앱이 값을 안 보낸다.

### 🔴 6번이 7번 **위**에 있어야 하는 이유 (2026-08-22 기사님 확정)

화면이 꺼지면 노드가 0인 것이 **당연하다.** 그걸 «접근성 고장»이라 하면 폰을 끌 때마다
거짓 경고가 뜬다. 그래서 서버는 화면이 꺼져 있으면 접근성 판정을 **건너뛴다**
(`devices.ts` — `if (session.isScreenOn === false) { session.blindSince = undefined; return; }`).

그래서 «안 읽힘»의 이유가 **둘로 갈려 있다** — 💤(폰을 켜세요)와 ⚪(접근성을 켜세요).
할 일이 다르므로 갈라 두는 것이 맞다.

### 표시는 합치되 값은 가른다

6+9 를 배지 하나로 그리고(`[💤 화면 꺼짐]`), 8+11 도 «신선할 때만» 한 덩이로 그린다.
🔴 **표시를 합치는 것이지 값을 합치는 것이 아니다** — 값을 합치면 위의 판정이 무너진다.

### ✅ 1단계로 만든 것 (2026-09-02 · 관제웹·서버만 · 앱은 한 줄도 안 고쳤다)

**6+9 를 배지 하나로** — 고르는 일은 `shared/screenLabels.ts` 의 `deviceScreenBadge` 하나가 한다.
화면이 꺼지면 배지가 통째로 `💤 화면 꺼짐` 이 되고 **배차망·화면명은 함께 나가지 않는다** —
그 값은 화면이 꺼지기 **전에** 읽은 것이라, 함께 그리면 «읽지도 않고 단언하는» 것이 된다.
⚠️ **끊김이 화면 꺼짐보다 위다.** 말이 없는 폰에 «화면이 꺼졌다»고 적는 것도 단언이므로,
`OFFLINE` 이면 예전처럼 그린다 (끊김은 폰 이름의 빨간 깜빡임이 말한다).

**3번 «조용한가»** — 규칙 ⑤-4 의 다섯을 먼저 확정하고 만들었다.

| | 확정한 것 |
|---|---|
| ① 스키마 | `DeviceSession.prevSeen`(직전 보고 시각) — **메모리 세션만**, DB 에 안 적는다 |
| ② 값 | **30초** (기사님 확정 2026-09-02). 지어낸 값이 아니라 **생존신고 60초의 절반**이다 — 앱은 일이 생기면 그때 보내고(수초) 아무 일도 없으면 60초마다 보내므로, 간격이 그 중간값으로 나오는 일이 거의 없다 |
| ③ 시점 | `touchDeviceSession` 에서 `lastSeen` 을 덮기 **직전** 한 곳 |
| ④ 화면 | 폰 이름 바로 옆 `👀`/`⏱️` (표의 «2 안»). **화면이 꺼져 있어도 그린다** — 최대 60초를 기다려야 하는 것이 바로 그때다 |
| ⑤ 읽는 곳 | **관제웹의 이 배지 하나뿐** (`deviceQuietIcon`). 서버 판정도 앱도 안 읽는다 — «콜을 줄까»를 이걸로 정하기 시작하면 한 값이 두 질문을 답한다 |

**🖥️ 화면이 바뀐 보고는 서버 로그에 한 줄 남는다** (기사님 지시 2026-09-02:
*"페이지 바뀌거나 할 때 post 로 서버로 값을 보내는 거지? 그때 콘솔을 꼭 넣어서 트래킹 할 수 있게 해 줘."*)

```
🖥️ [화면 바뀜] 1234 · 픽커 홈 → 콜 리스트 · 직전 보고와 3.2초 만
```

간격이 함께 찍히므로 *"앱이 알아채는 데 얼마나 걸렸나"* 가 그 줄에서 바로 읽힌다 —
2026-09-02 에 정지 화면이 18.3초 걸린다는 것도 이 숫자로 알았다.
🔴 **판정(«조용/움직임»)은 로그에 안 적는다.** 사실(초)만 적는다 — 판정하는 곳은 하나여야 한다.

### ✅ 0단계 — 다섯을 정했다 (2026-09-02 · 규칙 ⑤-4)

**① 작업 단계는 다섯 칸** (기사님 확정). 앱 안엔 상태가 여섯인데, 관제에서 뜻이 있는 것만 추렸다.

| 칸 | 언제 | 화면 |
|---|---|---|
| 대기 | 아무 콜도 안 잡고 있다 | `대기` |
| 상세 보는 중 | 콜을 눌러 상세에 들어갔다 | `상세` |
| 팝업 | 상·하차지·적요를 걷는 중 | `팝업 2/3` |
| 판결 대기 | 서버 판결을 기다린다 | `판결 대기` |
| 안전취소 | 위약금 없이 무를 수 있는 30초 | `안전취소 12초` |

🔴 **«왜 멈췄나»가 그 한 칸에서 보여야 한다** — 간단히 세 칸으로 줄이면 «어느 팝업에서
막혔나»를 못 본다. 기사님은 운전 중 1~2초에 읽으시므로 **글자는 짧게, 숫자는 크게.**

**② 모드 «대답»은 값 그대로** — 필터처럼 «버전»을 쓰지 않는다. 필터가 버전을 쓰는 것은
본문이 커서 아끼려는 것인데, 모드는 글자 하나라 아낄 것이 없다. 앱이 매 보고에
«나 지금 ALARM이다»를 싣고, 서버가 **«내가 보낸 것»과 대조**하면 「적용중」이
저절로 파생된다 (규칙 ③ — 상태를 저장하지 말고 데이터에서 파생시킨다).

**③ 「적용중」이 안 풀릴 때는 10초 뒤 버튼만 다시 눌리게 한다** (기사님 «10초쯤»).
🔴 **글자는 «반영 안 됨»이라 쓰지 않는다** — 홈에 있는 폰은 실제로 나중에 반영되므로
그건 거짓말이 된다. `적용중 · 마지막 통신 42초 전` 처럼 **사실만** 적는다.

**④ 화면**은 위 표 그대로(10번 칸 · 9번 안). **⑤ 읽는 곳**은 관제웹의 그 칸 **하나뿐**이다 —
서버 판정은 계속 **목표값**으로 돈다. «적용 안 된 폰은 콜을 안 준다» 같은 것이 붙으면
한 값이 두 질문을 답하게 된다.

---

## 3. 방향은 둘뿐이다

기사님: *"post 방식의 통신을 하고 있어서 요청하지 않으면 변하지 않는 것도 알고 있는 거지?"*

### 앱 → 관제 (1~11) — **즉시 만들 수 있다**

```
앱이 알아챔 → POST → 서버가 받자마자 → 소켓 푸시(1초) → 관제웹
```

앱이 **변화를 알아채는 순간 쏘면** 된다. 오늘 홈에서 늦은 것은 *전달이 느려서가 아니라
앱이 못 알아채서*였다 — 그래서 9번만 반영 시간을 «앱이 알아챌 때»라고 적었다.

> ⚠️ **왜 못 알아채나**: 안드로이드 접근성은 화면을 캐시하고 그 캐시는 **이벤트가 와야** 버린다.
> 이벤트 없이 다시 읽으면 아까 그 화면을 준다. 2026-09-02 에 «몇 박자 뒤 다시 읽기»를
> 넣었다가 **18.3초 → 18.3초**, 전혀 안 나아져서 되돌렸다. 자세한 것은 `todo.md` 최상단.

### 관제 → 앱 → 관제 (12) — **왕복이라 즉시가 불가능하다**

```
①클릭 → ②서버가 들고 있음 → ③앱이 가지러 옴 → ④앱이 «받았다» 대답 → ⑤관제에 표시
        └────────── 이 사이가 «적용중» ──────────┘        └ 여기서 활성
```

②~④ 동안 서버는 *«보냈다»* 만 알고 *«적용됐다»* 는 모른다.
**그래서 버튼이 그 사이를 «적용중»으로 보여야 한다.**

### ✅ 어떻게 보이나 (기사님 안 확정 2026-09-05)

```
평소     [자동]                  ← **고른 것 하나만** (셋을 늘어놓으면 14칸, 하나면 4칸)
누르면   [자동][알람][직접]        ← **버튼을 덮고** 열린다 (아래가 아니다)
고르면   [자̶동̶] ⟳                ← **이전 값**이 딤드되고 그 위에 로딩
받으면   [알람]                   ← 앱이 가져가면 그때 새 값
```

🔴 **덮고 열리는 까닭** — 아래로 열면 «다른 것이 하나 더 생긴» 것으로 읽힌다.
   덮고 열려야 **«이 자리에서 고르는 것»** 이 되고, 고르고 나면 그 자리에 결과가 남는다.
   세로 가운데를 맞춰 위아래로 고르게 넘친다 — 한쪽으로만 자라면 누른 자리가 밀려 보인다.
🔴 **같은 것을 고르면 조용히 닫는다** — «이미 그것입니다» 같은 당연한 말을 안 적는다.
🔴 **딤드되는 것이 «이전 값»인 까닭** — 관제가 바꾼 것은 **아직 참이 아니다.**
   앱이 가져가기 전까지 폰은 여전히 옛 모드로 돈다. 새 값을 미리 그리면
   화면이 «벌써 됐다»고 거짓말한다.
🔴 **10초가 지나면 버튼이 다시 눌린다** — 로딩은 계속 돌되 **손이 묶이지 않는다.**
   앱이 영영 안 받을 수도 있다.
🔴 **「곧 됩니다」를 안 적는다** — 언제 될지 우리가 모른다. 적을 수 있는 것은
   «적용중»과 «마지막 통신 N초 전» 같은 **사실**뿐이다.

지금은 ①에서 곧장 바뀐 것처럼 그린다 — 코드에도 그렇게 적혀 있다:
`// 즉각적인 UI 피드백을 위해 로컬 상태 선갱신 (낙관적 업데이트)` (`useDevices.ts`).
**폰이 받았는지 모르는 채로 «바뀌었다»고 그리는 것**이라, 2026-09-02 오전에 고친
«읽지도 않고 화면을 단언하던 것»과 같은 병이다.

🔴 **④가 지금 없다.** 앱이 «내가 이 모드다»를 대답하지 않는다.
필터에는 그 대답(`filterVersion`)이 있는데 모드에만 없는 **비대칭**이다.

---

## 4. 아직 없는 것 셋 — 전부 «앱 안에는 있는데 안 보낸다»

| | 앱 안의 값 | 지금 서버가 아는 것 |
|---|---|---|
| **5 앱 버전** | `AppInfo.versionLabel()` | `DeviceSession.version` 칸은 **있는데 아무도 안 채운다** |
| **10 작업 단계** | `isAutoActive` · `collectState`(팝업 3장) · `isDetailScrapSent` · `isWaitingForDecision` · 30초 타이머 · `isPreview` | `isHolding` **불리언 하나**로 눌려서 올라온다 |
| **12 모드 대답** | 앱이 받아서 적용은 한다 | **대답이 없다** |

🔴 셋 다 **앱을 고쳐야** 하므로 **한 번의 재배포에 모아서** 넣는다.

---

## 5. 이 표를 다시 쓸 때

- 요소가 늘면 **포함 관계 어디에 들어가는지**를 먼저 정한다. 위가 꺼지면 뜻이 없는가?
- «관제 ✓» 가 붙는 요소는 **반드시 왕복**이고, 따라서 **«적용중» 상태를 가진다**
- «앱 ✗» 인데 앱 안에 값이 있으면, 그것은 **구멍**이지 설계가 아니다

---

# 4. 값과 폭 — 전수 측정 (2026-09-05)

> 기사님: *"순서별로 요소를 찾고 리스트업하고, 그중에 나올 수 있는 텍스트의 길이를
> 측정해줘."*

**한글 2칸 · 그 밖 1칸**으로 셌다 (`sheetStatus.textWidth` 와 같은 셈).
폰 한 줄은 **약 56칸** (400px · 13px).

## 4-1. 나올 수 있는 값 — **전부**

**한 줄이 한 값이다.** 「가장 긴 것」만 적으면 짧은 값이 어떻게 보이는지를 못 본다.
폭은 **화면에 실제로 나가는 글자만** 셌다 — 설명은 안 센다.

🔴 **「자리」는 화면에 놓인 왼쪽→오른쪽 차례다** (기사님 물음 2026-09-05: *"지금 폰의
   순서대로 정리된 건가?"* — 처음엔 §1 의 **번호순**으로 적어 두어 화면과 달랐다).
   §1 의 번호(①②③…)는 «포함 관계»를 매긴 것이지 놓인 자리가 아니다.

| 자리 | 순서 | 요소 | 글 | 폭 | 노출 시점 |
|:--:|:--:|---|---|--:|---|
| 1 | ① | 폰 이름 | `A24` | 3칸 | 언제나 — 서버가 등록한 이름 (기사님이 정한다) |
| 1 | ① | 폰 이름 | `1234` | 4칸 | 〃 — 숫자만 쓰면 4칸 |
| 1 | ②⑦ | 폰 이름의 상태 | `` | 0칸 | 끊기면 **붉게 깜빡**, 접근성이 읽히면 🟢 / 안 읽히면 ⚪ — **색으로만** 말한다 |
| 2 | ⑤ | 앱 버전 | `2.9.1-hello (49)` | 16칸 | 앱이 값을 보낼 때 — 끊기면 안 그린다 |
| 3 | ⑧⑨ | 배지 — 배차망 | `인성 · 24시 · 픽커` | 18칸 | 화면이 켜져 있을 때만 (각 4칸) |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `콜리스트` | 8칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `적요팝업` | 8칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `완료리스트` | 10칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `상세페이지` | 10칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `확정페이지` | 10칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `출발지팝업` | 10칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `도착지팝업` | 10칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `취소불가 팝업` | 13칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (인성·24시) | `알수 없는 화면` | 14칸 | 화면이 켜져 있을 때만 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `홈` | 2칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `콜리스트` | 8칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `배송완료` | 8칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `완료리스트` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `상세페이지` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `확정페이지` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `이미배정됨` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `픽업지이동` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `배송지이동` | 10칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `픽업완료 대기` | 13칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `배송완료 대기` | 13칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑨ | 배지 — 화면명 (픽커) | `알수 없는 화면` | 14칸 | 화면이 켜져 있을 때만 · 수락 뒤 운행 화면은 픽커에만 있다 |
| 3 | ⑥ | 배지 — 화면 꺼짐 | `💤 화면 꺼짐` | 12칸 | 화면이 꺼지면 **이것 하나뿐** — 배차망·화면명이 함께 사라진다 |
| 3 | ② | 배지 — 끊김 | `접근성 꺼짐` | 11칸 | 끊겼을 때 **화면 이름 대신** 왜 끊겼는지. 못 들었으면 「연결 끊김」 |
| 3 | ② | 배지 — 끊김 | `앱 꺼짐` | 7칸 | 끊겼을 때 **화면 이름 대신** 왜 끊겼는지. 못 들었으면 「연결 끊김」 |
| 3 | ② | 배지 — 끊김 | `연결 끊김` | 9칸 | 끊겼을 때 **화면 이름 대신** 왜 끊겼는지. 못 들었으면 「연결 끊김」 |
| 4 | ⑬ | 필터 배지 | `첫짐` | 4칸 | 자동·알람 · 국면 STANDBY — 첫 콜을 찾는다 |
| 4 | ⑬ | 필터 배지 | `합짐` | 4칸 | 자동·알람 · 국면 GATHERING — 가는 길의 콜을 찾는다 |
| 4 | ⑬ | 필터 배지 | `경로` | 4칸 | 자동·알람 · 국면 DELIVERING |
| 4 | ⑬ | 필터 배지 | `하차` | 4칸 | 기사님이 하차 중일 때 |
| 4 | ⑬ | 필터 배지 | `직접 모드` | 9칸 | 모드가 «직접» 이면 필터가 안 돈다 |
| 5 | ⑩ | 앱 작업 단계 | `대기` | 4칸 | ⬜ **앱이 아직 안 보낸다** — 화면은 그릴 준비가 됐다 |
| 5 | ⑩ | 앱 작업 단계 | `상세` | 4칸 | ⬜ **앱이 아직 안 보낸다** — 화면은 그릴 준비가 됐다 |
| 5 | ⑩ | 앱 작업 단계 | `팝업 2/3` | 8칸 | ⬜ **앱이 아직 안 보낸다** — 화면은 그릴 준비가 됐다 |
| 5 | ⑩ | 앱 작업 단계 | `판결 대기` | 9칸 | ⬜ **앱이 아직 안 보낸다** — 화면은 그릴 준비가 됐다 |
| 5 | ⑩ | 앱 작업 단계 | `안전취소 12초` | 13칸 | ⬜ **앱이 아직 안 보낸다** — 화면은 그릴 준비가 됐다 |
| 6 | ⑦ | 눈 가림 | `👁️` | 4칸 | 화면은 켜져 있는데 **접근성이 막혀 못 읽을 때만** |
| 7 | ② | 마지막 보고 | `13:19` | 5칸 | 언제나 — 누적 앞에 붙는다 |
| 8 | ③ | 조용한가 | `⏱️` | 4칸 | ⬜ **아직 화면에 없다** — 판정 함수(`isDeviceQuiet`)가 없다 |
| 9 | ④ | 누적 | `수집1234 수락5 취소2` | 20칸 | 언제나 · 늘기만 한다 (자릿수에 따라 폭이 변한다) |
| 9 | ④ | 누적 | `수집12 수락1 취소0` | 18칸 | 언제나 · 늘기만 한다 (자릿수에 따라 폭이 변한다) |
| 10 | ⑪ | 성적표 | `본 42 · 통과 3 요금12` | 21칸 | **마지막 보고에 함께 온 것만** · 걸린 축은 **최다 하나만** |
| 10 | ⑪ | 성적표 | `본 1 · 통과 0 차종1` | 19칸 | **마지막 보고에 함께 온 것만** · 걸린 축은 **최다 하나만** |
| 11 | ⑫ | 모드 | `자동` | 4칸 | 언제나 · 🔴 관제가 바꾸는 유일한 것 (고른 것 하나만 · §12) |
| 11 | ⑫ | 모드 | `알람` | 4칸 | 언제나 · 🔴 관제가 바꾸는 유일한 것 (고른 것 하나만 · §12) |
| 11 | ⑫ | 모드 | `직접` | 4칸 | 언제나 · 🔴 관제가 바꾸는 유일한 것 (고른 것 하나만 · §12) |

🔴 **⑬ 필터 배지가 §1 표에 통째로 빠져 있었다** (2026-09-05 발견) — 실물
   `DeviceControlPanel` 이 그리고 있는데 이 문서가 안 적었다. 기사님이 «지금 화면에서
   대기는 뭐지?» 라고 물으셔서 찾았다. **12요소만 옮겨 적고 실물을 안 본 탓이다.**

🔴 **⑤ 앱 버전을 「🆕 아직 없다」로 적어 둔 것도 틀렸다** — 화면은 이미 그린다
   (`device.version`). 앱이 값을 보내면 바로 뜬다. §1 표의 「앱 ✗」도 그래서 어긋난다.

🔴 **③ 「조용한가」가 아직 화면에 없다** (2026-09-05 발견). todo 1단계에 «✅ 완료»로
   적혀 있으나 **절반만 됐다** — 서버가 `prevSeen` 을 챙기는 것까지는 맞고,
   판정 함수(`isDeviceQuiet`)도 검사 10건도 화면도 **없다.** 주석만 그 이름을 가리킨다.
   이 레포가 네 번 당한 «계획을 완료로 적는» 사고의 **다섯 번째**다.

⚠️ **⑩ 은 앱이 아직 안 보낸다** (폰 상태 바 정비 2단계 · todo.md). 값은 정해 뒀다.
⚠️ **④ 누적과 ⑪ 성적표는 폭이 값에 따라 변한다** — 자릿수·축 이름이 달라지기 때문이다.
   위의 것은 **그럴듯한 최악**이지 상한이 아니다.


## 4-4. 🔎 엄선 — 무엇이 자리값을 하나 (2026-09-05)

> 기사님: *"필요 요소만 네가 뽑아줘. 공간을 최소화해야 하니까 엄선해서."*

**«관제»가 답해야 하는 물음은 셋이다** — 그 셋에 답하는 것만 늘 보이고, 나머지는 접는다.

| 물음 | 답하는 요소 |
|---|---|
| **① 폰이 살아 있나** | ② 시각 · ③ 조용한가 · ⑦ 접근성(색) |
| **② 지금 어디 있나** | ⑥⑨ 배지 (배차망 + 화면명) |
| **③ 왜 하나도 안 잡나** | ⑪ 성적표 |

여기에 **손이 닿는 것** 하나가 더 붙는다 — ⑫ 모드(관제가 바꾸는 유일한 것).

### 늘 보이는 여섯

| 요소 | 보통 폭 | 왜 남기나 |
|---|--:|---|
| ① 폰 이름 | 3~4칸 | 스캔폰이 **배차망마다 늘어난다** — 어느 폰 이야기인지 |
| ② 살아있나 `13:19` | 5칸 | 🔴 죽었는지는 이것 하나로 안다 |
| ③ 조용한가 `⏱️` | **0~4칸** | 🔴 **조용할 때만 그린다** (기사님 확정 2026-09-05) — 평소엔 0칸이다. 잘 도는 폰에 `👀` 를 붙일 이유가 없다. **화면이 꺼져 있어도 그린다** — 최대 60초를 기다리는 것이 그때다 |
| ⑦ 접근성 (색) | **0칸** | 🔴 **공짜다** — 폰 이름의 색으로만 말한다 |
| ⑥⑨ 배지 | 8~13칸 | 🔴 지금 어디 있나. 끊기면 **왜 끊겼는지**가 이 자리에 온다 |
| ⑫ 모드 | 4칸 | 🔴 관제가 바꾸는 유일한 것 (고른 것 하나만 · §12) |

**합 20~30칸** — 여백을 더해도 **56칸 안에 넉넉히 든다.**
(`👀` 를 안 그리니 평소는 20칸대다 — 이상할 때만 자리를 쓴다.)

### 조건부 하나 — ⑪ 성적표는 **안 잡힐 때만**

| | |
|---|---|
| 폭 | 19~21칸 (`본 42 · 통과 3 요금12`) |
| 언제 | 🔴 **통과가 0일 때만.** 잘 잡히면 볼 이유가 없다 |

*"«도착지에서 5개» 면 경유 반경을 넓힐 때고, «요금에서만» 이면 콜할인율을 만질 때"*
— 이 물음은 **안 잡힐 때만** 생긴다. 그때만 자리를 내주면 평소 21칸을 아낀다.
⚠️ 실물은 이미 «걸린 축 **최다 하나만**» 적는다 — 더 줄일 곳이 없다.

### 접는 넷

| 요소 | 폭 | 왜 접나 |
|---|--:|---|
| ④ 누적 | 20칸 | «일을 하고는 있나»는 ③ 조용한가가 **더 정확하고 2칸**이다 |
| ⑤ 앱 버전 | 16칸 | **배포 때만** 본다. 평소에 16칸을 쓸 일이 아니다 |
| ⑩ 작업 단계 | 4~13칸 | 🟡 아깝다 — 다만 ⑥⑨ 배지가 «어디 있나»를 이미 말한다. **⑩은 앱이 아직 안 보내니** 그때 다시 본다 |
| ⑬ 필터 배지 | 4~9칸 | 🟡 «무엇을 찾나». 모드(⑫)가 «찾긴 하나»를 이미 말한다 |

**접힌 넷의 합 44~58칸** — 이것이 한 줄을 116칸으로 만들던 몫이다.

### 🔴 그래서

```
평소      A24  [인성 콜리스트]  13:19                    [자동]     ≈ 26칸
조용하면   A24  [인성 콜리스트]  13:19 ⏱️                  [자동]     ≈ 30칸
안 잡히면  A24  [인성 콜리스트]  13:19  본42·통과0 요금12   [자동]     ≈ 47칸
끊기면    A24  [접근성 꺼짐]    13:19 ⏱️                  [자동]     ≈ 26칸
```

⚠️ **가장 긴 조합은 「알 수 없는 화면」(19칸) + 성적표(21칸)** 이라 57칸이 된다.
   한 칸 넘친다 — 다만 «알 수 없는 화면인데 성적표가 온다»는 **동시에 참이기 어렵다**
   (화면을 모르면 스캔도 못 한다). 실주행에서 보고 넘치면 성적표를 줄인다.

## 4-2. 🔴 한 줄에 다 넣으면 **두 배로 넘친다**

최악의 값으로 세면:

```
①폰이름 3 · ②시각 5 · ③조용 4 · ⑧⑨배지 19 · ⑩단계 13 · ⑤버전 12
· ④누적 20 · ⑪성적표 17 · ⑫모드 14                    합 107칸
칸 사이 여백 9 을 더하면                                  116칸
폰 한 줄은 56칸                                    🔴 60칸 넘친다
```

👉 **한 줄에 다 못 넣는다.** §2 의 포함 관계(«무엇을 접었을 때 무엇이 사라지나»)가
   그래서 있는 것이고, **접힌 상태가 기본**이어야 한다.

## 4-3. 🔴 낱말 붙이기가 **반만** 되고 있다

`deviceScreenBadge` 가 «폭을 아끼려고 낱말 사이를 붙인다» 면서
`label.replace(" ", "")` 를 쓴다 — **`replace` 는 첫 공백 하나만 지운다.**

| 원문 | 지금 | 의도대로면 |
|---|---|---|
| 알 수 없는 화면 | 알수 없는 화면 (14칸) | 알수없는화면 (**12칸**) |
| 취소 불가 팝업 | 취소불가 팝업 (13칸) | 취소불가팝업 (12칸) |
| 픽업 완료 대기 | 픽업완료 대기 (13칸) | 픽업완료대기 (12칸) |
| 배송 완료 대기 | 배송완료 대기 (13칸) | 배송완료대기 (12칸) |

⚠️ **고치기 전에 물어야 한다** — «반만 붙은» 지금이 오히려 읽기 좋을 수도 있다.
   `알수없는화면` 은 붙여 놓으면 읽기 힘들다. **폭 2칸을 아낄 값어치가 있는가**가 물음이다.

---

# 5. 폰이 여럿일 때 (2026-09-05 · 목업에서 봄)

> 기사님: *"이 영역도 필터처럼 늘어나도록 할까? 폰이 2~3개 되면 더 복잡해질 텐데."*

**폰이 늘면 물음이 바뀐다** — 한 대일 때는 «이 폰이 일하나»지만
여러 대면 «**어느 폰이 문제인가**»다. 정상인 것은 볼 이유가 없다.

## 5-1. 산수

폰 하나가 **12~17칸**이다 (이름 3~4 + 배지 8~13 + 시각 5 + 모드 4, 여백 포함).

| 폰 | 한 줄 | 56칸에 드나 |
|---|--:|---|
| 1대 | ≈26칸 | 🟢 넉넉하다 |
| **2대** | **≈50칸** | 🟡 **아슬아슬하게 든다** |
| 3대 | ≈73칸 | 🔴 **넘친다** |

👉 **2대까지는 한 줄, 3대부터 접어야 한다.**

## 5-2. 지금 목업은 «넘치는 것을 보여 준다»

3대를 고르면 **가로로 밀린다** — 숨기지 않는다 (규칙 ④).
**넘치는 것을 눈으로 봐야** 어떻게 접을지 정할 수 있기 때문이다.

## 5-3. 🔴 모드는 폰마다 하나씩이다

**«셋 다 바꾸기»로 묶지 않았다** — 픽커만 대기로 두고 싶을 때가 있다.
그래서 접을 때 걸린다: 접힌 줄에서 모드를 어떻게 보여 주나?

| | 안 | 문제 |
|---|---|---|
| **가** | 접힘에서는 **안 보인다** — 바꾸려면 펼친다 | 자주 바꾸는 값이 아니면 괜찮다 |
| 나 | 셋이 같으면 그 값, 다르면 «섞임» | «셋 다 바꾸기»가 위험하다 |
| 다 | 대표 폰 하나의 모드만 | «대표»를 누가 정하나가 또 축이 된다 |

⏳ **기사님이 모드를 하루에 몇 번 바꾸시는지가 이것을 가른다** — 아직 안 정했다.

## 5-4. ⋯ 접힌 넷도 폰 수만큼 는다

누적·버전·작업 단계·필터 배지는 **폰마다 하나씩**이라, 3대면 펼쳤을 때 **세 줄**이 된다.
