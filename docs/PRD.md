# 1DAL 글로벌 기획서 (Global Master PRD)

본 문서는 `onedal-app`(전방 스캐너)과 `onedal-web`(관제탑 서버) 두 개의 애플리케이션이 융합되어, 물류 현장의 악질 생태계와 속도전을 어떻게 극복하고 궁극적인 AI 배차 관제 시스템으로 거듭나는지를 정의한 핵심 설계도입니다.

---

## 🕒 1. 기기별 폴링 & 선점 경합 타임라인 (3-Phones 시나리오)
인성앱의 고유 기능(동기화)과 매크로들의 경합 상황을 시간대별로 재구성한 기초 원리입니다.

| 경과 시간(T) | 📱 1번 폰 (먼저 로딩) | 📱 2번 폰 (확정배차기능 사용) | 📱 3번 폰 (나중에 로딩) | ☁️ 인성 서버의 상태 (DB) |
| :--- | :--- | :--- | :--- | :--- |
| **T+0초** | (로딩 1초 전) 리스트 대기 중 | (로딩 2초 전) 리스트 대기 중 | (로딩 3초 전) 리스트 대기 중 | 꿀콜 row 등록 (주인 없음) |
| **T+1초** | **(로딩 지금)** 새 리스트 수신 후 꿀콜 발견 ➡️ `[상세 페이지 진입]` | (로딩 1초 전) 리스트 대기 중 | (로딩 2초 전) 리스트 대기 중 | 콜의 임시 대기 상태 유지 |
| **T+2초** | 상세 페이지 정보 확인 중 (머뭇거림) | **(로딩 지금)** 새 리스트 수신 후 조건 맞는 꿀콜 발견 ➡️ `[상세 페이지 진입]` | (로딩 1초 전) 리스트 대기 중 | (아직 어느 폰도 확정 안 누름) |
| **T+3초** | 사용자 머리로 판단 중 | **즉시 `[확정]` 버튼 터치! (0.01초)** | **(로딩 지금)** 새 리스트 수신 (하지만 꿀콜 안보임) | **2번 폰의 확정 요청 수신** |
| **T+4초** | 뒤늦게 `[확정]` 클릭 시도 (광클) | 서버 응답 수신 ➡️ **확정 성공! (`[취소]` 버튼 활성화, 진짜 화면)** | 리스트 수신 완료 ➡️ **할당 완료로 미노출** | **2번 폰을 콜 주인으로 할당** |
| **T+5초** | 서버 응답 ➡️ **"확정 실패"** | (승리) 데스밸리 (30초) 판별 대기 | 다음 로딩 대기 중 | 1번 폰에게 실패 응답 전송 |
| **T+6초** | 알럿 확인 후 리스트로 쫓겨남 | `[닫기]` or `[취소]` 선택 | 다음 로딩 대기 중 | 2번 폰 연결 유지 중 |

*   **분석 포인트**: 2번 폰이 늦게 리스트를 받았음에도, 선빵(광클)을 통해 인성 서버의 권한을 획득했습니다. 확정 후에도 우측 상단 박스 위에 활성화된 `[취소]` 버튼을 통해 우리는 최악의 똥콜을 버릴 **[데스밸리 30초]** 를 확보할 수 있습니다.

---

## 🏆 2. 1DAL 다중 연속 사냥 타임라인 (첫 콜 + 합짐 연속 선점 시나리오) 🌟 NEW 🌟
승욱님이 설계하신 인성앱의 '1초 시차 로딩(엇박자)' 시스템을 활용해, 1번 폰이 첫 콜을 잡은 직후 서버가 필터를 갱신하고, 연이어 2번 폰이 합짐 콜을 잡아내는 13초간의 압도적인 연속 사냥 타임라인입니다.

| 경과 시간(T) | ☁️ 1DAL 서버 | 📱 1DAL 웹폰 (관제) | 📱 1DAL 앱폰1 (먼저 로딩) | 📱 1DAL 앱폰2 (1초 뒤 로딩) | 📱 확정배차사용폰 (지지기) | ☁️ 인성 서버 (DB) |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **T+0초** | 콜필터 디폴트 등록 | 앱폰의 필터 상황 표현 | (인성앱 시작) 새 리스트 수신, 콜필터 적용 | - | - | |
| **T+1초** | - | - | (로딩 2초 전) 이전 리스트 보는 중 | (인성앱 시작) 새 리스트 수신, 콜필터 적용 | - | |
| **T+2초** | - | - | (로딩 1초 전) 이전 리스트 보는 중 | (로딩 2초 전) 이전 리스트 보는 중 | (인성앱 시작) 새 리스트 수신 꿀콜 없음 | |
| **T+3초** | 콜 기본 정보 수신<br>화물 여유 확인 후 '콜 대기필터' 변경 | 콜 설정 노출<br>`[취소]` `[닫기]` 노출 | **(로딩 지금)** 새 리스트 수신.<br>콜 필터 통과 ➡️ 상세 페이지 진입 후 `[확정]`<br>**인성콜 확정 후 API 호출** | (로딩 1초 전) 이전 리스트 보는 중 | (로딩 2초 전) 이전 리스트 보는 중 | 꿀콜 row 등록<br>(주인 없음) |
| **T+4초** | 콜 상세 정보 수신<br>화물 여유 확인 + 카카오 맵 API 확인 | 콜 설정 노출<br>`[상차지 전화]` 추가노출 | 확정 상세 페이지 (상차지,하차지,적요) 확인<br>➡️ API 콜 | **(로딩 지금)** 새 리스트 수신.<br>콜 필터 통과 ➡️ 상세 페이지 진입 후 `[확정]`<br>**인성콜 : 등록실패 (이미 뺏김)** | (로딩 1초 전) 이전 리스트 보는 중 | DB row에<br>**앱폰1 할당** |
| **T+5초** | 콜 수행 여부 판단 | 콜 설정 노출<br>'콜 수행 여부 판단' 추가노출 | API 리턴 대기 중 | (로딩 지금) 새 리스트 수신, 콜대기필터 수신<br>(해당 콜은 앱폰1이 삼켜서 안 보임) | (로딩 지금) 새 리스트 수신<br>(해당 콜은 앱폰1이 삼켜서 안 보임) | |
| **T+6초** | 합짐 콜필터 등록<br>DB 운행일지 row 생성<br>콜필터 '합짐'으로 변경 후 등록<br>카카오 내비 url 생성 ➡️ 앱폰1 API 리턴 | 사용자 꿀콜 판단 후 `[닫기]` 클릭<br>카카오 톡 `[길안내]` 버튼 추가노출 | API 유지 판정 ➡️ **`[닫기]` 눌림** | (로딩 2초 전) 이전 리스트 보는 중 | (로딩 2초 전) 이전 리스트 보는 중 | |
| **T+7초** | 대기 중 | 대기 중 | **(로딩 지금)** 새 리스트 수신<br>**새로운 합짐 콜필터** 수신 | (로딩 1초 전) 이전 리스트 보는 중 | (로딩 1초 전) 이전 리스트 보는 중 | |
| **T+8초** | 대기 중 | 대기 중 | (로딩 2초 전) 이전 리스트 보는 중 | **(로딩 지금)** 새 리스트 수신<br>대기 필터로 리스트 클릭 없음 | (로딩 지금) 새 리스트 수신 | |
| **T+9초** | 콜 기본 정보 수신<br>화물 여유 확인 후 '콜 대기필터' 변경 | 콜 설정 노출<br>`[취소]` `[닫기]` 노출 | (로딩 1초 전) 이전 리스트 보는 중 | **(로딩 지금)** 새 리스트 수신, 합짐 콜필터 수신<br>콜 합짐필터 통과 ➡️ 상세 페이지 진입 후 `[확정]`<br>**인성콜 확정 후 API 호출** | (로딩 2초 전) 이전 리스트 보는 중 | 두 번째 꿀콜<br>row 등록 |
| **T+10초** | 콜 상세 정보 수신<br>화물 여유 확인 + 카카오 맵 API 확인 | 콜 설정 노출<br>`[상차지 전화]` 추가노출 | (로딩 지금) 새 리스트 수신<br>(해당 콜은 앱폰2가 삼켜서 안 보임) | 확정 상세 페이지 (상차지,하차지,적요) 확인<br>➡️ API 콜 | (로딩 1초 전) 이전 리스트 보는 중 | DB row에<br>**앱폰2 할당** |
| **T+11초** | 콜 수행 여부 판단 | 콜 설정 노출<br>'콜 수행 여부 판단' 추가노출 | - | API 리턴 대기 중 | (로딩 지금) 새 리스트 수신 | |
| **T+12초** | 두 번째 합짐 콜필터 등록<br>DB 운행일지 row 생성<br>콜필터 합짐으로 변경 후 등록<br>카카오 내비 url 생성 ➡️ 앱폰2 API 리턴 | 사용자 꿀콜 판단 후 `[닫기]` 클릭<br>카카오 톡 `[길안내]` 버튼 추가노출 | - | API 유지 판정 ➡️ **`[닫기]` 눌림** | (로딩 2초 전) 이전 리스트 보는 중 | |

*   **1DAL 아키텍처의 우월성**: 기사님은(1DAL 웹폰) 운전대만 잡고 앞만 보고 있으면 됩니다. 백그라운드에서 **앱폰1, 앱폰2** 가 1초 시차 교대로 인성 서버를 털어대며(선빵), 훔쳐온 콜들을 **1DAL 서버** 가 카카오 API로 0.n초 만에 걸러주고, 걸러진 결과만 **기사님 폰(관제웹과 카톡)** 에 예쁜 UI와 내비게이션 링크로 상납하는 완벽한 3각 연동 마이크로서비스(MSA)입니다!

---

## ⚡ 2. 100% 서버 하이브리드 연산 모델 (Shoot First, Ask Later)

### 🛡️ [1단계: 무지성 엣지 광클 (0.01초 기계적 선빵)]
폰은 선택하지 않은 리스트(쓰레기 콜) 데이터도 서버로 보내 필터 업데이트용으로 활용합니다. 폰은 로컬(로딩된 폰 메모리)에 저장된 관제탑 필터 조건만 맞으면 **무조건 `[확정]`** 을 갈겨 콜을 훔쳐 옵니다.

### 🧠 [2단계: 관제탑 30초 정밀 검수 (데스밸리 타임)]
콜을 삼킨 폰은 화면의 텍스트를 `onedal-web` 서버로 던지고 연산 결과 리턴을 기다립니다.
*   **Fail-Safe 장치**: 서버 무응답 시 무조건 **'30초 후 스스로 앱 내 [취소] 클릭'** 타이머 가동.
*   **서버 API 리턴**: 카카오 API 통행료/시간 계산 후 `{"action": "확정(유지)"}` 또는 `{"action": "취소"}` 정보 응답.

### 🗑️ [3단계: 데스밸리 판정에 따른 폰 동작 (뱉기 vs 사냥 재개)]
*   **똥콜 판정 (취소 하달)**: 툴비/까대기로 취소 명령을 받으면 즉결 상단의 `[취소]` 버튼 터치 (페널티 감수).
*   **꿀콜 판정 (사냥 재개)**: 유지 명령을 받으면 즉시 하단 **`[닫기]` 버튼 터치하여 리스트 화면으로 복귀 (다음 합짐 사냥)**.

### 🗼 [4단계: 관제탑(서버)이 해야 할 핵심 후속 작업]
콜 배차가 무사히 확정 유지되었다면 관제탑은 백그라운드 작업을 실행합니다:
1.  **필터 값 전파**: 필드에 나간 모든 폰들에게 **추가/변경된 합짐 타겟 필터(회랑 좌표)** 를 업데이트(Push).
2.  **카카오 내비 전송**: 경유지가 추가된 스마트 TSP 정렬 카카오맵 링크를 기사님 카카오톡으로 전송.
3.  **운행 일지 DB 세팅**: DB에 오더 row를 생성하여 즉시 통화 내용(화물 제원)을 입력할 수 있게 대비.

---

## 🎛️ 3. 기기 원격 관제 및 텔레메트리 (Remote Fleet Control)
관제 웹에서 개별 폰의 사냥 성과를 보고 `[오토-확정]`, `[반자동-읽기만]` 토글을 원격 제어하여 누적 페널티 킬스위치(Kill-Switch) 관리.

---

## 🏧 4. 출근 전 프리플라이트 (잔고 락 & 엇박자 부팅)
*   **엇박자 폴링(Time-Staggered Polling)**: 기기 3대를 1.5초 시차를 두고 켜서, 인성 서버의 5초 주기 새로고침의 한계를 우회하여 **1.6초마다 실시간 폴링**을 거두는 초단타 체계 구축.

---

## 🚫 5. 바닥콜(Floor Call) 지뢰 탐지기 (Toxic Filter)
2분 넘게 살아있는 콜은 통계적 똥콜(지뢰)로 간주. 광역주소 암막 화주, "전화 금지" 등 갑질 적요는 폰에 저장된 필터 선에서 0.001초 만에 스킵.

---

## 🧭 6. 동적 합짐(On-The-Way) 레이더 및 카카오 TSP 통합 라우팅
*   **지리 회랑(Corridor) 필터링**: 현재 배차된 본콜의 이동 방향(가상의 5km 파이프라인) 반경 안에 떨어지는 콜만 앱폰의 레이더망에서 1차 여과.
*   **서버-투-서버(S2S) 우회 연산**: 인성앱에서 낚아챈 합짐 콜의 데이터를 **관제탑 백엔드(Node.js RAM)**가 직접 물고 카카오 모빌리티 API와 즉각 통신. 프론트엔드의 부하를 없애고 0.2초 내로 기존 노선 대비 '추가 소요시간(분)'과 '추가 거리(km)' 패널티를 정밀 도출.
*   **통합 타임라인 시각화 (Subway UI)**: 복잡하게 얽힌 합짐(경유지)들의 방문 순서를 관제 대시보드 상단에 지하철 노선도 형태(`상차 ➡️ 상차 ➡️ 하차 ➡️ 하차`)로 일직선화 배열. 운전 중 직관적인 가시성 제공.

---

## 🗣️ 7. 통화 대본 & 기어 재연산(Re-Routing) 동기화
전화 시 `"몇 파레트죠?"` 대본 화면 제공. 입력한 CBM(용적)을 실시간으로 서버에 날려 후속 합짐 연산의 공간 제한 필터에 즉각 락을 검.

---

## 🛣️ 8. 듀얼 라우팅(Dual Routing) 가성비 엔진
RECOMMEND(고속) vs FARE(무료) 듀얼 계산. `[톨비 4천 원 절감 = 시간 12분 페널티]` 의 명확한 기회비용 평가 브리핑.

---

## 🔮 9. 돌발 지연 시 복귀콜 확률 AI 리라우팅
ETA -20분 통화 강제 알람. 특정 하차 지연 시 '오지 고립 생존 확률' 빅데이터를 분석, 다른 목적지 우회 추천.

---

## 👨‍💼 10. 기사 맞춤형 까대기 회피 모드
동네 방어 모드 시 육체노동(수작업) 스킵 가중치, 유료도로 회피 우선 보정치 적용.

---

## 🛠 11. 로우 레벨 코어 아키텍처 (RESTful 피기백 통신)
웹소켓(Socket.io)의 모바일 끊김을 지양하고, 폰의 POST 요청 응답 패킷(Piggyback) 꼬리에 데스밸리 명령이나 새 필터 룰셋을 실어 보내는 고효율 경량 통신망.

---
## 🎯 최종 운영 목표
단순한 캡처 매크로를 넘어, 대한민국 화물 시장의 비대칭 데이터와 시간차를 완벽하게 점유하는 궁극의 **A.I Fleet Management Cockpit** 구현.