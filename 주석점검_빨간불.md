# 주석 점검 — 코드와 달랐던 말(🔴) 한 장

주석에서 경위를 걷고 «지금 상태와 까닭»만 남기는 일을 하면서, **주석이 코드와 다른 말을 하던 자리**를 모았다.
전부 **주석만** 고쳤다. 코드는 한 줄도 바꾸지 않았고, 파일마다 주석을 뺀 코드 글자가 그대로인지 대조했다.

- 점검한 파일: 코드 2백여 개 + 검사 319개
- 커밋: 파일 하나에 `chore(주석): <경로>` 하나
- 게이트: 문서 감사 · 서버 타입 · 서버 검사 2,166 · 관제웹 타입 · 관제웹·shared 검사 1,032 · 시뮬레이터 검사 195 · 죽은 코드 · 린트 — 전부 통과

## 기사님이 정하실 것

| # | 무엇이 갈리나 | 지금 이렇게 돈다 | 어디 |
|---|---|---|---|
| 1 | **하차 마감**을 재는 식이 둘이다 | 시간 계산은 «상차 마감 + 단독 주행 + 휴식 여유 30분». 경로 타임라인은 «상차 완료 + 배송 주행 × 150%» | shared `timing.ts` · 서버 경로 조립 |
| 2 | **하차 약속의 바닥**이 둘이다 | 타임라인 검사는 «도착 예상으로 눌러 올리지 않는다». 서버 단계 시딩은 도착 예상을 바닥으로 쓴다 | 서버 `stepSeeder.ts` · `timelineDeadlineCap.test.ts` |
| 3 | **지도를 확대하면 «어둡게»가 옅어진다** (4배에서 다 걷힌다) | 기사님 말씀은 «줌인할수록 흐려지는 기능은 필요 없을꺼 같아» | 관제웹 `mapProjection.ts` `tileToneFor` |
| 4 | **경로 순서 비교 도구**의 화면 제목이 지금 동작과 다르다 | 서버는 «지나가는 길목부터» 순서로 짠다. 도구 제목은 «상차 먼저(지금)» | `scripts/route-order.mjs` |
| 5 | **«[임시 · Phase 7 도입 시 삭제]» 표시**가 아직 임시인가 | 미완료 콜을 날짜와 무관하게 3일까지 되살린다 | shared `index.ts` · 서버 배차 엔진 |

## shared

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `callNet.ts` | 예외 장치(homeCaught)는 폐기했다 | 살아 있는 목적지를 `activeGoals` 가 정하며 그 값을 받는다 |
| `callNet.ts` | `PHASE_FIELDS.home` 도 같은 말 | `PHASE_FIELDS` 가 없다. `PHASE_AUTO_SOURCE.home` 만 있다 |
| `callNet.ts` | 사각형의 기점은 현위치 | 라인이 생기면 마지막 하차지·목적지를 기점으로 쓴다 |
| `callOptions.ts` | 이 표는 아직 아무도 안 쓴다 | 서버가 `call_options` 를 읽고, 정차 값은 네 곳이 읽는다 |
| `callOptions.ts` | `stop_cargo_reports` 가 이미 같은 방식 | 그 표는 없다. 갈래는 `category` 칸으로 가른다 |
| `cargoTags.ts` | 그 상수는 쓰는 곳이 없다 | 그 상수 자체가 코드에 없다 |
| `stepRecords.ts` | 옛 테이블이 철거되면 유일한 생산자가 된다 | 그 두 표는 이미 없다. 지금 유일한 생산자다 |
| `judgment.ts` | `PHASE_FIELDS` 가 이미 같은 패턴이다 | `PHASE_FIELDS` 가 없다 |
| `phases.ts` | 국면 표(`user_filter_phases`)의 칸에서 나온다 · 국면 칸은 `PhaseSettings` 에 산다 | 그 표도 그 타입도 없다. 값 다섯은 한 벌이다 |
| `timing.ts` | 하차 마감 = 상차 완료 + 배송 × 150% | 상차 마감 + 단독 주행 + 휴식 여유 30분 (**위 결정 1**) |
| `timing.ts` | 휴식 여유는 대체 때 함께 제거 | 아직 두 곳이 읽는다 |
| `index.ts` | 운행중 국면이 자기 경유 허용값을 갖는다(기본 0) | 값은 국면마다 두지 않는다. 필터에 저장한 반경 한 벌 |
| 여러 파일 | 설계서 §4-2 · 명세 §6 · 필터.md §10-1 같은 가리킴 | 그 문서들이 레포에 없다 |

## server

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `SettingsRepository.ts` | 콜할인율의 원천은 국면별 표 | 그 표는 없다. 필터 값 한 벌에 있다 |
| `routes/settings.ts` | 지도 데이터는 수도권만 있다 | 충청권도 들어 있다 |
| `kakaoService.ts` | 도시 별칭은 지도에서 센다 | «광주시 → 광주» 같은 글자 규칙으로 만든다 |
| `userSessionStore.ts` | 노선·반경·할인율의 원천은 국면 표 | shared 의 기본 필터 값 하나 |
| `core/helpers.ts` | 주행분을 저장할 칸이 없어 재기동하면 잃는다 | 칸이 있고 복구 때 읽는다 |
| `routeComposer.ts` | 경로 순서를 바꿀지는 별개 문제(재는 중) | 이미 «지나가는 길목부터»로 짠다 |
| `services/geoService.ts` | 도착 감지는 다음 정거장 하나만 본다 | 순서를 보지 않고 500m 안의 후보를 모두 본다 |
| `routes/orders.ts` · `routes/devices.ts` | 수락 안 한 미리보기는 시간으로도 치운다 | 그런 타이머가 없다. 폰이 끊기면(자체 보고·통신 두절) 기기 쪽이 치운다 |
| `scripts/route-order.mjs` | 실제 카카오 요청은 상차 전부 먼저 | 지나가는 길목부터다 (**위 결정 4**) |

## 원달앱

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `core/engine/ScanContext.kt` | 인성 수순은 아직 서비스 안에 있고 함수는 아직 안 옮긴다 | 이미 인성 전용 파일로 옮겨져 있다 |
| `plugins/insung/InsungSequence.kt` | 팝업 하나만 먼저 옮겼고 나머지는 뒤따른다 | 상세·확정·팝업 셋이 모두 이 파일에 있다 |
| `plugins/hwamul24/Hwamul24Parser.kt` | 요금 조건 머리에 «조건 3»과 «조건 2»가 겹쳐 있었다 | 도착지가 조건 2다. 요금은 조건 3 |

## 관제웹

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `OrderFilterModal.tsx` | 국면 탭 다섯 · 국면별 콜할인율 · 국면 저장 통로 · 자동이면 반경 넷을 흐린다 | 탭이 없고 값은 한 벌이다. 흐리는 것은 «지금 안 쓰는 칸»뿐 |
| `OrderFilterStatus.tsx` | 국면에 관내(민트)가 있다 | 노선행·복귀행 둘뿐이다 |
| `JudgmentSeat.tsx` | 미리보기는 서버가 시간으로 치운다 | 폰이 상세를 떠나면 꺼진다 |
| `JudgmentSettingsTab.tsx` | 저장 버튼 없이 바꾸면 바로 적용된다 | 「적용」과 「되돌리기」 버튼이 있다 |
| `DepartureCountdown.tsx` | 약속을 모르면 일과 17시·이동 제외 2시간으로 추정 | 상차는 «잡은 시각 + 상차 약속 분», 하차는 «상차 완료 + 배송 주행 × 비율» |
| `NaviQr.tsx` | 다음 정거장 하나만 보내고 경유지는 안 쓴다 | 경유 3곳 + 도착 1곳을 한 QR 에 담는다 |
| `DeviceControlPanel.tsx` | 모드 버튼 셋이 나란히 있다 | 버튼 하나를 누르면 셋이 펼쳐진다 |
| `stageRules.ts` · `sheetTransition.ts` | 손으로 만진 뒤 30초는 자동이 못 바꾼다 · 이미 「다」면 그대로 둔다 | 새 판정과 필터 열림이 손 유예보다 먼저다. 어느 높이에서든 「나」로 간다 |
| `PinnedRouteCanvas.tsx` | 다녀온 곳에 동그라미를 친다 | 다녀온 곳의 테두리는 투명이다 |
| `CallDeck.tsx` · `callPalette.ts` | 다녀온 곳은 흰 링 · 번호 색은 지도 테마 색 | 링은 투명이고, 번호 색은 콜 색을 쓴다 |
| `Dashboard.tsx` | 노선·동선을 기억하지 않는다 · 곁 패널 330px | 서버 필터에 저장한다 · 346px |
| `StatusBoard.tsx` | 앱 판정을 꺼내는 문이 없다 · 서버가 필터 버전을 안 남긴다 | 문이 있고, 서버는 기기 세션에 남긴다 |
| `useMasterGps.ts` | 실 GPS 가 15초 끊기면 시뮬이 이어 달린다 | 기사님이 켜고 실 GPS 가 없을 때만 돈다 |
| `mapProjection.ts` | 배경 톤은 배율과 상관없이 같다 | 캔버스는 배율에 따라 옅어지는 값을 쓴다 (**위 결정 3**) |
| `finishedCalls.ts` · `Drawer.tsx` | 서랍과 옛 화면 두 곳이 끝난 콜을 읽는다 | ☰ 서랍 하나다 |
| `StageSheet.tsx` | 세 높이 이름 · 높이의 원천 · 끄는 동안 시트가 손을 따라온다 | 이름이 다르고, 40px 마다 한 단씩 옮긴다 |
| `labPortMap.ts` | 하차 약속은 상차 완료부터 세고 상차가 늦으면 같이 밀린다 | 상차 약속 + 싣는 분 + 배송 × 150% 라 밀리지 않는다 |

## 시뮬레이터 · 운행일지

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `packages/core-simulator/src/presets.ts` | 없는 문제지 여섯(지도청주 · 볼첨지 · 축 · 오탐 · 서진 · 초월이천)을 길게 설명 | 지금 문제지는 아홉이다 |
| 〃 | 칠지점은 알람 3번(01·03·05) | 05 는 막히는 콜이라 둘(01·03)이다 |
| 〃 | 없는 문제지 이름을 부르면 조용히 랜덤으로 돈다 | 시뮬레이터가 멈춘다 |
| `insung/insungCall.ts` | 앱 파서가 차종 정규식으로 읽는다 | 그 이름이 없다. «승»을 포함한 차종 목록이 기준이다 |
| `nets.ts` | 넷째 배차망(픽커)을 붙일 때 | 배차망은 셋이다 |
| `InsungOngoingDetailScreen.tsx` | 탁송 버튼이 콜을 지운다 | 그 버튼은 아무 일도 하지 않는다 |
| `injectedCall.ts` | 옛 서버면 칸이 없다 | 서버는 늘 싣는다 |

## 검사 파일

| 파일 | 주석이 하던 말 | 코드가 하는 일 |
|---|---|---|
| `timing.test.ts` | 상차 마감 = 잡은 시각 + 60분 | 상차 약속 = 잡은 시각 + 20분, 마감은 약속 + 상차 정차 |
| `stepSeeder.test.ts` · `judgment.test.ts` | 상차 약속 기본 30~60분 | 20분 |
| `vehicles.test.ts` | 1t짐 = 라보 둘 = 다마스 셋 | 라면박스 단위(1t 100 · 1t짐 80 · 라보 40 · 다마스 30 · 승용차 5 · 오토바이 1) |
| `deviceMode.test.ts` | 모드는 셋이고 직접도 필터가 돈다 | 넷이다(자동·알람·직접·체험). 직접은 필터를 켜지 않는다 |
| `stopOrderStability.test.ts` · `gpsArrival.test.ts` | 도착 감시는 첫 정거장 하나만 본다 · 모의는 근접만으로 찍힌다 | 후보 전부를 본다 · 모의는 «서 있다»는 틱이 와야 찍힌다 |
| `phaseUi.test.ts` | 국면 탭 · 확인창을 거치는 복귀 전환 · 클라이언트가 그물을 부른다 | 탭도 확인창도 없다. 지도는 필터에서 영역을 그린다 |
| `detour.test.ts` · `phaseApply.test.ts` | 국면마다 경유 허용값을 갖고 전환 때 펼친다 | 값은 한 벌이라 펼 것이 없다 |
| `driverOriginFallback.test.ts` · `filterReview.test.ts` | 기점을 비우고 메우는 함수 넷 | 그 함수들이 없다. 물을 때마다 고른다 |
| `helpers.test.ts` | `completeOrder` · `startTwoTrack` 이 갱신한다 | 두 함수가 없다 |
| `cancelBudget.test.ts` | 한도 `/10` 은 관제웹 문자열이고 서버는 모른다 | 한도는 shared 한 곳이고 서버가 쓴다 |
| `mapCoverage.test.ts` | 지도는 수도권만이라 청주는 0개이고 이 검사는 반드시 실패한다 | 청주 세 구와 오창읍이 이미 들어 있다 |
| `newCallVisible.test.ts` | 조회 탭에 머물면 새 콜을 놓친다 | 끝난 콜은 ☰ 서랍에 있고 새 콜이 오면 서랍을 닫는다 |
| `soloMinutes.test.ts` | 합짐 콜은 단독 주행을 가질 방법이 없다 | KEEP 뒤 한 번 잰다 |
| `gpsTrack.test.ts` | 20점 또는 30초마다 쓰고 5MB 에서 멈춘다 | 5점 또는 10초마다 쓴다. 멈추는 코드는 없다 |
| `stepSkip.test.ts` | 현장 스킵은 세션 안에만 남는다 | 현장 스킵도 서버에 간다 |
| `previewCall.test.ts` | 네 곳이 각자 취소를 센다 | 세는 자리는 한 곳이다 |
| `KakaoPickerParserTest.kt` | 상세 낱말이 하나라도 섞이면 상세 · 문제지는 2023 추정 | 두 낱말이 다 있어야 한다 · 문제지는 실물 캡처다 |
| `WorkStageTest.kt` | 관제웹은 `isHolding` 하나만 받는다 | 앱이 단계·걸음·초를 보내고 관제웹이 이름을 짓는다 |
| `SimulatorButtonTest.kt` | 설정 화면 버튼이 시뮬레이터 앱을 켠다 | 그 버튼이 없다 |
| `TapShiftTest.kt` | 요금 글자에서 왼쪽으로 옮겨 찍는다 | 카드 줄을 찾으면 줄 왼쪽 끝을 찍는다 |

## 검사 자체의 구멍 (고치지 않았다)

- **가짜 초록불** — 필터 창 검사 셋이 없는 함수 이름까지 소스를 자르려 한다. 못 찾으면 파일 끝까지 보므로 엉뚱한 자리의 같은 글자에도 통과한다 (`phaseUi.test.ts`).
- **못 잡는 검사** — «자동일 때만 누른다»를 지키는 검사가, 체험 모드도 누르는 지금 코드에서 그대로 통과한다 (`deviceMode.test.ts`).
- **경위를 요구하는 검사** — 콜 카드의 상·하차 줄을 다시 걷지 않게 하려고, 소스 주석에 «철거»라는 말이 있어야 통과하는 검사가 있다 (`timeDisplay.test.ts`). 지금은 «이 줄은 그와 달라 철거 대상이 아니다»라는 규칙 문장으로 남겼다.
- **검사 제목은 코드라 손대지 않았다** — 제목에 날짜와 지금 동작과 다른 말이 남아 있는 곳이 여럿이다.

## 지운 기사님 말씀

규칙(값·결정)인 말씀은 모두 남겼다. 지운 것은 **버그 신고 · 질문 · 작업 지시 · 지금 규칙과 반대인 지난 결정**이다. 대표적인 것만 적는다.

| 갈래 | 원문 |
|---|---|
| 지난 결정 | *"첫짐 도착반경 5km 로 콜을 잡다가 첫짐을 잡으면 … 저장된 합짐 도착반경 1km 를 꺼내와 콜을 잡고 싶은 거야."* |
| 지난 결정 | *"모든 탭마다 키를 가지고 있고 탭마다 디스플레이만 달리해서 숨기고 노출하면 될 듯."* |
| 지난 결정 | *"지워버려, 그거 있으면 너가 또 딴소리해"* (옛 내비 화면) |
| 질문 | *"이제 우리에게 국면이라는 것이 없어진 것 같은데.. 원칙이 바뀐 거 아냐?"* |
| 질문 | *"뭐가 우리 테스트에 가장 큰 문제야?"* |
| 증상 신고 | *"경로가 이상하다.."* · *"궤적이 엉망이야"* · *"신둔면이 6이었다가 3이었다가"* |
| 증상 신고 | *"사용자 설정에는 파주가 선택되어 있고 새로고침하고 필터 열어 보면 용인"* |
| 증상 신고 | *"분명 폰 이름 1234에 파란불이 들어와 있었어."* |
| 작업 지시 | *"시니어 개발자라 생각하고 먼발치에서 … 오류를 찾아 리뷰해."* |

## 남은 자잘한 것

- 시뮬레이터 문제지 파일에 **쓰는 곳 없는 지점 상수가 약 20개** 남아 있다 (광주 남동 · 서진 여섯 · 지도확장 일곱 등). 주석만 다루는 일이라 코드는 두었다.
- `onedal-map/README.md` 의 지도 개수(«지금 11·28·41»)가 실제 지도(충청 포함)와 다르다.
- 검사 파일에 한글 식별자가 있다(`시딩`·`실으러가기` 등). 식별자 감사는 서버 `tests/` 를 훑지 않아 걸리지 않는다.
- 원달앱의 Kotlin 검사는 이번에 돌리지 않았다. 바뀐 것이 주석뿐이고, 파일마다 주석을 뺀 코드가 그대로인지 대조했다.
